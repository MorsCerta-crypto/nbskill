

<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

## Restored command-line tools

[`install_nbskill`](https://MorsCerta-crypto.github.io/nbskill/cli.html#install_nbskill)
installs the nbskill skill and writes the project-local Codex MCP
configuration.
[`nbskill_mcp_log`](https://MorsCerta-crypto.github.io/nbskill/cli.html#nbskill_mcp_log)
prints the recent MCP metrics and problems, while
[`nbskill_mcp_log_problems`](https://MorsCerta-crypto.github.io/nbskill/cli.html#nbskill_mcp_log_problems)
limits the report to problems. These terminal commands work without an
already-connected MCP client; `nbskill_mcp` remains the MCP server entry
point.

``` python
from fastcore.test import *
```

------------------------------------------------------------------------

<a
href="https://github.com/MorsCerta-crypto/nbskill/blob/main/nbskill/cli.py#L17"
target="_blank" style="float:right; font-size:smaller">source</a>

### install_nbskill

``` python
def install_nbskill(
    target:str='codex', # codex, claude, cursor, both, or custom with skills_dir
    skills_dir:str | None=None, # Parent skills directory for a custom target
    skill_name:str='jupyter-notebooks', # Installed skill directory name
    overwrite:bool=True, # Update stale managed files and MCP entries
    install_hooks:bool=False, # Install nbdev pre-commit hooks in the current project
    restart_mcp:bool=True, # Print reconnect guidance after configuring the server
    cursor_workspace:str | None=None, # Cursor workspace, or global configuration when omitted
    codex_workspace:str | None='.', # Project workspace for .codex/config.toml
    reference_roots:str='~/projects', # Local Git repositories to register
    index_references:bool=True, # Index discovered references during installation
):
```

*Install current nbskill instructions and optional MCP configuration.*

### Read MCP logs

Use
[`nbskill_mcp_log`](https://MorsCerta-crypto.github.io/nbskill/cli.html#nbskill_mcp_log)
for the recent tool and problem summary. Use
[`nbskill_mcp_log_problems`](https://MorsCerta-crypto.github.io/nbskill/cli.html#nbskill_mcp_log_problems)
when only failures and warnings matter. Both accept `--path`, `--limit`,
and `--json-output`.

------------------------------------------------------------------------

<a
href="https://github.com/MorsCerta-crypto/nbskill/blob/main/nbskill/cli.py#L45"
target="_blank" style="float:right; font-size:smaller">source</a>

### nbskill_mcp_log

``` python
def nbskill_mcp_log(
    path:str | None=None, # MCP JSONL log path; uses NBSKILL_MCP_LOG or ~/.nbskill-mcp.jsonl
    limit:int=20, # Maximum problem and tool rows to print
    json_output:bool=False, # Print machine-readable JSON
):
```

*Print concise nbskill MCP log metrics and recent problems.*

------------------------------------------------------------------------

<a
href="https://github.com/MorsCerta-crypto/nbskill/blob/main/nbskill/cli.py#L55"
target="_blank" style="float:right; font-size:smaller">source</a>

### nbskill_mcp_log_problems

``` python
def nbskill_mcp_log_problems(
    path:str | None=None, # MCP JSONL log path; uses NBSKILL_MCP_LOG or ~/.nbskill-mcp.jsonl
    limit:int=20, # Maximum problem rows to print
    json_output:bool=False, # Print machine-readable JSON
):
```

*Print only the problem-focused nbskill MCP log summary.*

``` python
import tomllib
from pathlib import Path

from fastcore.test import *
```

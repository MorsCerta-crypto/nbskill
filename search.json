[
  {
    "objectID": "04_review.html",
    "href": "04_review.html",
    "title": "nbskill",
    "section": "",
    "text": "Diff and style feedback that focuses on code cells.\nReview should focus on the behavior encoded in code cells, not on notebook metadata churn. This notebook keeps that review loop small: run fast.ai style hints when desired, and print nbdev-style code diffs when comparing notebooks.\nReview tools draw a line between source changes and notebook noise. style_check is useful before publishing exported code because it combines fast.ai style hints with notebook hygiene reports, while diff_nb is useful during agent edits because it ignores outputs and metadata unless metadata is the only thing that changed.\n\n\nReview tools are production core. Validation must fail on missing or stale nbskill metadata, style_check must combine capped style output with notebook hygiene diagnostics, and diff_nb must show code-cell behavior changes without raw notebook metadata noise.\n\nimport os\nfrom fastcore.nbio import new_nb\nfrom fastcore.test import test_eq\nfrom nbskill.foundation import demo_path, remove_demo_path, write_demo_notebook, write_tool_notebook\n\n\ndef _write_review_notebook(path, cells, stamp=True):\n    nb = new_nb(cells)\n    if stamp: stamp_notebook_metadata(nb)\n    _write_nb(nb, path)\n    return nb\n\n\n\n\nstyle_check wraps the fast.ai style checker as a direct Python API; MCP exposes its common diagnostics through doctor.\nPublic notebook APIs stay maintainable when their docs, implementation, examples, and tests travel together. public_function_literacy_problems checks exported public functions for that compact contract so maintainers and agents can quickly understand what each function is for, how to call it, and what behavior is protected.\n\nsource\n\n\n\ndef public_function_literacy_problems(\n    path:str='.'\n):\nReturn docs/example/test contract warnings for exported public notebook functions.\nNotebook craft is a small subset of style diagnostics that should steer agents toward literate, reviewable notebook changes.\n\n_literacy_preview = public_function_literacy_problems(\"nbs/04_review.ipynb\")[:2]\n_literacy_preview\n\n[{'code': 'public-function-markdown',\n  'path': '04_review.ipynb',\n  'severity': 'warning',\n  'source': 'nbskill',\n  'detail': \"public function 'notebook_size_problems' needs Markdown docs cell mentioning the function\",\n  'cell_id': '93269841',\n  'symbol': 'notebook_size_problems',\n  'line': 1,\n  'missing': 'Markdown docs cell mentioning the function'},\n {'code': 'public-function-example',\n  'path': '04_review.ipynb',\n  'severity': 'warning',\n  'source': 'nbskill',\n  'detail': \"public function 'notebook_size_problems' needs example cell that calls the function\",\n  'cell_id': '93269841',\n  'symbol': 'notebook_size_problems',\n  'line': 1,\n  'missing': 'example cell that calls the function'}]\n\n\n\nassert {\n    \"public-function-docstring\", \"public-function-markdown\",\n    \"public-function-example\", \"public-function-test\",\n} &lt;= {problem[\"code\"] for problem in public_function_literacy_problems(\"nbs/04_review.ipynb\")}\n\n\nsource\n\n\n\ndef notebook_validation_problems(\n    path:str='.'\n):\nReturn notebook metadata and Python syntax validation problems.\n\nsource\n\n\n\ndef notebook_size_problems(\n    path:str='.'\n):\nReturn size warnings for notebook cells and generated Python files.\n\nsource\n\n\n\ndef reset_global_usage_summary():\nCall self as a function.\n\nsource\n\n\n\ndef notebook_autofix(\n    path:str='.', dry_run:bool=False\n):\nCall self as a function.\n\nsource\n\n\n\ndef style_report(\n    path:str='.', # File or folder to check\n    chkstyle:dict | None=None, # Captured chkstyle result to include\n    max_output_chars:int=12000, # Maximum raw chkstyle text to keep in report\n    max_diagnostics:int=200, # Maximum parsed chkstyle diagnostics\n    changed_only:bool=False, # Report only diagnostics for changed code cells\n    ref_a:str | None='HEAD', # First git ref for changed_only filtering\n    ref_b:str | None=None, # Second git ref; defaults to working tree\n    skip_folder_re:str | None=None, # Regex for notebook paths to skip\n    skip_path:str | None=None, # Folder name/path to skip\n):\nReturn structured style diagnostics, problem chart, and global nbskill usage data.\n\nwith write_tool_notebook(\"04_review_style_report.ipynb\") as path:\n    report = style_report(path)\n    test_eq({\"summary\", \"notebook_problems\"} &lt;= set(report), True)\n    test_eq(isinstance(report[\"notebook_problems\"], list), True)\n    test_eq(\"Global nbskill usage:\" in report[\"text\"], True)\n\nThe structured report is meant for tools as much as people. summary gives a compact count of problems, problem_chart groups diagnostics by source and code, and text is the human-readable report.\n\nsample_chkstyle = {\"status\": 1, \"output\": \"# demo.ipynb:cell[abc123]:2: Missing whitespace (hint: add a blank line)\"}\nwith write_tool_notebook(\"04_review_sample_report.ipynb\") as path:\n    sample_report = style_report(path, chkstyle=sample_chkstyle, max_output_chars=200, max_diagnostics=5)\n    print(sample_report[\"summary\"])\n    print(sample_report[\"problem_chart\"][\"by_source\"])\n    print(sample_report[\"diagnostics\"][0])\n\n\nsource\n\n\n\ndef run_style_check(\n    path:str='.', skip_folder_re:NoneType=None, skip_path:NoneType=None, strict:bool=False,\n    max_output_chars:NoneType=None\n):\nRun chkstyle with nbskill’s default skip paths and capped output.\n\nargv = _style_check_argv(\".\")\nassert \"--skip-path\" in argv\nassert \"_proc\" in argv\nassert \"--skip-folder-re\" not in argv\nassert \"--skip-path-re\" in _style_check_argv(\".\", skip_folder_re=r\"^_proc/\")\n\n\nproc_report = run_style_check(\"_proc/01_read.ipynb\")\nassert proc_report[\"status\"] == 0\nassert proc_report[\"output\"] == \"\"\nassert notebook_validation_problems(\"_proc/01_read.ipynb\") == []\nassert not any(_style_root_is_skipped(problem[\"path\"], skip_style_paths) for problem in _notebook_style_problems(\".\"))\n\n\nsource\n\n\n\ndef style_check(\n    path:str='.', skip_folder_re:str | None=None, # Regex for folders to skip\n    skip_path:str | None=None, # Folder name/path to skip\n    strict:bool=False, # Raise when style hints or notebook hygiene problems are found\n    delete_after_output:bool=False, # Reset ~/.nbskill-errors.json after printing the global summary\n    max_output_chars:int=12000, # Cap printed chkstyle output\n    max_diagnostics:int=200, # Cap returned diagnostics\n    fix:bool=False, # Show conservative fix suggestions\n    dry_run:bool=True, # Keep fix mode non-mutating by default\n    changed_only:bool=False, # Report only diagnostics for changed code cells\n    ref_a:str | None='HEAD', # First git ref for changed_only filtering\n    ref_b:str | None=None, # Second git ref; defaults to working tree\n):\nPrint capped fast.ai style hints, notebook hygiene warnings, and global tool usage.\n\nsource\n\n\n\ndef validate_nbs(\n    path:str='nbs', strict:bool=True, # Raise when invalid metadata is found\n):\nValidate nbskill metadata needed for safe notebook tools.\n\nsource\n\n\n\ndef code_source(\n    cell\n):\nReturn source for code cells; ignore markdown and raw cells.\nstyle_check is the direct Python wrapper around style_report. It captures fast.ai style output, appends notebook hygiene findings, prints the combined text report, and raises in strict mode when diagnostics are present.\n\nwith write_tool_notebook(\"04_review_style_check.ipynb\") as path:\n    style_output = StringIO()\n    with redirect_stdout(style_output): style_check(path, strict=False, max_output_chars=500, max_diagnostics=5)\n    print(\"\\n\".join(style_output.getvalue().splitlines()[:6]))\n\n\nwith write_demo_notebook(\"04_review_validate_valid.ipynb\") as valid_path:\n    _write_review_notebook(valid_path, [mk_cell(\"assert True\", cell_type=\"code\")])\n    assert notebook_validation_problems(valid_path) == []\n    validate_nbs(str(valid_path), strict=False)\n\n\nwith write_demo_notebook(\"04_review_validate_invalid.ipynb\") as invalid_path:\n    invalid_nb = _write_review_notebook(invalid_path, [\n        mk_cell(\"x = 1\", cell_type=\"code\"),\n        mk_cell(\"Some docs\", cell_type=\"markdown\"),\n        mk_cell(\"assert True\", cell_type=\"code\"),\n    ])\n    invalid_nb.cells[1].metadata[\"nbskill\"][\"semantic_types\"] = []\n    invalid_nb.cells[2].metadata[\"nbskill\"][\"cell_type\"] = \"markdown\"\n    _write_nb(invalid_nb, invalid_path)\n    codes = {problem[\"code\"] for problem in notebook_validation_problems(invalid_path)}\n    assert {\"missing-cell-semantic-types\", \"cell-type-mismatch\"} &lt;= codes\n\n\nwith write_demo_notebook(\"04_review_export_hash.ipynb\", base=\"nbs\") as export_nb_path:\n    export_nb = _write_review_notebook(export_nb_path, [mk_cell(\"#| default_exp sample_tool\")])\n    generated_py_path = exported_py_path(export_nb_path, export_nb)\n    try:\n        generated_py_path.parent.mkdir(parents=True, exist_ok=True)\n        generated_py_path.write_text(\"actual = 1\\n\", encoding=\"utf-8\")\n        export_nb.metadata[\"nbskill\"] = {\"exported_py_hash\": \"bad\"}\n        _write_nb(export_nb, export_nb_path)\n        codes = {problem[\"code\"] for problem in notebook_validation_problems(export_nb_path)}\n        assert \"exported-py-hash-mismatch\" in codes\n    finally:\n        if generated_py_path is not None: remove_demo_path(generated_py_path)\n\nnotebook_validation_problems is narrower than style_report: it checks nbskill metadata plus Python syntax in code cells, the prerequisites for reliable notebook tooling. validate_nbs exposes the same failures to Python callers.\n\nwith write_demo_notebook(\"04_review_validation_example.ipynb\") as validation_path:\n    _write_review_notebook(validation_path, [\n        mk_cell(\"x = 1\", cell_type=\"code\"),\n        mk_cell(\"Some docs\", cell_type=\"markdown\"),\n    ], stamp=False)\n    problems = notebook_validation_problems(validation_path)\n    print([problem[\"code\"] for problem in problems[:4]])\n\n\n\n\nNotebook diffs are noisy when metadata and outputs are included. diff_nb asks nbdev for code-cell source on each side of a comparison and prints only the added, changed, or deleted code blocks the caller requested.\n\n\n\nFastHTML and HTML-producing notebook cells often hide customer-visible copy inside Python component calls. A normal text search finds too much: headings, button labels, docstrings, route names, tests, exceptions, and implementation notes all look like strings. The public UI text surface gives agents a smaller first pass over likely visible copy before editing.\nUse visible_text_inventory before an edit when the task is phrased in product or customer language. It answers: which notebook cell and symbol owns the visible words I may need to change? That reduces the manual rg loop where an agent has to compare notebook source, exported modules, and internal strings by hand.\nUse diff_nb(surface=\"public-ui\") after an edit when the review question is user-facing copy rather than Python structure. It keeps headings, labels, link text, image alt text, and similar UI literals separate from code churn, metadata churn, tests, and docstring edits.\nThis is deliberately heuristic. It is meant to support a faster notebook iteration loop, not to prove accessibility or render every possible runtime branch: inventory the likely visible text, make a focused edit, run the route/example smoke snippet, then review the public UI diff.\n\nsource\n\n\n\ndef visible_text_inventory(\n    path:str='.', include_re:NoneType=None, max_records:int=200\n):\nPrint likely user-visible text from FastHTML/HTML-producing notebook code cells.\n\nsource\n\n\n\ndef diff_nb(\n    path:str, # Notebook path\n    ref_a:str | None='HEAD', # First git ref; use None for working tree\n    ref_b:str | None=None, # Second git ref; defaults to working tree\n    adds:bool=True, # Include code cells added in ref_b\n    changes:bool=True, # Include changed code cells\n    dels:bool=False, # Include deleted code cells\n    cell_id:str | None=None, # Limit output to comma/space-separated cell ids\n    after_id:str | None=None, # Limit output to cells after this id in ref_b/the working tree\n    surface:str='code', # Review surface: \"code\" or \"public-ui\"\n):\nPrint nbdev-style diffs for code cells, or a public UI text surface.\n\nfrom tempfile import gettempdir\n\nwith write_demo_notebook(\"04_review_no_git.ipynb\", base=gettempdir()) as path:\n    _write_review_notebook(path, [mk_cell(\"x = 1\", cell_type=\"code\")])\n    try:\n        diff_nb(str(path))\n    except (SystemExit, ValueError) as exc:\n        if isinstance(exc, SystemExit): assert exc.code == 1\n        else:\n            msg = str(exc)\n            assert \"disposable notebook\" in msg and \"ref_a=None\" in msg and \"ref_b=None\" in msg\n    out = StringIO()\n    with redirect_stdout(out):\n        diff_nb(str(path), ref_a=None, ref_b=None)\n    report = out.getvalue()\n    assert \"--- code cell\" in report and \"+x = 1\" in report\n\n\ndef _committed_review_notebook(root, cells=None):\n    root.mkdir()\n    path = root / \"demo.ipynb\"\n    _write_review_notebook(path, cells or [mk_cell(\"x = 1\", cell_type=\"code\")])\n    subprocess.run([\"git\", \"init\"], cwd=root, check=True, capture_output=True)\n    subprocess.run([\"git\", \"add\", \"demo.ipynb\"], cwd=root, check=True, capture_output=True)\n    subprocess.run([\n        \"git\", \"-c\", \"user.name=Nbskill\", \"-c\", \"user.email=nbskill@example.com\",\n        \"commit\", \"-m\", \"base\",\n    ], cwd=root, check=True, capture_output=True)\n    return path\n\n\nroot = demo_path(\"04_review_git_metadata\")\ntry:\n    path = _committed_review_notebook(root)\n    nb = read_nb(path)\n    nb.cells[0].metadata[\"nbskill\"] = {\"cell_type\": \"code\", \"semantic_types\": []}\n    _write_nb(nb, path)\n    out = StringIO()\n    with redirect_stdout(out):\n        diff_nb(str(path))\n    text = out.getvalue()\n    assert \"No code cell changes\" in text\n    assert \"Ignored nbskill metadata changes in 1 cell\" in text\nfinally:\n    remove_demo_path(root)\n\n\nroot = demo_path(\"04_review_git_filters\")\ntry:\n    path = _committed_review_notebook(root)\n    nb = read_nb(path)\n    first_id = nb.cells[0].id\n    nb.cells[0].source = \"x = 2\"\n    nb.cells.append(mk_cell(\"y = 3\", cell_type=\"code\"))\n    _write_nb(nb, path)\n    out = StringIO()\n    with redirect_stdout(out): diff_nb(str(path), cell_id=first_id)\n    text = out.getvalue()\n    assert \"x = 2\" in text and \"y = 3\" not in text\n    out = StringIO()\n    with redirect_stdout(out): diff_nb(str(path), after_id=first_id)\n    text = out.getvalue()\n    assert \"y = 3\" in text and \"x = 2\" not in text\n    delta = style_report(str(path), changed_only=True)\n    assert delta[\"summary\"][\"changed_only\"] is True\n    assert delta[\"summary\"][\"changed_cell_count\"] == 2\nfinally:\n    remove_demo_path(root)\n\n\ndef _write_style_problem_notebook(path):\n    long_source = \"\\n\".join([f\"x{i} = {i}\" for i in range(31)])\n    long_markdown = \"\\n\".join([f\"paragraph {i}\" for i in range(21)])\n    mixed_import = mk_cell(\"import json\", cell_type=\"code\")\n    mixed_import.outputs = [dict(output_type=\"stream\", name=\"stdout\", text=\"json\\n\")]\n    _write_review_notebook(path, [\n        mk_cell(\n            \"#| export\\n\"\n            \"def a():\\n\"\n            \"    pass\\n\"\n            \"def b():\\n\"\n            \"    \\\"\\\"\\\"Describe b.\\\"\\\"\\\"\\n\"\n            \"    pass\\n\"\n            \"def c():\\n\"\n            \"    \\\"\\\"\\\"Line one.\\n\"\n            \"    Line two.\\\"\\\"\\\"\\n\"\n            \"    pass\\n\"\n            \"def _private():\\n\"\n            \"    pass\",\n            cell_type=\"code\",\n        ),\n        mk_cell(long_source, cell_type=\"code\"),\n        mk_cell(long_markdown, cell_type=\"markdown\"),\n        mk_cell(\"assert 1 == 1\\nassert 2 == 2\\nassert 3 == 3\\nassert 4 == 4\", cell_type=\"code\"),\n        mk_cell(\"#| export\\nimport os\", cell_type=\"code\"),\n        mk_cell(\"#| export\\nimport os\", cell_type=\"code\"),\n        mixed_import,\n        mk_cell(\"import sys\", cell_type=\"code\"),\n        mk_cell(\"import sys\", cell_type=\"code\"),\n        mk_cell(\"result = later_helper()\", cell_type=\"code\"),\n        mk_cell(\"def later_helper():\\n    return 1\", cell_type=\"code\"),\n        mk_cell(\"def loader():\\n    return MissingPath('x')\", cell_type=\"code\"),\n    ])\n\n_write_literacy_problem_notebook builds a compact demo notebook for the literate review contract. The fixture keeps compliant, missing-docs, missing-example, missing-test, and multiline-docstring cases in one place so each warning code has a precise example.\n\ndef _write_literacy_problem_notebook(path):\n    _write_review_notebook(path, [\n        mk_cell(\"`ready` has docs, a docstring, an example, and a test.\", cell_type=\"markdown\"),\n        mk_cell(\"#| export\\ndef ready(x):\\n    \\\"Return x plus one.\\\"\\n    return x + 1\", cell_type=\"code\"),\n        mk_cell(\"`no_example` has docs and a test but no example call.\", cell_type=\"markdown\"),\n        mk_cell(\"#| export\\ndef no_example(x):\\n    \\\"Return x plus one.\\\"\\n    return x + 1\", cell_type=\"code\"),\n        mk_cell(\"`no_test` has docs and an example but no test assertion.\", cell_type=\"markdown\"),\n        mk_cell(\"#| export\\ndef no_test(x):\\n    \\\"Return x plus one.\\\"\\n    return x + 1\", cell_type=\"code\"),\n        mk_cell(\"`bad_doc` has a Markdown note but its docstring is too long.\", cell_type=\"markdown\"),\n        mk_cell(\"#| export\\ndef bad_doc(x):\\n    \\\"\\\"\\\"Return x plus one.\\n    Keep this second line out of file_context.\\\"\\\"\\\"\\n    return x + 1\", cell_type=\"code\"),\n        mk_cell(\"`eq_checked`, `ne_checked`, and `fail_checked` cover fastcore test helpers.\", cell_type=\"markdown\"),\n        mk_cell(\"#| export\\ndef eq_checked(x):\\n    \\\"Return x plus one.\\\"\\n    return x + 1\", cell_type=\"code\"),\n        mk_cell(\"#| export\\ndef ne_checked(x):\\n    \\\"Return x plus one.\\\"\\n    return x + 1\", cell_type=\"code\"),\n        mk_cell(\"#| export\\ndef fail_checked():\\n    \\\"Raise a demo failure.\\\"\\n    raise ValueError(\\\"boom\\\")\", cell_type=\"code\"),\n        mk_cell(\"#| export\\ndef no_docs(x):\\n    \\\"Return x plus one.\\\"\\n    return x + 1\", cell_type=\"code\"),\n        mk_cell(\"#| export\\ndef _hidden(x):\\n    return x\\n\\ndef test_helper(x):\\n    return x\", cell_type=\"code\"),\n        mk_cell(\"ready(1)\\nno_docs(1)\\nno_test(1)\\nbad_doc(1)\\neq_checked(1)\\nne_checked(1)\\ntry:\\n    fail_checked()\\nexcept ValueError:\\n    pass\", cell_type=\"code\"),\n        mk_cell(\"assert ready(1) == 2\\nassert no_docs(1) == 2\\nassert no_example(1) == 2\\nassert bad_doc(1) == 2\\ntest_eq(eq_checked(1), 2)\\ntest_ne(ne_checked(1), 3)\\ntest_fail(lambda: fail_checked())\\nassert _hidden(1) == 1\\nassert test_helper(1) == 1\", cell_type=\"code\"),\n    ])\n\n\nwith write_demo_notebook(\"04_review_literacy.ipynb\") as path:\n    _write_literacy_problem_notebook(path)\n    problems = public_function_literacy_problems(path)\n    codes = {(problem.get(\"symbol\"), problem[\"code\"]) for problem in problems}\n    missing_codes = {(\"no_docs\", \"public-function-markdown\"), (\"no_example\", \"public-function-example\"), (\"no_test\", \"public-function-test\")}\n    ready_codes = {code for symbol, code in codes if symbol == \"ready\"}\n    assert missing_codes &lt;= codes\n    assert not ready_codes\n\n\nwith write_demo_notebook(\"04_review_literacy_docstrings.ipynb\") as path:\n    _write_literacy_problem_notebook(path)\n    problems = public_function_literacy_problems(path)\n    codes = {(problem.get(\"symbol\"), problem[\"code\"]) for problem in problems}\n    tested_codes = {\n        (\"eq_checked\", \"public-function-test\"),\n        (\"ne_checked\", \"public-function-test\"),\n        (\"fail_checked\", \"public-function-test\"),\n    }\n    assert (\"bad_doc\", \"public-function-docstring\") in codes\n    assert not tested_codes & codes\n    assert not any(problem.get(\"symbol\") in {\"_hidden\", \"test_helper\"} for problem in problems)\n\n\nprint(report)\n\nNotebook style report:\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=18cf6a7f\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=3e6417a6\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=9d964403\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=56e0e6c4\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=4debe573\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=742a5cf5\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=f41bdaef\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=3b1110e1\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=23f2d90a\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=4268cb78\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=762d5962\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=711b360d\n- large-cell: nbs/data/04_review_style.ipynb id=18cf6a7f function_count=4 4 top-level functions; split into one-idea cells\n- large-cell: nbs/data/04_review_style.ipynb id=3e6417a6 line_count=31 31 code content lines; keep one idea per cell\n- large-cell: nbs/data/04_review_style.ipynb id=9d964403 line_count=21 21 markdown content lines; keep one idea per cell\n- public-function-docstring: nbs/data/04_review_style.ipynb id=18cf6a7f line=1 symbol='a' missing='one-line docstring' docstring_lines=0 public function 'a' needs a one-line docstring for context summaries\n- public-function-markdown: nbs/data/04_review_style.ipynb id=18cf6a7f line=1 symbol='a' missing='Markdown docs cell mentioning the function' public function 'a' needs Markdown docs cell mentioning the function\n- public-function-example: nbs/data/04_review_style.ipynb id=18cf6a7f line=1 symbol='a' missing='example cell that calls the function' public function 'a' needs example cell that calls the function\n- public-function-test: nbs/data/04_review_style.ipynb id=18cf6a7f line=1 symbol='a' missing='test cell that asserts or checks the function' public function 'a' needs test cell that asserts or checks the function\n- public-function-markdown: nbs/data/04_review_style.ipynb id=18cf6a7f line=3 symbol='b' missing='Markdown docs cell mentioning the function' public function 'b' needs Markdown docs cell mentioning the function\n- public-function-example: nbs/data/04_review_style.ipynb id=18cf6a7f line=3 symbol='b' missing='example cell that calls the function' public function 'b' needs example cell that calls the function\n- public-function-test: nbs/data/04_review_style.ipynb id=18cf6a7f line=3 symbol='b' missing='test cell that asserts or checks the function' public function 'b' needs test cell that asserts or checks the function\n- public-function-docstring: nbs/data/04_review_style.ipynb id=18cf6a7f line=6 symbol='c' missing='one-line docstring' docstring_lines=2 public function 'c' has 2 docstring lines; keep it to one line for context summaries\n- public-function-markdown: nbs/data/04_review_style.ipynb id=18cf6a7f line=6 symbol='c' missing='Markdown docs cell mentioning the function' public function 'c' needs Markdown docs cell mentioning the function\n- public-function-example: nbs/data/04_review_style.ipynb id=18cf6a7f line=6 symbol='c' missing='example cell that calls the function' public function 'c' needs example cell that calls the function\n- public-function-test: nbs/data/04_review_style.ipynb id=18cf6a7f line=6 symbol='c' missing='test cell that asserts or checks the function' public function 'c' needs test cell that asserts or checks the function\n- multi-problem-test: nbs/data/04_review_style.ipynb id=56e0e6c4 4 asserts/test functions; split into one-problem-at-a-time cells\n- unclean-cell: nbs/data/04_review_style.ipynb id=f41bdaef semantic_types=test_cell, private_code\n- duplicate-import: nbs/data/04_review_style.ipynb scope=exported import_key='import os as os' cells=4debe573, 742a5cf5\n- duplicate-import: nbs/data/04_review_style.ipynb scope=internal import_key='import sys as sys' cells=3b1110e1, 23f2d90a\n- cell-order: nbs/data/04_review_style.ipynb id=4268cb78 line=1 symbol='later_helper' confidence=high called before definition/import in later cell id=762d5962\n- missing-import: nbs/data/04_review_style.ipynb id=711b360d line=2 symbol='MissingPath' confidence=medium called without an earlier definition/import\n\n\n\nwith write_demo_notebook(\"04_review_generated.ipynb\") as generated_path:\n    generated_nb = _write_review_notebook(generated_path, [mk_cell(\"#| default_exp big_review_demo\")])\n    generated_py_path = exported_py_path(generated_path, generated_nb)\n    try:\n        generated_py_path.parent.mkdir(parents=True, exist_ok=True)\n        generated_py_path.write_text(\"\\n\".join(f\"x{i} = {i}\" for i in range(1001)), encoding=\"utf-8\")\n        size_codes = {problem[\"code\"] for problem in notebook_size_problems(generated_path)}\n        assert \"large-generated-py\" in size_codes\n    finally:\n        if generated_py_path is not None: remove_demo_path(generated_py_path)\n\n\nwith write_demo_notebook(\"04_review_style.ipynb\") as path:\n    _write_style_problem_notebook(path)\n    custom_map = demo_path(\"04_review_errors.json\")\n    old_map = os.environ.get(\"NBSKILL_FAILURE_MAP\")\n    try:\n        os.environ[\"NBSKILL_FAILURE_MAP\"] = str(custom_map)\n        with redirect_stdout(StringIO()):\n            style_check(str(path), delete_after_output=True)\n        assert \"usage:\" in _format_global_usage_summary()\n        assert not custom_map.exists()\n    finally:\n        if old_map is None: os.environ.pop(\"NBSKILL_FAILURE_MAP\", None)\n        else: os.environ[\"NBSKILL_FAILURE_MAP\"] = old_map\n\n\nwith write_demo_notebook(\"04_review_style.ipynb\") as path:\n    _write_style_problem_notebook(path)\n    try:\n        with redirect_stdout(StringIO()): style_check(str(path), strict=True)\n    except RuntimeError:\n        pass\n    else:\n        raise AssertionError(\"strict style_check should raise for notebook hygiene problems\")\n\n\nassert code_source(mk_cell(\"plain docs\", cell_type=\"markdown\")) is None\nassert code_source(mk_cell(\"answer = 42\", cell_type=\"code\")) == \"answer = 42\"\n\ndiff_nb deliberately compares code-cell source only. Markdown edits and notebook metadata churn stay out of the main diff so reviewers can see the executable behavior that changed.\n\ndiff_root = demo_path(\"04_review_diff_example\")\nremove_demo_path(diff_root)\ntry:\n    diff_root.mkdir()\n    diff_path = diff_root / \"demo.ipynb\"\n    _write_nb(new_nb([\n        mk_cell(\"value = 1\\nvalue\", cell_type=\"code\"),\n        mk_cell(\"Original note\", cell_type=\"markdown\"),\n    ]), diff_path)\n    subprocess.run([\"git\", \"init\"], cwd=diff_root, check=True, capture_output=True)\n    subprocess.run([\"git\", \"add\", \"demo.ipynb\"], cwd=diff_root, check=True, capture_output=True)\n    subprocess.run([\n        \"git\", \"-c\", \"user.name=Nbskill\", \"-c\", \"user.email=nbskill@example.com\",\n        \"commit\", \"-m\", \"base\",\n    ], cwd=diff_root, check=True, capture_output=True)\n\n    diff_nb_json = read_nb(diff_path)\n    diff_nb_json.cells[0].source = \"value = 2\\nvalue\"\n    diff_nb_json.cells[1].source = \"Updated note that will not appear in the code diff\"\n    _write_nb(diff_nb_json, diff_path)\n\n    diff_output = StringIO()\n    with redirect_stdout(diff_output):\n        diff_nb(str(diff_path), ref_a=\"HEAD\")\n    print(diff_output.getvalue())\nfinally:\n    remove_demo_path(diff_root)\n\nHere is the intended workflow on a tiny FastHTML-shaped notebook: first inventory likely visible text, then review just the public UI surface. The output is compact enough for an agent to paste into a status update or use as a pre-edit checklist.\n\nfrom tempfile import gettempdir\n\nui_source = \"\"\"\ndef _hero():\n    return Div(\n        H1('Welcome'),\n        P('Compare providers in minutes.'),\n        Button('Start now'),\n        Img(src='/logo.png', alt='Company logo'),\n    )\n\nraise ValueError('internal diagnostic, not customer copy')\n\"\"\"\n\nwith write_demo_notebook(\"04_review_public_ui_demo.ipynb\", base=gettempdir()) as ui_path:\n    _write_review_notebook(ui_path, [mk_cell(ui_source, cell_type=\"code\")])\n\n    print(\"Inventory before editing:\")\n    visible_text_inventory(ui_path)\n\n    print(\"\\nPublic UI diff for the current notebook contents:\")\n    diff_nb(str(ui_path), ref_a=None, ref_b=None, surface=\"public-ui\")\n\nInventory before editing:\ncell_id | line | symbol | call | text\na19e06a5 | 4 | _hero | H1 | Welcome\na19e06a5 | 5 | _hero | P | Compare providers in minutes.\na19e06a5 | 6 | _hero | Button | Start now\na19e06a5 | 7 | _hero | Img.alt | Company logo\n\nPublic UI diff for the current notebook contents:\n--- public-ui cell a19e06a5 ---\n--- \n+++ \n@@ -0,0 +1,4 @@\n+H1: Welcome\n+P: Compare providers in minutes.\n+Button: Start now\n+Img.alt: Company logo\n\n\nReview functions are direct Python APIs; MCP consolidates their common diagnostics through doctor.",
    "crumbs": [
      "Reviewing notebooks"
    ]
  },
  {
    "objectID": "04_review.html#reviewing-notebooks",
    "href": "04_review.html#reviewing-notebooks",
    "title": "nbskill",
    "section": "",
    "text": "Diff and style feedback that focuses on code cells.\nReview should focus on the behavior encoded in code cells, not on notebook metadata churn. This notebook keeps that review loop small: run fast.ai style hints when desired, and print nbdev-style code diffs when comparing notebooks.\nReview tools draw a line between source changes and notebook noise. style_check is useful before publishing exported code because it combines fast.ai style hints with notebook hygiene reports, while diff_nb is useful during agent edits because it ignores outputs and metadata unless metadata is the only thing that changed.\n\n\nReview tools are production core. Validation must fail on missing or stale nbskill metadata, style_check must combine capped style output with notebook hygiene diagnostics, and diff_nb must show code-cell behavior changes without raw notebook metadata noise.\n\nimport os\nfrom fastcore.nbio import new_nb\nfrom fastcore.test import test_eq\nfrom nbskill.foundation import demo_path, remove_demo_path, write_demo_notebook, write_tool_notebook\n\n\ndef _write_review_notebook(path, cells, stamp=True):\n    nb = new_nb(cells)\n    if stamp: stamp_notebook_metadata(nb)\n    _write_nb(nb, path)\n    return nb\n\n\n\n\nstyle_check wraps the fast.ai style checker as a direct Python API; MCP exposes its common diagnostics through doctor.\nPublic notebook APIs stay maintainable when their docs, implementation, examples, and tests travel together. public_function_literacy_problems checks exported public functions for that compact contract so maintainers and agents can quickly understand what each function is for, how to call it, and what behavior is protected.\n\nsource\n\n\n\ndef public_function_literacy_problems(\n    path:str='.'\n):\nReturn docs/example/test contract warnings for exported public notebook functions.\nNotebook craft is a small subset of style diagnostics that should steer agents toward literate, reviewable notebook changes.\n\n_literacy_preview = public_function_literacy_problems(\"nbs/04_review.ipynb\")[:2]\n_literacy_preview\n\n[{'code': 'public-function-markdown',\n  'path': '04_review.ipynb',\n  'severity': 'warning',\n  'source': 'nbskill',\n  'detail': \"public function 'notebook_size_problems' needs Markdown docs cell mentioning the function\",\n  'cell_id': '93269841',\n  'symbol': 'notebook_size_problems',\n  'line': 1,\n  'missing': 'Markdown docs cell mentioning the function'},\n {'code': 'public-function-example',\n  'path': '04_review.ipynb',\n  'severity': 'warning',\n  'source': 'nbskill',\n  'detail': \"public function 'notebook_size_problems' needs example cell that calls the function\",\n  'cell_id': '93269841',\n  'symbol': 'notebook_size_problems',\n  'line': 1,\n  'missing': 'example cell that calls the function'}]\n\n\n\nassert {\n    \"public-function-docstring\", \"public-function-markdown\",\n    \"public-function-example\", \"public-function-test\",\n} &lt;= {problem[\"code\"] for problem in public_function_literacy_problems(\"nbs/04_review.ipynb\")}\n\n\nsource\n\n\n\ndef notebook_validation_problems(\n    path:str='.'\n):\nReturn notebook metadata and Python syntax validation problems.\n\nsource\n\n\n\ndef notebook_size_problems(\n    path:str='.'\n):\nReturn size warnings for notebook cells and generated Python files.\n\nsource\n\n\n\ndef reset_global_usage_summary():\nCall self as a function.\n\nsource\n\n\n\ndef notebook_autofix(\n    path:str='.', dry_run:bool=False\n):\nCall self as a function.\n\nsource\n\n\n\ndef style_report(\n    path:str='.', # File or folder to check\n    chkstyle:dict | None=None, # Captured chkstyle result to include\n    max_output_chars:int=12000, # Maximum raw chkstyle text to keep in report\n    max_diagnostics:int=200, # Maximum parsed chkstyle diagnostics\n    changed_only:bool=False, # Report only diagnostics for changed code cells\n    ref_a:str | None='HEAD', # First git ref for changed_only filtering\n    ref_b:str | None=None, # Second git ref; defaults to working tree\n    skip_folder_re:str | None=None, # Regex for notebook paths to skip\n    skip_path:str | None=None, # Folder name/path to skip\n):\nReturn structured style diagnostics, problem chart, and global nbskill usage data.\n\nwith write_tool_notebook(\"04_review_style_report.ipynb\") as path:\n    report = style_report(path)\n    test_eq({\"summary\", \"notebook_problems\"} &lt;= set(report), True)\n    test_eq(isinstance(report[\"notebook_problems\"], list), True)\n    test_eq(\"Global nbskill usage:\" in report[\"text\"], True)\n\nThe structured report is meant for tools as much as people. summary gives a compact count of problems, problem_chart groups diagnostics by source and code, and text is the human-readable report.\n\nsample_chkstyle = {\"status\": 1, \"output\": \"# demo.ipynb:cell[abc123]:2: Missing whitespace (hint: add a blank line)\"}\nwith write_tool_notebook(\"04_review_sample_report.ipynb\") as path:\n    sample_report = style_report(path, chkstyle=sample_chkstyle, max_output_chars=200, max_diagnostics=5)\n    print(sample_report[\"summary\"])\n    print(sample_report[\"problem_chart\"][\"by_source\"])\n    print(sample_report[\"diagnostics\"][0])\n\n\nsource\n\n\n\ndef run_style_check(\n    path:str='.', skip_folder_re:NoneType=None, skip_path:NoneType=None, strict:bool=False,\n    max_output_chars:NoneType=None\n):\nRun chkstyle with nbskill’s default skip paths and capped output.\n\nargv = _style_check_argv(\".\")\nassert \"--skip-path\" in argv\nassert \"_proc\" in argv\nassert \"--skip-folder-re\" not in argv\nassert \"--skip-path-re\" in _style_check_argv(\".\", skip_folder_re=r\"^_proc/\")\n\n\nproc_report = run_style_check(\"_proc/01_read.ipynb\")\nassert proc_report[\"status\"] == 0\nassert proc_report[\"output\"] == \"\"\nassert notebook_validation_problems(\"_proc/01_read.ipynb\") == []\nassert not any(_style_root_is_skipped(problem[\"path\"], skip_style_paths) for problem in _notebook_style_problems(\".\"))\n\n\nsource\n\n\n\ndef style_check(\n    path:str='.', skip_folder_re:str | None=None, # Regex for folders to skip\n    skip_path:str | None=None, # Folder name/path to skip\n    strict:bool=False, # Raise when style hints or notebook hygiene problems are found\n    delete_after_output:bool=False, # Reset ~/.nbskill-errors.json after printing the global summary\n    max_output_chars:int=12000, # Cap printed chkstyle output\n    max_diagnostics:int=200, # Cap returned diagnostics\n    fix:bool=False, # Show conservative fix suggestions\n    dry_run:bool=True, # Keep fix mode non-mutating by default\n    changed_only:bool=False, # Report only diagnostics for changed code cells\n    ref_a:str | None='HEAD', # First git ref for changed_only filtering\n    ref_b:str | None=None, # Second git ref; defaults to working tree\n):\nPrint capped fast.ai style hints, notebook hygiene warnings, and global tool usage.\n\nsource\n\n\n\ndef validate_nbs(\n    path:str='nbs', strict:bool=True, # Raise when invalid metadata is found\n):\nValidate nbskill metadata needed for safe notebook tools.\n\nsource\n\n\n\ndef code_source(\n    cell\n):\nReturn source for code cells; ignore markdown and raw cells.\nstyle_check is the direct Python wrapper around style_report. It captures fast.ai style output, appends notebook hygiene findings, prints the combined text report, and raises in strict mode when diagnostics are present.\n\nwith write_tool_notebook(\"04_review_style_check.ipynb\") as path:\n    style_output = StringIO()\n    with redirect_stdout(style_output): style_check(path, strict=False, max_output_chars=500, max_diagnostics=5)\n    print(\"\\n\".join(style_output.getvalue().splitlines()[:6]))\n\n\nwith write_demo_notebook(\"04_review_validate_valid.ipynb\") as valid_path:\n    _write_review_notebook(valid_path, [mk_cell(\"assert True\", cell_type=\"code\")])\n    assert notebook_validation_problems(valid_path) == []\n    validate_nbs(str(valid_path), strict=False)\n\n\nwith write_demo_notebook(\"04_review_validate_invalid.ipynb\") as invalid_path:\n    invalid_nb = _write_review_notebook(invalid_path, [\n        mk_cell(\"x = 1\", cell_type=\"code\"),\n        mk_cell(\"Some docs\", cell_type=\"markdown\"),\n        mk_cell(\"assert True\", cell_type=\"code\"),\n    ])\n    invalid_nb.cells[1].metadata[\"nbskill\"][\"semantic_types\"] = []\n    invalid_nb.cells[2].metadata[\"nbskill\"][\"cell_type\"] = \"markdown\"\n    _write_nb(invalid_nb, invalid_path)\n    codes = {problem[\"code\"] for problem in notebook_validation_problems(invalid_path)}\n    assert {\"missing-cell-semantic-types\", \"cell-type-mismatch\"} &lt;= codes\n\n\nwith write_demo_notebook(\"04_review_export_hash.ipynb\", base=\"nbs\") as export_nb_path:\n    export_nb = _write_review_notebook(export_nb_path, [mk_cell(\"#| default_exp sample_tool\")])\n    generated_py_path = exported_py_path(export_nb_path, export_nb)\n    try:\n        generated_py_path.parent.mkdir(parents=True, exist_ok=True)\n        generated_py_path.write_text(\"actual = 1\\n\", encoding=\"utf-8\")\n        export_nb.metadata[\"nbskill\"] = {\"exported_py_hash\": \"bad\"}\n        _write_nb(export_nb, export_nb_path)\n        codes = {problem[\"code\"] for problem in notebook_validation_problems(export_nb_path)}\n        assert \"exported-py-hash-mismatch\" in codes\n    finally:\n        if generated_py_path is not None: remove_demo_path(generated_py_path)\n\nnotebook_validation_problems is narrower than style_report: it checks nbskill metadata plus Python syntax in code cells, the prerequisites for reliable notebook tooling. validate_nbs exposes the same failures to Python callers.\n\nwith write_demo_notebook(\"04_review_validation_example.ipynb\") as validation_path:\n    _write_review_notebook(validation_path, [\n        mk_cell(\"x = 1\", cell_type=\"code\"),\n        mk_cell(\"Some docs\", cell_type=\"markdown\"),\n    ], stamp=False)\n    problems = notebook_validation_problems(validation_path)\n    print([problem[\"code\"] for problem in problems[:4]])\n\n\n\n\nNotebook diffs are noisy when metadata and outputs are included. diff_nb asks nbdev for code-cell source on each side of a comparison and prints only the added, changed, or deleted code blocks the caller requested.\n\n\n\nFastHTML and HTML-producing notebook cells often hide customer-visible copy inside Python component calls. A normal text search finds too much: headings, button labels, docstrings, route names, tests, exceptions, and implementation notes all look like strings. The public UI text surface gives agents a smaller first pass over likely visible copy before editing.\nUse visible_text_inventory before an edit when the task is phrased in product or customer language. It answers: which notebook cell and symbol owns the visible words I may need to change? That reduces the manual rg loop where an agent has to compare notebook source, exported modules, and internal strings by hand.\nUse diff_nb(surface=\"public-ui\") after an edit when the review question is user-facing copy rather than Python structure. It keeps headings, labels, link text, image alt text, and similar UI literals separate from code churn, metadata churn, tests, and docstring edits.\nThis is deliberately heuristic. It is meant to support a faster notebook iteration loop, not to prove accessibility or render every possible runtime branch: inventory the likely visible text, make a focused edit, run the route/example smoke snippet, then review the public UI diff.\n\nsource\n\n\n\ndef visible_text_inventory(\n    path:str='.', include_re:NoneType=None, max_records:int=200\n):\nPrint likely user-visible text from FastHTML/HTML-producing notebook code cells.\n\nsource\n\n\n\ndef diff_nb(\n    path:str, # Notebook path\n    ref_a:str | None='HEAD', # First git ref; use None for working tree\n    ref_b:str | None=None, # Second git ref; defaults to working tree\n    adds:bool=True, # Include code cells added in ref_b\n    changes:bool=True, # Include changed code cells\n    dels:bool=False, # Include deleted code cells\n    cell_id:str | None=None, # Limit output to comma/space-separated cell ids\n    after_id:str | None=None, # Limit output to cells after this id in ref_b/the working tree\n    surface:str='code', # Review surface: \"code\" or \"public-ui\"\n):\nPrint nbdev-style diffs for code cells, or a public UI text surface.\n\nfrom tempfile import gettempdir\n\nwith write_demo_notebook(\"04_review_no_git.ipynb\", base=gettempdir()) as path:\n    _write_review_notebook(path, [mk_cell(\"x = 1\", cell_type=\"code\")])\n    try:\n        diff_nb(str(path))\n    except (SystemExit, ValueError) as exc:\n        if isinstance(exc, SystemExit): assert exc.code == 1\n        else:\n            msg = str(exc)\n            assert \"disposable notebook\" in msg and \"ref_a=None\" in msg and \"ref_b=None\" in msg\n    out = StringIO()\n    with redirect_stdout(out):\n        diff_nb(str(path), ref_a=None, ref_b=None)\n    report = out.getvalue()\n    assert \"--- code cell\" in report and \"+x = 1\" in report\n\n\ndef _committed_review_notebook(root, cells=None):\n    root.mkdir()\n    path = root / \"demo.ipynb\"\n    _write_review_notebook(path, cells or [mk_cell(\"x = 1\", cell_type=\"code\")])\n    subprocess.run([\"git\", \"init\"], cwd=root, check=True, capture_output=True)\n    subprocess.run([\"git\", \"add\", \"demo.ipynb\"], cwd=root, check=True, capture_output=True)\n    subprocess.run([\n        \"git\", \"-c\", \"user.name=Nbskill\", \"-c\", \"user.email=nbskill@example.com\",\n        \"commit\", \"-m\", \"base\",\n    ], cwd=root, check=True, capture_output=True)\n    return path\n\n\nroot = demo_path(\"04_review_git_metadata\")\ntry:\n    path = _committed_review_notebook(root)\n    nb = read_nb(path)\n    nb.cells[0].metadata[\"nbskill\"] = {\"cell_type\": \"code\", \"semantic_types\": []}\n    _write_nb(nb, path)\n    out = StringIO()\n    with redirect_stdout(out):\n        diff_nb(str(path))\n    text = out.getvalue()\n    assert \"No code cell changes\" in text\n    assert \"Ignored nbskill metadata changes in 1 cell\" in text\nfinally:\n    remove_demo_path(root)\n\n\nroot = demo_path(\"04_review_git_filters\")\ntry:\n    path = _committed_review_notebook(root)\n    nb = read_nb(path)\n    first_id = nb.cells[0].id\n    nb.cells[0].source = \"x = 2\"\n    nb.cells.append(mk_cell(\"y = 3\", cell_type=\"code\"))\n    _write_nb(nb, path)\n    out = StringIO()\n    with redirect_stdout(out): diff_nb(str(path), cell_id=first_id)\n    text = out.getvalue()\n    assert \"x = 2\" in text and \"y = 3\" not in text\n    out = StringIO()\n    with redirect_stdout(out): diff_nb(str(path), after_id=first_id)\n    text = out.getvalue()\n    assert \"y = 3\" in text and \"x = 2\" not in text\n    delta = style_report(str(path), changed_only=True)\n    assert delta[\"summary\"][\"changed_only\"] is True\n    assert delta[\"summary\"][\"changed_cell_count\"] == 2\nfinally:\n    remove_demo_path(root)\n\n\ndef _write_style_problem_notebook(path):\n    long_source = \"\\n\".join([f\"x{i} = {i}\" for i in range(31)])\n    long_markdown = \"\\n\".join([f\"paragraph {i}\" for i in range(21)])\n    mixed_import = mk_cell(\"import json\", cell_type=\"code\")\n    mixed_import.outputs = [dict(output_type=\"stream\", name=\"stdout\", text=\"json\\n\")]\n    _write_review_notebook(path, [\n        mk_cell(\n            \"#| export\\n\"\n            \"def a():\\n\"\n            \"    pass\\n\"\n            \"def b():\\n\"\n            \"    \\\"\\\"\\\"Describe b.\\\"\\\"\\\"\\n\"\n            \"    pass\\n\"\n            \"def c():\\n\"\n            \"    \\\"\\\"\\\"Line one.\\n\"\n            \"    Line two.\\\"\\\"\\\"\\n\"\n            \"    pass\\n\"\n            \"def _private():\\n\"\n            \"    pass\",\n            cell_type=\"code\",\n        ),\n        mk_cell(long_source, cell_type=\"code\"),\n        mk_cell(long_markdown, cell_type=\"markdown\"),\n        mk_cell(\"assert 1 == 1\\nassert 2 == 2\\nassert 3 == 3\\nassert 4 == 4\", cell_type=\"code\"),\n        mk_cell(\"#| export\\nimport os\", cell_type=\"code\"),\n        mk_cell(\"#| export\\nimport os\", cell_type=\"code\"),\n        mixed_import,\n        mk_cell(\"import sys\", cell_type=\"code\"),\n        mk_cell(\"import sys\", cell_type=\"code\"),\n        mk_cell(\"result = later_helper()\", cell_type=\"code\"),\n        mk_cell(\"def later_helper():\\n    return 1\", cell_type=\"code\"),\n        mk_cell(\"def loader():\\n    return MissingPath('x')\", cell_type=\"code\"),\n    ])\n\n_write_literacy_problem_notebook builds a compact demo notebook for the literate review contract. The fixture keeps compliant, missing-docs, missing-example, missing-test, and multiline-docstring cases in one place so each warning code has a precise example.\n\ndef _write_literacy_problem_notebook(path):\n    _write_review_notebook(path, [\n        mk_cell(\"`ready` has docs, a docstring, an example, and a test.\", cell_type=\"markdown\"),\n        mk_cell(\"#| export\\ndef ready(x):\\n    \\\"Return x plus one.\\\"\\n    return x + 1\", cell_type=\"code\"),\n        mk_cell(\"`no_example` has docs and a test but no example call.\", cell_type=\"markdown\"),\n        mk_cell(\"#| export\\ndef no_example(x):\\n    \\\"Return x plus one.\\\"\\n    return x + 1\", cell_type=\"code\"),\n        mk_cell(\"`no_test` has docs and an example but no test assertion.\", cell_type=\"markdown\"),\n        mk_cell(\"#| export\\ndef no_test(x):\\n    \\\"Return x plus one.\\\"\\n    return x + 1\", cell_type=\"code\"),\n        mk_cell(\"`bad_doc` has a Markdown note but its docstring is too long.\", cell_type=\"markdown\"),\n        mk_cell(\"#| export\\ndef bad_doc(x):\\n    \\\"\\\"\\\"Return x plus one.\\n    Keep this second line out of file_context.\\\"\\\"\\\"\\n    return x + 1\", cell_type=\"code\"),\n        mk_cell(\"`eq_checked`, `ne_checked`, and `fail_checked` cover fastcore test helpers.\", cell_type=\"markdown\"),\n        mk_cell(\"#| export\\ndef eq_checked(x):\\n    \\\"Return x plus one.\\\"\\n    return x + 1\", cell_type=\"code\"),\n        mk_cell(\"#| export\\ndef ne_checked(x):\\n    \\\"Return x plus one.\\\"\\n    return x + 1\", cell_type=\"code\"),\n        mk_cell(\"#| export\\ndef fail_checked():\\n    \\\"Raise a demo failure.\\\"\\n    raise ValueError(\\\"boom\\\")\", cell_type=\"code\"),\n        mk_cell(\"#| export\\ndef no_docs(x):\\n    \\\"Return x plus one.\\\"\\n    return x + 1\", cell_type=\"code\"),\n        mk_cell(\"#| export\\ndef _hidden(x):\\n    return x\\n\\ndef test_helper(x):\\n    return x\", cell_type=\"code\"),\n        mk_cell(\"ready(1)\\nno_docs(1)\\nno_test(1)\\nbad_doc(1)\\neq_checked(1)\\nne_checked(1)\\ntry:\\n    fail_checked()\\nexcept ValueError:\\n    pass\", cell_type=\"code\"),\n        mk_cell(\"assert ready(1) == 2\\nassert no_docs(1) == 2\\nassert no_example(1) == 2\\nassert bad_doc(1) == 2\\ntest_eq(eq_checked(1), 2)\\ntest_ne(ne_checked(1), 3)\\ntest_fail(lambda: fail_checked())\\nassert _hidden(1) == 1\\nassert test_helper(1) == 1\", cell_type=\"code\"),\n    ])\n\n\nwith write_demo_notebook(\"04_review_literacy.ipynb\") as path:\n    _write_literacy_problem_notebook(path)\n    problems = public_function_literacy_problems(path)\n    codes = {(problem.get(\"symbol\"), problem[\"code\"]) for problem in problems}\n    missing_codes = {(\"no_docs\", \"public-function-markdown\"), (\"no_example\", \"public-function-example\"), (\"no_test\", \"public-function-test\")}\n    ready_codes = {code for symbol, code in codes if symbol == \"ready\"}\n    assert missing_codes &lt;= codes\n    assert not ready_codes\n\n\nwith write_demo_notebook(\"04_review_literacy_docstrings.ipynb\") as path:\n    _write_literacy_problem_notebook(path)\n    problems = public_function_literacy_problems(path)\n    codes = {(problem.get(\"symbol\"), problem[\"code\"]) for problem in problems}\n    tested_codes = {\n        (\"eq_checked\", \"public-function-test\"),\n        (\"ne_checked\", \"public-function-test\"),\n        (\"fail_checked\", \"public-function-test\"),\n    }\n    assert (\"bad_doc\", \"public-function-docstring\") in codes\n    assert not tested_codes & codes\n    assert not any(problem.get(\"symbol\") in {\"_hidden\", \"test_helper\"} for problem in problems)\n\n\nprint(report)\n\nNotebook style report:\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=18cf6a7f\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=3e6417a6\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=9d964403\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=56e0e6c4\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=4debe573\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=742a5cf5\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=f41bdaef\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=3b1110e1\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=23f2d90a\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=4268cb78\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=762d5962\n- missing-cell-nbskill-metadata: nbs/data/04_review_style.ipynb id=711b360d\n- large-cell: nbs/data/04_review_style.ipynb id=18cf6a7f function_count=4 4 top-level functions; split into one-idea cells\n- large-cell: nbs/data/04_review_style.ipynb id=3e6417a6 line_count=31 31 code content lines; keep one idea per cell\n- large-cell: nbs/data/04_review_style.ipynb id=9d964403 line_count=21 21 markdown content lines; keep one idea per cell\n- public-function-docstring: nbs/data/04_review_style.ipynb id=18cf6a7f line=1 symbol='a' missing='one-line docstring' docstring_lines=0 public function 'a' needs a one-line docstring for context summaries\n- public-function-markdown: nbs/data/04_review_style.ipynb id=18cf6a7f line=1 symbol='a' missing='Markdown docs cell mentioning the function' public function 'a' needs Markdown docs cell mentioning the function\n- public-function-example: nbs/data/04_review_style.ipynb id=18cf6a7f line=1 symbol='a' missing='example cell that calls the function' public function 'a' needs example cell that calls the function\n- public-function-test: nbs/data/04_review_style.ipynb id=18cf6a7f line=1 symbol='a' missing='test cell that asserts or checks the function' public function 'a' needs test cell that asserts or checks the function\n- public-function-markdown: nbs/data/04_review_style.ipynb id=18cf6a7f line=3 symbol='b' missing='Markdown docs cell mentioning the function' public function 'b' needs Markdown docs cell mentioning the function\n- public-function-example: nbs/data/04_review_style.ipynb id=18cf6a7f line=3 symbol='b' missing='example cell that calls the function' public function 'b' needs example cell that calls the function\n- public-function-test: nbs/data/04_review_style.ipynb id=18cf6a7f line=3 symbol='b' missing='test cell that asserts or checks the function' public function 'b' needs test cell that asserts or checks the function\n- public-function-docstring: nbs/data/04_review_style.ipynb id=18cf6a7f line=6 symbol='c' missing='one-line docstring' docstring_lines=2 public function 'c' has 2 docstring lines; keep it to one line for context summaries\n- public-function-markdown: nbs/data/04_review_style.ipynb id=18cf6a7f line=6 symbol='c' missing='Markdown docs cell mentioning the function' public function 'c' needs Markdown docs cell mentioning the function\n- public-function-example: nbs/data/04_review_style.ipynb id=18cf6a7f line=6 symbol='c' missing='example cell that calls the function' public function 'c' needs example cell that calls the function\n- public-function-test: nbs/data/04_review_style.ipynb id=18cf6a7f line=6 symbol='c' missing='test cell that asserts or checks the function' public function 'c' needs test cell that asserts or checks the function\n- multi-problem-test: nbs/data/04_review_style.ipynb id=56e0e6c4 4 asserts/test functions; split into one-problem-at-a-time cells\n- unclean-cell: nbs/data/04_review_style.ipynb id=f41bdaef semantic_types=test_cell, private_code\n- duplicate-import: nbs/data/04_review_style.ipynb scope=exported import_key='import os as os' cells=4debe573, 742a5cf5\n- duplicate-import: nbs/data/04_review_style.ipynb scope=internal import_key='import sys as sys' cells=3b1110e1, 23f2d90a\n- cell-order: nbs/data/04_review_style.ipynb id=4268cb78 line=1 symbol='later_helper' confidence=high called before definition/import in later cell id=762d5962\n- missing-import: nbs/data/04_review_style.ipynb id=711b360d line=2 symbol='MissingPath' confidence=medium called without an earlier definition/import\n\n\n\nwith write_demo_notebook(\"04_review_generated.ipynb\") as generated_path:\n    generated_nb = _write_review_notebook(generated_path, [mk_cell(\"#| default_exp big_review_demo\")])\n    generated_py_path = exported_py_path(generated_path, generated_nb)\n    try:\n        generated_py_path.parent.mkdir(parents=True, exist_ok=True)\n        generated_py_path.write_text(\"\\n\".join(f\"x{i} = {i}\" for i in range(1001)), encoding=\"utf-8\")\n        size_codes = {problem[\"code\"] for problem in notebook_size_problems(generated_path)}\n        assert \"large-generated-py\" in size_codes\n    finally:\n        if generated_py_path is not None: remove_demo_path(generated_py_path)\n\n\nwith write_demo_notebook(\"04_review_style.ipynb\") as path:\n    _write_style_problem_notebook(path)\n    custom_map = demo_path(\"04_review_errors.json\")\n    old_map = os.environ.get(\"NBSKILL_FAILURE_MAP\")\n    try:\n        os.environ[\"NBSKILL_FAILURE_MAP\"] = str(custom_map)\n        with redirect_stdout(StringIO()):\n            style_check(str(path), delete_after_output=True)\n        assert \"usage:\" in _format_global_usage_summary()\n        assert not custom_map.exists()\n    finally:\n        if old_map is None: os.environ.pop(\"NBSKILL_FAILURE_MAP\", None)\n        else: os.environ[\"NBSKILL_FAILURE_MAP\"] = old_map\n\n\nwith write_demo_notebook(\"04_review_style.ipynb\") as path:\n    _write_style_problem_notebook(path)\n    try:\n        with redirect_stdout(StringIO()): style_check(str(path), strict=True)\n    except RuntimeError:\n        pass\n    else:\n        raise AssertionError(\"strict style_check should raise for notebook hygiene problems\")\n\n\nassert code_source(mk_cell(\"plain docs\", cell_type=\"markdown\")) is None\nassert code_source(mk_cell(\"answer = 42\", cell_type=\"code\")) == \"answer = 42\"\n\ndiff_nb deliberately compares code-cell source only. Markdown edits and notebook metadata churn stay out of the main diff so reviewers can see the executable behavior that changed.\n\ndiff_root = demo_path(\"04_review_diff_example\")\nremove_demo_path(diff_root)\ntry:\n    diff_root.mkdir()\n    diff_path = diff_root / \"demo.ipynb\"\n    _write_nb(new_nb([\n        mk_cell(\"value = 1\\nvalue\", cell_type=\"code\"),\n        mk_cell(\"Original note\", cell_type=\"markdown\"),\n    ]), diff_path)\n    subprocess.run([\"git\", \"init\"], cwd=diff_root, check=True, capture_output=True)\n    subprocess.run([\"git\", \"add\", \"demo.ipynb\"], cwd=diff_root, check=True, capture_output=True)\n    subprocess.run([\n        \"git\", \"-c\", \"user.name=Nbskill\", \"-c\", \"user.email=nbskill@example.com\",\n        \"commit\", \"-m\", \"base\",\n    ], cwd=diff_root, check=True, capture_output=True)\n\n    diff_nb_json = read_nb(diff_path)\n    diff_nb_json.cells[0].source = \"value = 2\\nvalue\"\n    diff_nb_json.cells[1].source = \"Updated note that will not appear in the code diff\"\n    _write_nb(diff_nb_json, diff_path)\n\n    diff_output = StringIO()\n    with redirect_stdout(diff_output):\n        diff_nb(str(diff_path), ref_a=\"HEAD\")\n    print(diff_output.getvalue())\nfinally:\n    remove_demo_path(diff_root)\n\nHere is the intended workflow on a tiny FastHTML-shaped notebook: first inventory likely visible text, then review just the public UI surface. The output is compact enough for an agent to paste into a status update or use as a pre-edit checklist.\n\nfrom tempfile import gettempdir\n\nui_source = \"\"\"\ndef _hero():\n    return Div(\n        H1('Welcome'),\n        P('Compare providers in minutes.'),\n        Button('Start now'),\n        Img(src='/logo.png', alt='Company logo'),\n    )\n\nraise ValueError('internal diagnostic, not customer copy')\n\"\"\"\n\nwith write_demo_notebook(\"04_review_public_ui_demo.ipynb\", base=gettempdir()) as ui_path:\n    _write_review_notebook(ui_path, [mk_cell(ui_source, cell_type=\"code\")])\n\n    print(\"Inventory before editing:\")\n    visible_text_inventory(ui_path)\n\n    print(\"\\nPublic UI diff for the current notebook contents:\")\n    diff_nb(str(ui_path), ref_a=None, ref_b=None, surface=\"public-ui\")\n\nInventory before editing:\ncell_id | line | symbol | call | text\na19e06a5 | 4 | _hero | H1 | Welcome\na19e06a5 | 5 | _hero | P | Compare providers in minutes.\na19e06a5 | 6 | _hero | Button | Start now\na19e06a5 | 7 | _hero | Img.alt | Company logo\n\nPublic UI diff for the current notebook contents:\n--- public-ui cell a19e06a5 ---\n--- \n+++ \n@@ -0,0 +1,4 @@\n+H1: Welcome\n+P: Compare providers in minutes.\n+Button: Start now\n+Img.alt: Company logo\n\n\nReview functions are direct Python APIs; MCP consolidates their common diagnostics through doctor.",
    "crumbs": [
      "Reviewing notebooks"
    ]
  },
  {
    "objectID": "02_write.html",
    "href": "02_write.html",
    "title": "nbskill",
    "section": "",
    "text": "Cell creation, targeted updates, documentation insertion, and examples.\nWriting notebooks safely is harder than appending text to a file. A notebook edit needs to preserve cell ids, clear stale outputs, validate Python when possible, export through nbdev automatically, and avoid overwriting the wrong cell.\nwrite_nb inserts cells; replace_str replaces literal text across notebook cell sources.\nThere are two writing modes because notebook edits have two different shapes. Use cell-block writes when adding or replacing structured cells, and use literal replacement only for exact renames across existing cells. Both paths stamp nbskill metadata and export affected notebooks automatically when they have an nbdev export target. write_literal_replacements is the public literal-replacement API.\n\n\nThe writing tools are production core. They must preserve stable cell ids where possible, clear stale outputs for changed cells, validate new Python, export nbdev modules after source changes, support dry-run review for broad replacements, and verify structured batch edits by reading the notebook back.",
    "crumbs": [
      "Writing and changing"
    ]
  },
  {
    "objectID": "02_write.html#writing-and-changing",
    "href": "02_write.html#writing-and-changing",
    "title": "nbskill",
    "section": "",
    "text": "Cell creation, targeted updates, documentation insertion, and examples.\nWriting notebooks safely is harder than appending text to a file. A notebook edit needs to preserve cell ids, clear stale outputs, validate Python when possible, export through nbdev automatically, and avoid overwriting the wrong cell.\nwrite_nb inserts cells; replace_str replaces literal text across notebook cell sources.\nThere are two writing modes because notebook edits have two different shapes. Use cell-block writes when adding or replacing structured cells, and use literal replacement only for exact renames across existing cells. Both paths stamp nbskill metadata and export affected notebooks automatically when they have an nbdev export target. write_literal_replacements is the public literal-replacement API.\n\n\nThe writing tools are production core. They must preserve stable cell ids where possible, clear stale outputs for changed cells, validate new Python, export nbdev modules after source changes, support dry-run review for broad replacements, and verify structured batch edits by reading the notebook back.",
    "crumbs": [
      "Writing and changing"
    ]
  },
  {
    "objectID": "02_write.html#writing-cells",
    "href": "02_write.html#writing-cells",
    "title": "nbskill",
    "section": "Writing cells",
    "text": "Writing cells\nThe write layer turns structured cell plans into valid notebook cells and keeps the on-disk notebook in sync with its exported module.\n\nAdding cells\nwrite_nb is the broad insertion tool. It can append, insert before or after a stable cell id, replace a whole notebook, or write into a named chapter while preserving notebook structure.\n\nsource\n\n\nwrite_literal_replacements\ndef write_literal_replacements(\n    path:str, # path of the notebook\n    old_str:str, # old string to replace\n    new_str:str='', # new string to insert\n    run_test:bool=False, # run the tests for this code\n    validate_code:bool=True, # run the code to make sure it works\n    dry_run:bool=False, # don't make actual changes\n    show_cells:bool=False, # show the cells that were edited\n):\nReplace literal text across notebook cell sources.\n\nwrite_literal_replacements(\"nbs/example.ipynb\", \"old_name\", \"new_name\", dry_run=True, show_cells=True)\n\n\nsource\n\n\nwrite_nb\ndef write_nb(\n    path:str, cells:str='', before_id:str | None=None, after_id:str | None=None, chapter:str | None=None,\n    replace:bool=False, cell_type:str='code', # cell type code|markdown\n    run_test:bool=False, # execute the tests\n    run_style:bool=False, # return style problems\n    style_strict:bool=False, validate_code:bool=True\n):\nWrite cells to a notebook.",
    "crumbs": [
      "Writing and changing"
    ]
  },
  {
    "objectID": "02_write.html#updating-and-splitting",
    "href": "02_write.html#updating-and-splitting",
    "title": "nbskill",
    "section": "Updating and splitting",
    "text": "Updating and splitting\nUse one-cell updates for surgical changes; use explosion or chapter splitting when the notebook structure itself is the thing being changed.\n\nUpdating one cell\nupdate_cell is the surgical tool. It keeps the original cell id, can replace a whole cell or only a line range, clears stale outputs,.\nFor whole-cell replacement, pass exactly one notebook cell block: optional %%code, %%markdown, or %%raw marker followed by the cell source. Do not include standalone --- separators; those mean multiple cells and belong with write_nb or batch_edit_nb. Use line_range or old_str when replacing only part of a cell.\nupdate_cell accepts exact Python text. MCP callers send structured source directly, so no newline decoding or temporary patch files are needed.\n\nsource\n\n\nupdate_cell\ndef update_cell(\n    path:str, # Notebook path\n    new:str='', new_file:str | None=None, # Read replacement text from a UTF-8 file\n    cell_id:str | None=None, # Stable notebook cell id to update\n    old_str:str | None=None, # Literal text to replace in the target cell, or used to locate the cell when cell_id is absent\n    line_range:str | None=None, # 1-based inclusive lines to replace, e.g. 3 or 3:5\n    split:bool=False, # Split a multi-cell replacement into separate cells\n    split_before:str | None=None, # Split existing cell before the first matching line\n    cell_type:str='code', # Default type for whole-cell replacements without %% marker\n    run_test:bool=False, # Execute the notebook with execnb after writing\n    validate_code:bool=True, # Validate changed Python code before writing\n    dry_run:bool=False, # Show the update plan without writing\n):\nUpdate one notebook cell: replace whole cell, a text substring, or a line range.\n\nsource\n\n\nsplit_cell\ndef split_cell(\n    path:str, # Notebook path\n    cell_id:str, # Stable cell id to split\n    new:str='', new_file:str | None=None, # Read replacement text from a UTF-8 file\n    split_before:str | None=None, # Split existing cell before the first line containing or matching this text\n    cell_type:str='code', # Default type for replacement cells without %% marker\n    run_test:bool=False, # Execute the notebook with execnb after writing\n    validate_code:bool=True, # Validate changed Python code before writing\n    dry_run:bool=False, # Show the split plan without writing\n):\nSplit one notebook cell into multiple cells, either at a matching line or by supplying replacement content.\n\nExploding function cells\nexplode_cell splits a code cell containing multiple top-level functions into one code cell per function. The generated cells keep the original leading directives, and the normal notebook commit path stamps ids and nbskill metadata.\n\nsource\n\n\n\nexplode_cell\ndef explode_cell(\n    path:str, # Notebook path\n    cell_id:str | None=None, # Stable cell id to explode\n    target_all:bool=False, # Explode every splittable code cell in the notebook\n    run_test:bool=False, # Execute the notebook with execnb after writing\n    validate_code:bool=True, # Validate changed Python code before writing\n    dry_run:bool=False, # Show the explode plan without writing\n):\nSplit top-level function definitions in one or all code cells into separate cells.\n\nPreviewing an explosion\nA dry run shows the planned split without changing the scratch notebook.\n\nwith write_demo_notebook(\"02_write_explode_preview.ipynb\", cells=[mk_cell(\"#| export\\ndef first():\\n    return 1\\n\\ndef second():\\n    return 2\")]) as path:\n    cell_id = read_nb(path).cells[0].id\n    explode_cell(str(path), cell_id=cell_id, dry_run=True)\n\n\nwith write_demo_notebook(\"02_write_update.ipynb\") as path:\n    _exercise_direct_update_cell(path)\n    print(\"direct update_cell preserved escaped newlines\")\n\n\n\nStructured cell edits\nMCP tools and other structured callers send lists of source lines instead of shell-escaped text. These helpers keep that transport shape in the write layer, including optional split_lines markers that only split on empty lines.\n\nsource\n\n\n\njoin_source_lines\ndef join_source_lines(\n    lines, field:str='source_lines'\n):\nReturn newline text from a structured list of source lines.\n\nsource\n\n\nvalid_split_line_numbers\ndef valid_split_line_numbers(\n    source_lines, split_lines:NoneType=None\n):\nReturn requested 1-based split lines that point at empty source lines.\n\nsource\n\n\nsplit_source_lines\ndef split_source_lines(\n    source_lines, split_lines:NoneType=None\n):\nSplit source lines at requested empty 1-based line numbers.\n\nsource\n\n\nsource_lines_cell\ndef source_lines_cell(\n    source_lines, cell_type:str='code'\n):\nBuild one clean notebook cell from structured source lines.\n\nsource\n\n\nsource_lines_cells\ndef source_lines_cells(\n    cell, default_cell_type:str='code'\n):\nBuild one or more cells from a structured cell object.\n\nsource\n\n\nshould_run_cell_feedback\ndef should_run_cell_feedback(\n    cell\n):\nReturn whether an edited cell should be executed for immediate feedback.\n\nsource\n\n\nnotebook_edit_feedback\ndef notebook_edit_feedback(\n    path, cell_ids, auto_feedback:bool=True, timeout:int=10, safe:bool=True\n):\nExecute through the last feedback-worthy edited cell and return visible output.\n\nsource\n\n\nappend_notebook_edit_feedback\ndef append_notebook_edit_feedback(\n    message, path, cell_ids, auto_feedback:bool=True, feedback_timeout:int=10, feedback_safe:bool=True\n):\nAppend automatic notebook execution feedback when an edited cell calls for it.\n\nsource\n\n\nsave_notebook_edit\ndef save_notebook_edit(\n    nb, path\n):\nStamp, write, export, and report whether nbdev produced Python.\n\nsource\n\n\nreplace_notebook_cell\ndef replace_notebook_cell(\n    path, cell_id, source_lines, cell_type:str='code', split_lines:NoneType=None, validate_code:bool=True,\n    auto_feedback:bool=True, feedback_timeout:int=10, feedback_safe:bool=True\n):\nReplace one notebook cell from structured source lines, optionally splitting on empty lines.\n\nsource\n\n\nreplace_notebook_range\ndef replace_notebook_range(\n    path, cell_id, start_line, end_line, replacement_lines, validate_code:bool=True, auto_feedback:bool=True,\n    feedback_timeout:int=10, feedback_safe:bool=True\n):\nReplace one inclusive 1-based line range from structured replacement lines.\n\nsource\n\n\ninsert_notebook_cells\ndef insert_notebook_cells(\n    path, anchor_id, where:str='after', cells:NoneType=None, validate_code:bool=True, default_cell_type:str='code',\n    auto_feedback:bool=True, feedback_timeout:int=10, feedback_safe:bool=True\n):\nInsert structured cells before or after an anchor cell id.\n\nsource\n\n\ndelete_notebook_cell\ndef delete_notebook_cell(\n    path, cell_id\n):\nDelete one notebook cell by stable id.\n\nsource\n\n\napply_notebook_edit\ndef apply_notebook_edit(\n    edit, default_path:NoneType=None, validate_code:bool=True, default_cell_type:str='code', auto_feedback:bool=True,\n    feedback_timeout:int=10, feedback_safe:bool=True, dry_run:bool=False\n):\nApply one structured notebook edit operation through edit_notebook.\n\nlines = [\"def first():\", \"    return 1\", \"\", \"def second():\", \"    return 2\"]\nassert join_source_lines([\"a\", \"b\"]) == \"a\\nb\"\nassert valid_split_line_numbers(lines, [2, 3]) == [3]\nassert split_source_lines(lines, [2, 3]) == [lines[:2], lines[3:]]\nassert source_lines_cell([\"scratch = True\"]).source == \"scratch = True\"\nassert len(source_lines_cells({\"source_lines\": lines, \"split_lines\": [3]})) == 2\nprint(\"structured line helpers:\", len(split_source_lines(lines, [3])))\n\nstructured line helpers: 2\n\n\n\nwith write_demo_notebook(\"02_write_structured_edit.ipynb\") as path:\n    write_nb(str(path), \"%%code\\nvalue = 1\", replace=True)\n    cell_id = read_nb(path).cells[0].id\n    source = [\"def first():\", \"    return 1\", \"\", \"def second():\", \"    return 2\"]\n    msg = replace_notebook_cell(str(path), cell_id, source, split_lines=[2, 3])\n    nb = read_nb(path)\n    assert len(nb.cells) == 2\n    range_msg = replace_notebook_range(str(path), nb.cells[0].id, 1, 1, [\"def uno():\"])\n    insert_msg = insert_notebook_cells(str(path), nb.cells[1].id, cells=[{\"source_lines\": [\"tail = True\"]}])\n    assert read_nb(path).cells[2].source == \"tail = True\"\n    print(msg)\n    print(range_msg)\n    print(insert_msg)\n\nWrote 1 cells to nbs/data/02_write_structured_edit.ipynb using replace\nUpdated cell into 2 cells id=aa02cf6a\nUpdated lines 1:1 id=aa02cf6a\nInserted 1 cell after id=e05b6833\n\n\n\nwith write_demo_notebook(\"02_write_structured_apply.ipynb\") as path:\n    write_nb(str(path), \"%%code\\ntail = True\", replace=True)\n    tail_id = read_nb(path).cells[0].id\n    op = dict(op=\"replace_cell\", cell_id=tail_id, source_lines=[\"tail = 'applied'\"])\n    apply_msg = apply_notebook_edit(op, str(path))\n    assert read_nb(path).cells[0].source == \"tail = 'applied'\"\n    delete_msg = delete_notebook_cell(str(path), tail_id)\n    assert len(read_nb(path).cells) == 0\n    assert save_notebook_edit(read_nb(path), path) is False\n    print(apply_msg)\n    print(delete_msg)\n    print(\"structured helpers left cells:\", len(read_nb(path).cells))\n\nWrote 1 cells to nbs/data/02_write_structured_apply.ipynb using replace\nUpdated cell id=892ad2a6\nDeleted cell id=892ad2a6\nstructured helpers left cells: 0\n\n\n\nwith write_demo_notebook(\"02_write_feedback.ipynb\") as path:\n    write_nb(str(path), \"%%code\\nseed = 1\", replace=True)\n    cell_id = read_nb(path).cells[0].id\n    msg = replace_notebook_cell(str(path), cell_id, [\"print('feedback', 3)\"])\n    assert \"Auto feedback\" in msg\n    assert \"feedback 3\" in msg\n    assert read_nb(path).cells[0].outputs == []\n    feedback_cell = read_nb(path).cells[0]\n    assert should_run_cell_feedback(feedback_cell)\n    assert \"feedback 3\" in notebook_edit_feedback(str(path), [cell_id])\n    assert \"feedback 3\" in append_notebook_edit_feedback(\"prefix\", str(path), [cell_id])\n    quiet = replace_notebook_cell(str(path), cell_id, [\"seed = 4\"])\n    assert \"Auto feedback\" not in quiet\n    print(\"automatic edit feedback returned output\")\n\nWrote 1 cells to nbs/data/02_write_feedback.ipynb using replace\nautomatic edit feedback returned output",
    "crumbs": [
      "Writing and changing"
    ]
  },
  {
    "objectID": "02_write.html#batch-operations",
    "href": "02_write.html#batch-operations",
    "title": "nbskill",
    "section": "Batch operations",
    "text": "Batch operations\nBatch plans are useful when several related edits should be validated and committed together.\n\nBatch editing\nAgents often need to change several notebook cells together. batch_edit_nb accepts a JSON edit plan, runs the same notebook-aware validation and locking as the single-cell tools, and prints a compact diff before writing. Use it when repeated shell calls would make multiline code or dry-run diffs fragile.\n\n\nVerified batch edits\nBatch plans are useful when an agent already knows the exact notebook edits it wants to make. They are also risky if the tool only says “applied” without checking disk. batch_edit_nb now writes the notebooks, reads them back, and reports a verification status plus before/after hashes for each operation.\n\nsource\n\n\nbatch_edit_nb\ndef batch_edit_nb(\n    plan:str='', plan_file:str | None=None, # Read the JSON plan from a UTF-8 file\n    path:str | None=None, # Default notebook path for operations that omit path\n    dry_run:bool=True, # Show the plan and diffs without writing\n    validate_code:bool=True, # Validate changed Python code before writing\n    default_cell_type:str='code', # Default cell type for inserted cells without %% markers\n):\nApply a JSON batch edit plan as thin edit_notebook operations.\n\nSplitting one chapter\nsplit_nb_chapter moves one ## chapter into a new nbdev notebook. It copies imports used by the moved code, imports source-notebook definitions that the moved chapter still references, and promotes referenced private source helpers by dropping the leading underscore. It remains a Python API rather than an MCP tool because splitting modules is a broad, deliberate refactor.\n\nsource\n\n\n\nsplit_nb_chapter\ndef split_nb_chapter(\n    path:str, # Source notebook path\n    chapter:str, # Chapter title string or regex to split out\n    dest:str, # Destination notebook path\n    default_exp:str | None=None, # Destination nbdev default_exp; defaults from dest path\n    dry_run:bool=True, # Show the split plan without writing notebooks\n    force:bool=False, # Overwrite dest if it already exists\n    promote_private:bool=True, # Promote referenced private source helpers by dropping the leading underscore\n):\nSplit one ## chapter into a new nbdev notebook.",
    "crumbs": [
      "Writing and changing"
    ]
  },
  {
    "objectID": "02_write.html#moving-cells-between-notebooks",
    "href": "02_write.html#moving-cells-between-notebooks",
    "title": "nbskill",
    "section": "Moving cells between notebooks",
    "text": "Moving cells between notebooks\nmove_cells is the transfer boundary: it keeps full cells intact, derives imports from their AST names, and rolls both files back when a normal commit fails. A name turns the destination into a new chapter; without one, moves stay inside one notebook.\n\nsource\n\nmove_cells\ndef move_cells(\n    source_path, destination_path:NoneType=None, cell_ids:NoneType=None, query:NoneType=None, chapter:NoneType=None,\n    name:NoneType=None, mode:str='move', destination_anchor:NoneType=None, destination_where:str='after',\n    default_exp:NoneType=None, promote_private:bool=True, dry_run:bool=False\n):\nMove or copy selected cells, preserving full cell payloads and inferred imports.\n\nsource\n\n\ncreate_notebook\ndef create_notebook(\n    path, name:NoneType=None, template:str='nbdev', default_exp:NoneType=None, dry_run:bool=False\n):\nCreate a minimal flat numbered nbdev notebook without overwriting one.\n\nplan_path = demo_path(\"batch_edit_plan.json\")\ntry:\n    with write_demo_notebook(\"batch_edit_target.ipynb\") as nb_path:\n        write_nb(str(nb_path), \"%%code\\nvalue = 1\", replace=True)\n        nb = read_nb(nb_path)\n        cell = nb.cells[0]\n        plan = {\n            \"operations\": [\n                {\"op\": \"set_cell_source\", \"path\": str(nb_path), \"cell_id\": cell.id, \"source\": \"value = 2\"},\n                {\"op\": \"insert_after_id\", \"path\": str(nb_path), \"cell_id\": cell.id, \"cells\": \"%%code\\nassert value == 2\"},\n            ]\n        }\n        plan_path.write_text(json.dumps(plan), encoding=\"utf-8\")\n        batch_edit_nb(plan_file=str(plan_path), dry_run=True)\n        out = StringIO()\n        with redirect_stdout(out):\n            batch_edit_nb(plan_file=str(plan_path), dry_run=False)\n        applied = out.getvalue()\n        print(applied, end=\"\")\n        nb = read_nb(nb_path)\n        assert cell_source(nb.cells[0]) == \"value = 2\"\n        assert \"assert value == 2\" in cell_source(nb.cells[1])\n        assert \"[verified]\" in applied\n        assert \"-&gt;\" in applied and \"inserted id=\" in applied\n        print(f\"verified {applied.count('[verified]')} batch edit records\")\nfinally:\n    remove_demo_path(plan_path)\n\nWrote 1 cells to nbs/data/batch_edit_target.ipynb using replace\nDry run: would apply 2 batch operations across 1 notebook(s)\n- #1 set_cell_source: nbs/data/batch_edit_target.ipynb id=13f4ef6d [planned] e143e5f6659b-&gt;9cfe9326a093\n    --- before\n    +++ after\n    @@ -1 +1 @@\n    -value = 1\n    +value = 2\n- #2 insert_after_id: nbs/data/batch_edit_target.ipynb id=13f4ef6d [planned]\n    inserted id=f631de99 hash=7e8872481e92\n    inserted 1 cell(s) after id=13f4ef6d\nApplied 2 batch operations across 1 notebook(s)\n- #1 set_cell_source: nbs/data/batch_edit_target.ipynb id=13f4ef6d [verified] e143e5f6659b-&gt;9cfe9326a093\n    --- before\n    +++ after\n    @@ -1 +1 @@\n    -value = 1\n    +value = 2\n- #2 insert_after_id: nbs/data/batch_edit_target.ipynb id=13f4ef6d [verified]\n    inserted id=74af8946 hash=7e8872481e92\n    inserted 1 cell(s) after id=13f4ef6d\nverified 2 batch edit records\n\n\nThe write APIs accept ordinary Python values; MCP exposes only the bounded editing operations needed for agent workflows.",
    "crumbs": [
      "Writing and changing"
    ]
  },
  {
    "objectID": "index.html",
    "href": "index.html",
    "title": "nbskill",
    "section": "",
    "text": "nbskill is notebook-aware tooling for agents and maintainers working in nbdev projects. It provides focused context, structured edits, safe execution, and review through a Python Pyskill and MCP tools.",
    "crumbs": [
      "nbskill"
    ]
  },
  {
    "objectID": "index.html#why-it-exists",
    "href": "index.html#why-it-exists",
    "title": "nbskill",
    "section": "Why it exists",
    "text": "Why it exists\nIn nbdev, the notebook is the source of truth. It holds the implementation, its explanation, examples, outputs, and metadata. Raw JSON edits are easy to misplace, and changes to generated Python are overwritten. nbskill makes notebook work deliberate and reviewable.",
    "crumbs": [
      "nbskill"
    ]
  },
  {
    "objectID": "index.html#what-it-does",
    "href": "index.html#what-it-does",
    "title": "nbskill",
    "section": "What it does",
    "text": "What it does\nUse nbskill to find the relevant project, notebook, chapter, cell, or symbol; edit the right cells; run focused checks in project context; and review the resulting diff and style. The MCP server exposes the same workflow to coding agents.",
    "crumbs": [
      "nbskill"
    ]
  },
  {
    "objectID": "index.html#what-it-offers",
    "href": "index.html#what-it-offers",
    "title": "nbskill",
    "section": "What it offers",
    "text": "What it offers\n\n\n\nNeed\nTooling\n\n\n\n\nFind the right context\ncontext reads a project, notebook, chapter, cell id, or symbol without raw JSON noise.\n\n\nMake focused edits\nedit_notebook preserves cell structure, validates source, and returns a structured diff.\n\n\nVerify behavior\nexec_nb runs the notebook in project context, with a check-only mode for review loops.\n\n\nVerify a change\nMCP verify_change bundles a notebook diff, focused check-only execution, and doctor diagnostics.\n\n\nReview changes\ndiff_nb and doctor focus on behavior changes, notebook hygiene, and tool diagnostics.\n\n\nUse the library directly\nThe nbskill.skill Pyskill exposes the curated Python workflow for an installed project environment.\n\n\nServe agents\nMCP tools expose the same notebook-aware workflow through structured calls.",
    "crumbs": [
      "nbskill"
    ]
  },
  {
    "objectID": "index.html#parse-notebook-cells",
    "href": "index.html#parse-notebook-cells",
    "title": "nbskill",
    "section": "Parse notebook cells",
    "text": "Parse notebook cells\nNotebook text uses %%markdown, %%code, and --- as a compact cell format. parse_cells turns that text into notebook cells while preserving each cell’s type and source.\n\ncell_text = \"\\n\".join([\"%%markdown\", \"## Demo\", \"---\", \"%%code\", \"value = 42\"])\nparsed_cells = parse_cells(cell_text)\n[(cell.cell_type, cell_source(cell).splitlines()[0]) for cell in parsed_cells]\n\n[('markdown', '## Demo'), ('code', 'value = 42')]",
    "crumbs": [
      "nbskill"
    ]
  },
  {
    "objectID": "index.html#read-a-notebook",
    "href": "index.html#read-a-notebook",
    "title": "nbskill",
    "section": "Read a notebook",
    "text": "Read a notebook\nStart with the smallest useful view. context resolves a project, notebook, chapter, cell, or symbol without exposing notebook JSON. In Python, the first call explains the notebook and the second follows the public write_nb symbol to its owning cell and related uses.\n\nwith redirect_stdout(StringIO()): notebook_context = context(\"02_write.ipynb\", mode=\"overview\")\nprint(\"\\n\".join([x['source'] for x in notebook_context[\"selection\"][\"markdown\"]]))",
    "crumbs": [
      "nbskill"
    ]
  },
  {
    "objectID": "index.html#python-and-mcp",
    "href": "index.html#python-and-mcp",
    "title": "nbskill",
    "section": "Python and MCP",
    "text": "Python and MCP\nLoad the curated Python workflow through Pyskills:\nfrom nbskill.skill import *\n\ncontext(\"nbs/02_write.ipynb#write_nb\", mode=\"overview\")\nUse MCP tools when structured calls are more convenient:\nmcp__nbskill__.healthcheck()\nmcp__nbskill__.verify_change(paths=[\"nbs/02_write.ipynb\"])",
    "crumbs": [
      "nbskill"
    ]
  },
  {
    "objectID": "index.html#use-with-aai-coding",
    "href": "index.html#use-with-aai-coding",
    "title": "nbskill",
    "section": "Use with aai-coding",
    "text": "Use with aai-coding\naai-coding owns the persistent Python session, hooks, ordinary Python files, configuration, and prose. nbskill.skill owns notebook source, generated modules, focused execution, and notebook review.\ninstall_nbskill --target codex installs the local jupyter-notebooks skill and optional MCP configuration. It never rewrites an aai-coding checkout or a README.\nIn an nbdev project, do not use exhash or a general editor to mutate a source notebook or its generated module. Import nbskill.skill, call generated_owner to route a Python file, read with context, query reference_query for nontrivial prior art, make one edit_notebook change, then prove it with exec_nb, diff_nb, and style_check.",
    "crumbs": [
      "nbskill"
    ]
  },
  {
    "objectID": "index.html#references",
    "href": "index.html#references",
    "title": "nbskill",
    "section": "References",
    "text": "References\nReferences are a local implementation knowledgebase, not code to copy. Discover or register repositories with reference_discover or reference_add, ingest them with reference_ingest, then query them with reference_query before adding a nontrivial helper. Use reference_propose when a match is worth turning into review material. Results combine structured filters, hybrid BM25/vector search, dependency status, and optional branch context.\nmcp__nbskill__.reference(\n    action=\"query\",\n    query=\"AST definition helper\",\n    top_k=5,\n    include_branch=True,\n)",
    "crumbs": [
      "nbskill"
    ]
  },
  {
    "objectID": "index.html#graph-structure",
    "href": "index.html#graph-structure",
    "title": "nbskill",
    "section": "Graph structure",
    "text": "Graph structure\nThe local graph records notebooks, cells, headings, symbols, and imports. Its edges record containment, definitions, calls, imports, documentation order, and related symbols. Call and import edges carry evidence separately from heuristic similarity edges.\nThis gives nbskill a fast, project-wide map of the notebook source: agents can assess the impact of a change, follow a symbol to its callers, and spot cross-notebook private-helper use without reading every notebook. It is a navigation and maintenance aid, not a runtime dependency analyzer.\nAsk for project context with the graph attached when you need to trace a symbol through the notebook structure:\nmcp__nbskill__.context(target=\"project\", scope=\"nbs\", include_graph=True)",
    "crumbs": [
      "nbskill"
    ]
  },
  {
    "objectID": "index.html#problem-solution-memory",
    "href": "index.html#problem-solution-memory",
    "title": "nbskill",
    "section": "Problem-solution memory",
    "text": "Problem-solution memory\nBefore choosing an approach, query problem_memory for similar verified lessons from this and other projects. After verification, add a reusable problem-solution pair with short evidence and at least four normalized tags, such as topic, sub-topic, library, problem category, and solution category.\nTags make lessons precise enough to reuse: a query with tags applies an exact all-of filter, while the natural-language problem ranks the remaining matches. This keeps a broadly similar memory from being mistaken for a directly applicable solution.\n\ngraph = notebook_knowledge_graph_data(\"nbs\")\ndict(nodes=len(graph[\"nodes\"]), edges=len(graph[\"edges\"]), node_types=sorted({node[\"type\"] for node in graph[\"nodes\"]}), edge_types=sorted({edge[\"type\"] for edge in graph[\"edges\"]}))\n\n{'nodes': 3588,\n 'edges': 13426,\n 'node_types': ['cell', 'heading', 'import', 'notebook', 'symbol'],\n 'edge_types': ['calls',\n  'contains',\n  'defines',\n  'documents',\n  'imports',\n  'precedes',\n  'similar_to']}",
    "crumbs": [
      "nbskill"
    ]
  },
  {
    "objectID": "13_cli.html",
    "href": "13_cli.html",
    "title": "nbskill",
    "section": "",
    "text": "install_nbskill installs the nbskill skill and writes the project-local Codex MCP configuration. nbskill_mcp_log prints the recent MCP metrics and problems, while nbskill_mcp_log_problems limits the report to problems. These terminal commands work without an already-connected MCP client; nbskill_mcp remains the MCP server entry point.\n\nfrom fastcore.test import *\n\n\nsource\n\n\ndef install_nbskill(\n    target:str='codex', # codex, claude, cursor, both, or custom with skills_dir\n    skills_dir:str | None=None, # Parent skills directory for a custom target\n    skill_name:str='jupyter-notebooks', # Installed skill directory name\n    overwrite:bool=True, # Update stale managed files and MCP entries\n    install_hooks:bool=False, # Install nbdev pre-commit hooks in the current project\n    restart_mcp:bool=True, # Print reconnect guidance after configuring the server\n    cursor_workspace:str | None=None, # Cursor workspace, or global configuration when omitted\n    codex_workspace:str | None='.', # Project workspace for .codex/config.toml\n    reference_roots:str='~/projects', # Local Git repositories to register\n    index_references:bool=True, # Index discovered references during installation\n):\nInstall current nbskill instructions and optional MCP configuration.\n\n\n\nUse nbskill_mcp_log for the recent tool and problem summary. Use nbskill_mcp_log_problems when only failures and warnings matter. Both accept --path, --limit, and --json-output.\n\nsource\n\n\n\ndef nbskill_mcp_log(\n    path:str | None=None, # MCP JSONL log path; uses NBSKILL_MCP_LOG or ~/.nbskill-mcp.jsonl\n    limit:int=20, # Maximum problem and tool rows to print\n    json_output:bool=False, # Print machine-readable JSON\n):\nPrint concise nbskill MCP log metrics and recent problems.\n\nsource\n\n\n\ndef nbskill_mcp_log_problems(\n    path:str | None=None, # MCP JSONL log path; uses NBSKILL_MCP_LOG or ~/.nbskill-mcp.jsonl\n    limit:int=20, # Maximum problem rows to print\n    json_output:bool=False, # Print machine-readable JSON\n):\nPrint only the problem-focused nbskill MCP log summary.\n\nimport tomllib\nfrom pathlib import Path\n\nfrom fastcore.test import *",
    "crumbs": [
      "Restored command-line tools"
    ]
  },
  {
    "objectID": "13_cli.html#restored-command-line-tools",
    "href": "13_cli.html#restored-command-line-tools",
    "title": "nbskill",
    "section": "",
    "text": "install_nbskill installs the nbskill skill and writes the project-local Codex MCP configuration. nbskill_mcp_log prints the recent MCP metrics and problems, while nbskill_mcp_log_problems limits the report to problems. These terminal commands work without an already-connected MCP client; nbskill_mcp remains the MCP server entry point.\n\nfrom fastcore.test import *\n\n\nsource\n\n\ndef install_nbskill(\n    target:str='codex', # codex, claude, cursor, both, or custom with skills_dir\n    skills_dir:str | None=None, # Parent skills directory for a custom target\n    skill_name:str='jupyter-notebooks', # Installed skill directory name\n    overwrite:bool=True, # Update stale managed files and MCP entries\n    install_hooks:bool=False, # Install nbdev pre-commit hooks in the current project\n    restart_mcp:bool=True, # Print reconnect guidance after configuring the server\n    cursor_workspace:str | None=None, # Cursor workspace, or global configuration when omitted\n    codex_workspace:str | None='.', # Project workspace for .codex/config.toml\n    reference_roots:str='~/projects', # Local Git repositories to register\n    index_references:bool=True, # Index discovered references during installation\n):\nInstall current nbskill instructions and optional MCP configuration.\n\n\n\nUse nbskill_mcp_log for the recent tool and problem summary. Use nbskill_mcp_log_problems when only failures and warnings matter. Both accept --path, --limit, and --json-output.\n\nsource\n\n\n\ndef nbskill_mcp_log(\n    path:str | None=None, # MCP JSONL log path; uses NBSKILL_MCP_LOG or ~/.nbskill-mcp.jsonl\n    limit:int=20, # Maximum problem and tool rows to print\n    json_output:bool=False, # Print machine-readable JSON\n):\nPrint concise nbskill MCP log metrics and recent problems.\n\nsource\n\n\n\ndef nbskill_mcp_log_problems(\n    path:str | None=None, # MCP JSONL log path; uses NBSKILL_MCP_LOG or ~/.nbskill-mcp.jsonl\n    limit:int=20, # Maximum problem rows to print\n    json_output:bool=False, # Print machine-readable JSON\n):\nPrint only the problem-focused nbskill MCP log summary.\n\nimport tomllib\nfrom pathlib import Path\n\nfrom fastcore.test import *",
    "crumbs": [
      "Restored command-line tools"
    ]
  },
  {
    "objectID": "pyskill.html",
    "href": "pyskill.html",
    "title": "Native nbskill Pyskill",
    "section": "",
    "text": "from pathlib import Path\nfrom nbskill import skill\nfrom tempfile import gettempdir\nfrom fastcore.nbio import mk_cell, read_nb\nfrom nbskill.foundation import write_demo_notebook\nfrom nbskill.skill import context\nnbskill.skill is the short, direct API for notebook-owned code. Install nbskill in the same Python environment that runs the agent, including its dependencies: use uv pip install nbskill, or uv pip install -e /path/to/nbskill while developing. A Pyskill entry point alone is discovery metadata, not an installation. Then import it with from nbskill.skill import *. It is the source of truth for the notebook workflow; MCP remains available for clients that need a tool adapter.\nReference queries use the machine-level ~/.nbskill/reference_knowledge directory across workspaces. Set NBSKILL_REFERENCE_HOME when a separate reference index is required.",
    "crumbs": [
      "Native nbskill Pyskill"
    ]
  },
  {
    "objectID": "pyskill.html#work-with-a-notebook",
    "href": "pyskill.html#work-with-a-notebook",
    "title": "Native nbskill Pyskill",
    "section": "Work with a notebook",
    "text": "Work with a notebook\nCall generated_owner(path) before touching a Python file. A returned notebook means the Python file is generated and must not be edited. Call context to read the relevant notebook cells. For a change that needs prior art, call reference_query before choosing an implementation. Use edit_notebook for the one structured mutation.\nProve the result in the same change loop. exec_nb runs the affected notebook scope without writing outputs when check_only=True. diff_nb shows the code-cell change, and style_check reports source diagnostics. Ordinary Python files remain on the normal coding path.",
    "crumbs": [
      "Native nbskill Pyskill"
    ]
  },
  {
    "objectID": "pyskill.html#context",
    "href": "pyskill.html#context",
    "title": "Native nbskill Pyskill",
    "section": "context",
    "text": "context\ncontext reads a notebook through its cells. Start with a summary, then request full context only for the cells that matter:\n\nsummary = context(\"nbs/01_read.ipynb#context\", scope=\"nbs\", view=\"summary\", verbose=False)\nassert summary[\"kind\"] == \"context\"",
    "crumbs": [
      "Native nbskill Pyskill"
    ]
  },
  {
    "objectID": "pyskill.html#generated_owner",
    "href": "pyskill.html#generated_owner",
    "title": "Native nbskill Pyskill",
    "section": "generated_owner",
    "text": "generated_owner\ngenerated_owner identifies the notebook that owns a generated Python file. A None result means ordinary Python tooling may edit the file:\n\nowner = generated_owner(\"nbskill/skill.py\")\nassert owner.name == \"14_pyskill.ipynb\"\nowner",
    "crumbs": [
      "Native nbskill Pyskill"
    ]
  },
  {
    "objectID": "pyskill.html#reference_query",
    "href": "pyskill.html#reference_query",
    "title": "Native nbskill Pyskill",
    "section": "reference_query",
    "text": "reference_query\nUse reference_query before adding a nontrivial helper. It searches the shared reference index and direct dependencies:\n\nmatches = reference_query(\"find an nbdev notebook export helper\", top_k=3)\nmatches[\"hits\"]",
    "crumbs": [
      "Native nbskill Pyskill"
    ]
  },
  {
    "objectID": "pyskill.html#edit_notebook",
    "href": "pyskill.html#edit_notebook",
    "title": "Native nbskill Pyskill",
    "section": "edit_notebook",
    "text": "edit_notebook\nedit_notebook applies a structured edit atomically. Read the notebook first and use the returned cell ID and exact source text:\n\nedits = [dict(op=\"replace_text\", cell_id=\"&lt;cell-id&gt;\", old=\"old text\", new=\"new text\")]\nchange = edit_notebook(\"nbs/14_pyskill.ipynb\", edits)\nchange[\"changed\"]",
    "crumbs": [
      "Native nbskill Pyskill"
    ]
  },
  {
    "objectID": "pyskill.html#exec_nb",
    "href": "pyskill.html#exec_nb",
    "title": "Native nbskill Pyskill",
    "section": "exec_nb",
    "text": "exec_nb\nexec_nb runs the affected notebook scope. check_only=True validates code without writing outputs back to the notebook:\n\nexec_nb(\"nbs/14_pyskill.ipynb\", check_only=True)",
    "crumbs": [
      "Native nbskill Pyskill"
    ]
  },
  {
    "objectID": "pyskill.html#diff_nb",
    "href": "pyskill.html#diff_nb",
    "title": "Native nbskill Pyskill",
    "section": "diff_nb",
    "text": "diff_nb\ndiff_nb shows changed source cells without the noise of raw notebook JSON:\n\ndiff_nb(\"nbs/14_pyskill.ipynb\", ref_a=\"HEAD\")",
    "crumbs": [
      "Native nbskill Pyskill"
    ]
  },
  {
    "objectID": "pyskill.html#style_check",
    "href": "pyskill.html#style_check",
    "title": "Native nbskill Pyskill",
    "section": "style_check",
    "text": "style_check\nstyle_check reports notebook hygiene and style diagnostics. Restrict it to changed cells while iterating:\n\nstyle_check(\"nbs/14_pyskill.ipynb\", changed_only=True)\n\n\nDirect workflow check\nThis test uses the public Pyskill to make a structured change to a fresh notebook. It protects the native path from quietly depending on MCP.",
    "crumbs": [
      "Native nbskill Pyskill"
    ]
  },
  {
    "objectID": "05_convert.html",
    "href": "05_convert.html",
    "title": "nbskill",
    "section": "",
    "text": "Move Python modules into nbdev notebooks while preserving an exportable story.\nConversion is a bridge for projects that began as .py files but want nbdev notebooks as the source of truth. The converter keeps the first pass mechanical: preserve source order, create explicit #| default_exp and #| export cells, and split large classes only when that makes future notebook edits easier.\nconvert(\"src/tool.py\", mode=\"notebook\", nbs_path=\"nbs\")\nconvert(\"src\", mode=\"notebook\", nbs_path=\"nbs\", preserve_tree=True)\nconvert(\"src\", mode=\"project\", dest=\"./nbdev-app\")\n\n\nConversion is Python-first. convert migrates one Python file, a folder of Python files, or a pure-Python package into an nbdev project. The lower-level helpers preserve source order, create stable nbdev cells and ids, support dry-run/force controls, keep package tree choices explicit, and export generated modules when writing real notebooks.\n\nfrom contextlib import redirect_stdout\nfrom io import StringIO\nfrom fastcore.nbio import read_nb\nfrom nbskill.convert import py2nb as _example_py2nb\nfrom nbskill.foundation import demo_path, remove_demo_path, write_demo_notebook\nfrom fastcore.test import *\nimport tomllib\n\n\n_preview = []\nroot = demo_path(\"05_convert_example\")\ntry:\n    root.mkdir()\n    src = root / \"calc.py\"\nfinally:\n    remove_demo_path(root)\n_preview\n\n\n\n\nConversion starts by finding exact source ranges for imports, functions, classes, and methods. Using ast keeps the conversion structured instead of relying on fragile string searches.\n\n\n\nThe converter writes #| default_exp first, collects imports together, skips existing __all__ declarations, and emits exported cells for the definitions that should become the notebook source.\n\n\n\nconvert dispatches by mode. mode=\"notebook\" handles either one Python file or a folder of Python files. mode=\"project\" creates a pragmatic nbdev project from a pure-Python package.\n\nsource\n\n\n\ndef convert(\n    path:str, # Python file/folder, or existing Python project/package root\n    mode:str='notebook', # notebook for file/folder conversion, project for nbdev project creation\n    dest:str | None=None, # Notebook path/output folder for notebook mode; project root for project mode\n    nbs_path:str='nbs', # Destination notebooks folder\n    recursive:bool=True, # Search subfolders in notebook mode\n    maxdepth:int | None=None, # Maximum folder scan depth in notebook mode\n    preserve_tree:bool=True, # Preserve package folder structure under nbs_path\n    class_lines:int=100, # Split classes larger than this line count\n    method_lines:int=10, # Split methods larger than this out of large classes\n    package:str | None=None, # Package root name\n    include:str | None=None, # Comma-separated include globs\n    exclude:str | None=None, # Comma-separated exclude globs\n    skip_init:bool=True, # Skip __init__.py files in notebook mode\n    include_tests:bool=False, # Include test files in notebook mode\n    dry_run:bool=False, # Show planned writes without writing\n    force:bool=True, # Overwrite existing notebooks/files\n    run_validation:bool=True, # Run nbdev validation in project mode\n):\nConvert Python sources to nbdev notebooks or a small nbdev project.",
    "crumbs": [
      "Converting Python into notebooks"
    ]
  },
  {
    "objectID": "05_convert.html#converting-python-into-notebooks",
    "href": "05_convert.html#converting-python-into-notebooks",
    "title": "nbskill",
    "section": "",
    "text": "Move Python modules into nbdev notebooks while preserving an exportable story.\nConversion is a bridge for projects that began as .py files but want nbdev notebooks as the source of truth. The converter keeps the first pass mechanical: preserve source order, create explicit #| default_exp and #| export cells, and split large classes only when that makes future notebook edits easier.\nconvert(\"src/tool.py\", mode=\"notebook\", nbs_path=\"nbs\")\nconvert(\"src\", mode=\"notebook\", nbs_path=\"nbs\", preserve_tree=True)\nconvert(\"src\", mode=\"project\", dest=\"./nbdev-app\")\n\n\nConversion is Python-first. convert migrates one Python file, a folder of Python files, or a pure-Python package into an nbdev project. The lower-level helpers preserve source order, create stable nbdev cells and ids, support dry-run/force controls, keep package tree choices explicit, and export generated modules when writing real notebooks.\n\nfrom contextlib import redirect_stdout\nfrom io import StringIO\nfrom fastcore.nbio import read_nb\nfrom nbskill.convert import py2nb as _example_py2nb\nfrom nbskill.foundation import demo_path, remove_demo_path, write_demo_notebook\nfrom fastcore.test import *\nimport tomllib\n\n\n_preview = []\nroot = demo_path(\"05_convert_example\")\ntry:\n    root.mkdir()\n    src = root / \"calc.py\"\nfinally:\n    remove_demo_path(root)\n_preview\n\n\n\n\nConversion starts by finding exact source ranges for imports, functions, classes, and methods. Using ast keeps the conversion structured instead of relying on fragile string searches.\n\n\n\nThe converter writes #| default_exp first, collects imports together, skips existing __all__ declarations, and emits exported cells for the definitions that should become the notebook source.\n\n\n\nconvert dispatches by mode. mode=\"notebook\" handles either one Python file or a folder of Python files. mode=\"project\" creates a pragmatic nbdev project from a pure-Python package.\n\nsource\n\n\n\ndef convert(\n    path:str, # Python file/folder, or existing Python project/package root\n    mode:str='notebook', # notebook for file/folder conversion, project for nbdev project creation\n    dest:str | None=None, # Notebook path/output folder for notebook mode; project root for project mode\n    nbs_path:str='nbs', # Destination notebooks folder\n    recursive:bool=True, # Search subfolders in notebook mode\n    maxdepth:int | None=None, # Maximum folder scan depth in notebook mode\n    preserve_tree:bool=True, # Preserve package folder structure under nbs_path\n    class_lines:int=100, # Split classes larger than this line count\n    method_lines:int=10, # Split methods larger than this out of large classes\n    package:str | None=None, # Package root name\n    include:str | None=None, # Comma-separated include globs\n    exclude:str | None=None, # Comma-separated exclude globs\n    skip_init:bool=True, # Skip __init__.py files in notebook mode\n    include_tests:bool=False, # Include test files in notebook mode\n    dry_run:bool=False, # Show planned writes without writing\n    force:bool=True, # Overwrite existing notebooks/files\n    run_validation:bool=True, # Run nbdev validation in project mode\n):\nConvert Python sources to nbdev notebooks or a small nbdev project.",
    "crumbs": [
      "Converting Python into notebooks"
    ]
  },
  {
    "objectID": "00_foundation.html",
    "href": "00_foundation.html",
    "title": "nbskill",
    "section": "",
    "text": "Small shared mechanics: Python API behavior, failure telemetry, cell parsing, validation, chapter navigation, and dynamic cell classification for notebook context.\nThis notebook is the shared basement of the project. It keeps the small private helpers that every public tool relies on: Python API behavior, safe error reporting, cell selection, cell parsing, semantic cell classification, and chapter addressing.\nMost functions here are intentionally private. The rest of the project can stay user-facing because this notebook absorbs the messy details of notebooks as data structures.\nRead it as a tour of the shapes nbskill needs to recognize. A notebook is not just text: it has cells, headings, generated Python files, visible outputs, and tiny bits of metadata. The foundation layer gives all later notebooks one vocabulary for those shapes, so the public tools can talk about notebooks without making users think in raw JSON.\n\n\nThis notebook owns the shared invariants for every other tool. Production behavior is: path helpers resolve notebooks from project-root and notebook-dir execution, cell-block parsing creates valid notebook cells, metadata stamping records semantic classes and export hashes, and validation reports missing or stale metadata before edits proceed.\nThe helpers here deliberately stay small because every higher-level tool depends on them. For example, parse_cells turns friendly cell-block text into notebook cells, the semantic classifiers let readers say “show me tests” or “show me exported code” without parsing raw notebook JSON.\n\nfrom contextlib import redirect_stdout\nfrom io import StringIO\nimport os\n\nfrom fastcore.test import test_eq\n\n\nsource\n\n\n\ndef numbered_notebook_path(\n    nbs_path, name, index:NoneType=None\n):\nReturn a flat numbered notebook path for a simple module name.\n\n\n\nExamples and tests in later notebooks need small notebooks they can safely mutate. Scratch helpers create disposable artifacts in the system temporary directory and remove them after use, so notebook test runners never create repository data folders. tool_notebook builds the shared reader, executor, reviewer, and MCP example shape in code.\n\nsource\n\n\n\ndef remove_demo_path(\n    path\n):\nCall self as a function.\ndemo_path names a scratch artifact in the system temporary directory by default and optionally removes any previous copy. It only creates the parent folder; helpers that write content decide what belongs at the path. An explicit base remains available when an example needs a chosen location.\nUse demo_path_context when a test needs an arbitrary scratch file or directory and should clean it up after the block. That keeps cleanup policy in one helper instead of repeating try/finally in every test.\n\nsource\n\n\n\ndef demo_path_context(\n    name, base:NoneType=None, reset:bool=True\n):\nYield a demo path and remove it when the block exits.\n\nsource\n\n\n\ndef demo_path(\n    name, base:NoneType=None, reset:bool=True\n):\nReturn a scratch path in the system temporary directory or base, optionally resetting it.\n\nsource\n\n\n\ndef path_candidates(\n    path\n):\nReturn cwd-relative path candidates for root and notebook-dir execution.\n\n_path_candidate_rows = path_candidates(\"nbs/00_foundation.ipynb\")\nassert _path_candidate_rows[0] == Path(\"nbs/00_foundation.ipynb\")\nif Path.cwd().name == \"nbs\":\n    assert _path_candidate_rows[-1] == Path(\"00_foundation.ipynb\")\nelse:\n    assert _path_candidate_rows == [Path(\"nbs/00_foundation.ipynb\")]\nassert path_candidates(Path(\"/tmp/example.ipynb\")) == [Path(\"/tmp/example.ipynb\")]\nprint(\"path_candidates handles root and notebook-dir paths\")\n\npath_candidates handles root and notebook-dir paths\n\n\nNotebook helpers should fail gently when a path is missing, unreadable, or shaped like ordinary JSON instead of a Jupyter notebook. is_valid_ipynb keeps that contract small and dependency-free.\n\nsource\n\n\n\ndef is_valid_ipynb(\n    path\n):\nCall self as a function.\nwrite_demo_notebook owns the whole lifetime of a scratch notebook: create it, yield its path to the example, then remove it even if the example fails.\n\nsource\n\n\n\ndef write_demo_notebook(\n    name, cells:NoneType=None, base:NoneType=None, reset:bool=True\n):\nCall self as a function.\ntool_notebook keeps the shared reader, executor, reviewer, and MCP test shape in code. Each test writes a fresh notebook and removes it afterwards.\n\nsource\n\n\n\ndef tool_notebook():\nReturn the canonical notebook used by tool tests and examples.\nwrite_tool_notebook gives each example and test an isolated copy of the canonical tool notebook.\n\nsource\n\n\n\ndef write_tool_notebook(\n    name:str='tool.ipynb', base:NoneType=None\n):\nWrite the canonical tool notebook for one example or test.\nwrite_demo_file keeps temporary conversion inputs out of the repository.\n\nsource\n\n\n\ndef write_demo_file(\n    name, source, base:NoneType=None\n):\nWrite one disposable text file for an example or test.\nHere is the tiny notebook fixture in the wild. It gives examples a real .ipynb to read, then vanishes after the context exits, which keeps the repo tidy while still letting the rendered notebook show concrete output.\n\nfrom fastcore.nbio import read_nb\n\nwith write_demo_notebook(\"00_foundation_tour.ipynb\") as path:\n    nb = read_nb(path)\n    print(path.name)\n    print([cell.cell_type for cell in nb.cells])\n    print(str(nb.cells[0].source).splitlines()[0])\n\nprint(path.exists())\n\n00_foundation_tour.ipynb\n['markdown', 'code', 'code']\n## Demo notebook\nFalse\n\n\n\nwith write_demo_notebook(\"00_foundation_bad.ipynb\") as bad_path:\n    bad_path.write_text(\"{}\", encoding=\"utf-8\")\n    assert not is_valid_ipynb(bad_path)\n    assert not is_valid_ipynb(bad_path.with_suffix(\".missing.ipynb\"))\n\n\n\n\nThe public functions in later notebooks are direct Python APIs. These helpers keep return values and validation errors predictable while recording tool starts or failures in a small local failure map for debugging repeated friction.\n\nsource\n\n\n\ndef api_return(\n    value:NoneType=None\n):\nReturn a Python API value unchanged.\n\nsource\n\n\n\ndef api_error(\n    msg\n):\nRaise a Python API validation error.\n\nsource\n\n\n\ndef failure_map_path():\nCall self as a function.\n\nsource\n\n\n\ndef empty_failure_map():\nCall self as a function.\n\nsource\n\n\n\ndef load_failure_map(\n    path\n):\nCall self as a function.\n\nsource\n\n\n\ndef git_root(\n    path:str='.'\n):\nReturn the containing git repository root, if one exists.\n\nsource\n\n\n\ndef git_run(\n    root, *args\n):\nRun a git command from root and return stdout, or empty text on failure.\n\nsource\n\n\n\ndef git_tracked_paths(\n    root\n):\nReturn git-tracked paths relative to root.\n\nsource\n\n\n\ndef git_status_paths(\n    root\n):\nReturn paths with working-tree changes relative to root.\n\nsource\n\n\n\ndef git_diff_stats(\n    root, ref:str='HEAD'\n):\nReturn added/deleted line stats for working-tree changes from ref.\n\nsource\n\n\n\ndef load_nbskill_config(\n    path:str='.'\n):\nLoad .nbskill.py config from project root, merged over defaults.\n\nsource\n\n\n\ndef notebook_paths(\n    path:str='nbs'\n):\nReturn visible notebook paths for a file, directory, glob, or nbdev project path.\n\nsource\n\n\n\ndef source_without_directives(\n    source\n):\nReturn source with nbdev cell directives removed.\n\nsource\n\n\n\ndef source_hash(\n    source, length:int=12\n):\nReturn a SHA-256 source hash, shortened unless length is None.\n\nsource\n\n\n\ndef cap_text(\n    text, limit:int=2000, max_output_chars:NoneType=None\n):\nCap text, returning a string or review-style metadata when max_output_chars is used.\n\nsource\n\n\n\ndef file_line_count(\n    path\n):\nReturn the number of text lines in path, or zero when it cannot be read.",
    "crumbs": [
      "Foundation"
    ]
  },
  {
    "objectID": "00_foundation.html#foundation",
    "href": "00_foundation.html#foundation",
    "title": "nbskill",
    "section": "",
    "text": "Small shared mechanics: Python API behavior, failure telemetry, cell parsing, validation, chapter navigation, and dynamic cell classification for notebook context.\nThis notebook is the shared basement of the project. It keeps the small private helpers that every public tool relies on: Python API behavior, safe error reporting, cell selection, cell parsing, semantic cell classification, and chapter addressing.\nMost functions here are intentionally private. The rest of the project can stay user-facing because this notebook absorbs the messy details of notebooks as data structures.\nRead it as a tour of the shapes nbskill needs to recognize. A notebook is not just text: it has cells, headings, generated Python files, visible outputs, and tiny bits of metadata. The foundation layer gives all later notebooks one vocabulary for those shapes, so the public tools can talk about notebooks without making users think in raw JSON.\n\n\nThis notebook owns the shared invariants for every other tool. Production behavior is: path helpers resolve notebooks from project-root and notebook-dir execution, cell-block parsing creates valid notebook cells, metadata stamping records semantic classes and export hashes, and validation reports missing or stale metadata before edits proceed.\nThe helpers here deliberately stay small because every higher-level tool depends on them. For example, parse_cells turns friendly cell-block text into notebook cells, the semantic classifiers let readers say “show me tests” or “show me exported code” without parsing raw notebook JSON.\n\nfrom contextlib import redirect_stdout\nfrom io import StringIO\nimport os\n\nfrom fastcore.test import test_eq\n\n\nsource\n\n\n\ndef numbered_notebook_path(\n    nbs_path, name, index:NoneType=None\n):\nReturn a flat numbered notebook path for a simple module name.\n\n\n\nExamples and tests in later notebooks need small notebooks they can safely mutate. Scratch helpers create disposable artifacts in the system temporary directory and remove them after use, so notebook test runners never create repository data folders. tool_notebook builds the shared reader, executor, reviewer, and MCP example shape in code.\n\nsource\n\n\n\ndef remove_demo_path(\n    path\n):\nCall self as a function.\ndemo_path names a scratch artifact in the system temporary directory by default and optionally removes any previous copy. It only creates the parent folder; helpers that write content decide what belongs at the path. An explicit base remains available when an example needs a chosen location.\nUse demo_path_context when a test needs an arbitrary scratch file or directory and should clean it up after the block. That keeps cleanup policy in one helper instead of repeating try/finally in every test.\n\nsource\n\n\n\ndef demo_path_context(\n    name, base:NoneType=None, reset:bool=True\n):\nYield a demo path and remove it when the block exits.\n\nsource\n\n\n\ndef demo_path(\n    name, base:NoneType=None, reset:bool=True\n):\nReturn a scratch path in the system temporary directory or base, optionally resetting it.\n\nsource\n\n\n\ndef path_candidates(\n    path\n):\nReturn cwd-relative path candidates for root and notebook-dir execution.\n\n_path_candidate_rows = path_candidates(\"nbs/00_foundation.ipynb\")\nassert _path_candidate_rows[0] == Path(\"nbs/00_foundation.ipynb\")\nif Path.cwd().name == \"nbs\":\n    assert _path_candidate_rows[-1] == Path(\"00_foundation.ipynb\")\nelse:\n    assert _path_candidate_rows == [Path(\"nbs/00_foundation.ipynb\")]\nassert path_candidates(Path(\"/tmp/example.ipynb\")) == [Path(\"/tmp/example.ipynb\")]\nprint(\"path_candidates handles root and notebook-dir paths\")\n\npath_candidates handles root and notebook-dir paths\n\n\nNotebook helpers should fail gently when a path is missing, unreadable, or shaped like ordinary JSON instead of a Jupyter notebook. is_valid_ipynb keeps that contract small and dependency-free.\n\nsource\n\n\n\ndef is_valid_ipynb(\n    path\n):\nCall self as a function.\nwrite_demo_notebook owns the whole lifetime of a scratch notebook: create it, yield its path to the example, then remove it even if the example fails.\n\nsource\n\n\n\ndef write_demo_notebook(\n    name, cells:NoneType=None, base:NoneType=None, reset:bool=True\n):\nCall self as a function.\ntool_notebook keeps the shared reader, executor, reviewer, and MCP test shape in code. Each test writes a fresh notebook and removes it afterwards.\n\nsource\n\n\n\ndef tool_notebook():\nReturn the canonical notebook used by tool tests and examples.\nwrite_tool_notebook gives each example and test an isolated copy of the canonical tool notebook.\n\nsource\n\n\n\ndef write_tool_notebook(\n    name:str='tool.ipynb', base:NoneType=None\n):\nWrite the canonical tool notebook for one example or test.\nwrite_demo_file keeps temporary conversion inputs out of the repository.\n\nsource\n\n\n\ndef write_demo_file(\n    name, source, base:NoneType=None\n):\nWrite one disposable text file for an example or test.\nHere is the tiny notebook fixture in the wild. It gives examples a real .ipynb to read, then vanishes after the context exits, which keeps the repo tidy while still letting the rendered notebook show concrete output.\n\nfrom fastcore.nbio import read_nb\n\nwith write_demo_notebook(\"00_foundation_tour.ipynb\") as path:\n    nb = read_nb(path)\n    print(path.name)\n    print([cell.cell_type for cell in nb.cells])\n    print(str(nb.cells[0].source).splitlines()[0])\n\nprint(path.exists())\n\n00_foundation_tour.ipynb\n['markdown', 'code', 'code']\n## Demo notebook\nFalse\n\n\n\nwith write_demo_notebook(\"00_foundation_bad.ipynb\") as bad_path:\n    bad_path.write_text(\"{}\", encoding=\"utf-8\")\n    assert not is_valid_ipynb(bad_path)\n    assert not is_valid_ipynb(bad_path.with_suffix(\".missing.ipynb\"))\n\n\n\n\nThe public functions in later notebooks are direct Python APIs. These helpers keep return values and validation errors predictable while recording tool starts or failures in a small local failure map for debugging repeated friction.\n\nsource\n\n\n\ndef api_return(\n    value:NoneType=None\n):\nReturn a Python API value unchanged.\n\nsource\n\n\n\ndef api_error(\n    msg\n):\nRaise a Python API validation error.\n\nsource\n\n\n\ndef failure_map_path():\nCall self as a function.\n\nsource\n\n\n\ndef empty_failure_map():\nCall self as a function.\n\nsource\n\n\n\ndef load_failure_map(\n    path\n):\nCall self as a function.\n\nsource\n\n\n\ndef git_root(\n    path:str='.'\n):\nReturn the containing git repository root, if one exists.\n\nsource\n\n\n\ndef git_run(\n    root, *args\n):\nRun a git command from root and return stdout, or empty text on failure.\n\nsource\n\n\n\ndef git_tracked_paths(\n    root\n):\nReturn git-tracked paths relative to root.\n\nsource\n\n\n\ndef git_status_paths(\n    root\n):\nReturn paths with working-tree changes relative to root.\n\nsource\n\n\n\ndef git_diff_stats(\n    root, ref:str='HEAD'\n):\nReturn added/deleted line stats for working-tree changes from ref.\n\nsource\n\n\n\ndef load_nbskill_config(\n    path:str='.'\n):\nLoad .nbskill.py config from project root, merged over defaults.\n\nsource\n\n\n\ndef notebook_paths(\n    path:str='nbs'\n):\nReturn visible notebook paths for a file, directory, glob, or nbdev project path.\n\nsource\n\n\n\ndef source_without_directives(\n    source\n):\nReturn source with nbdev cell directives removed.\n\nsource\n\n\n\ndef source_hash(\n    source, length:int=12\n):\nReturn a SHA-256 source hash, shortened unless length is None.\n\nsource\n\n\n\ndef cap_text(\n    text, limit:int=2000, max_output_chars:NoneType=None\n):\nCap text, returning a string or review-style metadata when max_output_chars is used.\n\nsource\n\n\n\ndef file_line_count(\n    path\n):\nReturn the number of text lines in path, or zero when it cannot be read.",
    "crumbs": [
      "Foundation"
    ]
  },
  {
    "objectID": "00_foundation.html#generated-source-ownership",
    "href": "00_foundation.html#generated-source-ownership",
    "title": "nbskill",
    "section": "Generated source ownership",
    "text": "Generated source ownership\nnbdev can place a module docstring before its autogenerated-file notice. Source ownership therefore finds the notice in the exported text instead of assuming that it is in the first three physical lines. This keeps generated modules read-only for agents.\n\nsource\n\ngenerated_owner\ndef generated_owner(\n    path, notebooks:NoneType=None\n):\nReturn the source notebook for an nbdev-generated Python file, when known.\n\nsource\n\n\ninstall_nbdev_pre_commit_hooks\ndef install_nbdev_pre_commit_hooks(\n    path:str='.', run_nbdev_install_hooks:bool=True\n):\nInstall nbdev-clean and nbdev-test pre-commit hooks in a git-backed nbdev project.\nThe MCP server may be launched from many notebook projects, so it needs a small idempotent project bootstrap. Once a client workspace root is known, bootstrap_nbskill_project can copy the shared agent instructions, make notebook CI serial, and preserve nbskill metadata through nbdev cleanups without clobbering local AGENTS.md edits.\n\nsource\n\n\nbootstrap_nbskill_project\ndef bootstrap_nbskill_project(\n    path:str='.', overwrite_agents:bool=False, pause:str='1'\n):\nInstall nbskill project guardrails once a workspace root is known.\n\n\nParsing user-facing selectors\nSelectors are the boundary between human shorthand and list operations. A caller might ask for 3, 2:5, -1, or None; these helpers normalize that text before any read or write tool touches notebook cells. Keeping the grammar here makes higher-level tools predictable and keeps cell-index mistakes from spreading.\n\nsource\n\n\nparse_literal\ndef parse_literal(\n    value\n):\nCall self as a function.\n\nsource\n\n\nnone_if_string\ndef none_if_string(\n    value\n):\nCall self as a function.\n\n\nTurning text into notebook cells\nWrite tools accept plain text because that is what agents and humans can produce quickly. This section turns that text into real cells: split on block separators, honor %%markdown/%%code markers, and split exported code into symbol-sized chunks when needed. It is the small parser that makes notebook edits feel like editing a document instead of assembling JSON.\n\nsource\n\n\nsymbol_short_name\ndef symbol_short_name(\n    symbol\n):\nReturn the final dotted component of a symbol name.\n\nsource\n\n\ncall_name\ndef call_name(\n    node\n):\nReturn dotted name text for an AST name, attribute, or call expression.\n\nsource\n\n\nshort_call_name\ndef short_call_name(\n    node, default:NoneType=None\n):\nReturn the final dotted component of a call/name node, or default.\n\nsource\n\n\nis_definition_node\ndef is_definition_node(\n    node\n):\nCall self as a function.\n\nsource\n\n\nnode_start_line\ndef node_start_line(\n    node\n):\nCall self as a function.\n\nsource\n\n\nis_export_directive\ndef is_export_directive(\n    line\n):\nCall self as a function.\n\nsource\n\n\ncell_source\ndef cell_source(\n    cell\n):\nCall self as a function.\n\nsource\n\n\nparse_code_cell\ndef parse_code_cell(\n    cell\n):\nParse a code cell after removing nbdev directives, or return None.\n\n\nCell metadata and semantic classes\nOnce cells can be parsed, the tools need names for what they are doing. A code cell with output is an example; a code cell with an assert is a test; a markdown cell with prose is documentation. Those labels power filters such as type=tests and help review tools talk about notebooks in human terms.\n\n\nDomain entities\nThese tiny models give the project names for the shapes it already manipulates: cells, chapters, notebooks, and symbols. The classes stay thin; most behavior is patched onto them so each concept can grow near the helper that understands it.\n\n\nTyped cell vocabularies\nCellType names the three notebook cell kinds, SemanticType names the reader-facing role a cell plays, and Directive names the nbdev directives an editor can stamp onto a cell. Enums keep the rest of the project from passing bare strings around, and let context drop directive lines because the type already carries that meaning.\n\nsource\n\n\napply_directives\ndef apply_directives(\n    source, directives\n):\nPrepend any missing #| ... directive lines to source.\n\nsource\n\n\ndirective_lines\ndef directive_lines(\n    directives\n):\nReturn #| ... lines for Directives given as members, names, or a comma string.\n\nsource\n\n\nDirective\ndef Directive(\n    *args, **kwds\n):\nnbdev cell directives an editor can stamp onto a cell.\n\nsource\n\n\nSemanticType\ndef SemanticType(\n    *args, **kwds\n):\nThe reader-facing role of a cell, used by context and review tools.\n\nsource\n\n\nCellType\ndef CellType(\n    *args, **kwds\n):\nThe notebook cell kinds nbskill understands.\n\nsource\n\n\nNotebookSymbol\ndef NotebookSymbol(\n    name:str, path:object=None, cell_id:str='', cell_idx:object=None, module:str='', kind:str='',\n    data:object=&lt;factory&gt;\n)-&gt;None:\nA named notebook symbol plus optional placement and graph data.\n\nsource\n\n\nNotebook\ndef Notebook(\n    nb:object, path:object=None\n)-&gt;None:\nA notebook plus the path it was read from.\n\nsource\n\n\nChapter\ndef Chapter(\n    title:str, start:int, end:int, cells:object=&lt;factory&gt;\n)-&gt;None:\nA contiguous heading-owned span inside a notebook.\n\nsource\n\n\nCell\ndef Cell(\n    cell:object, idx:object=None, path:object=None\n)-&gt;None:\nA focused view of one notebook cell plus its optional location.\nxml_escape and xml_attrs keep model-to-XML snippets boring and predictable. They are intentionally small because the XML is only for readable LLM context, not a general document writer.\n\nsource\n\n\nxml_attrs\ndef xml_attrs(\n    **kwargs\n):\nRender non-empty keyword values as XML attributes.\n\nsource\n\n\nxml_escape\ndef xml_escape(\n    text\n):\nEscape text for simple XML-shaped context snippets.\n\n{\n    \"escaped\": xml_escape(\"&lt;cell&gt;\"),\n    \"attrs\": xml_attrs(kind=\"cell\", title=\"&lt;demo&gt;\"),\n}\n\n{'escaped': '&lt;cell&gt;', 'attrs': 'kind=\"cell\" title=\"&lt;demo&gt;\"'}\n\n\n\n\n\nCell.from_source\ndef from_source(\n    source, default_type:str='code', idx:NoneType=None, path:NoneType=None\n):\nBuild a cell model from editable source text.\n\nsource\n\n\nCell.cell_type\ndef cell_type():\nReturn the cell type as a CellType.\n\nsource\n\n\nCell.id\ndef id():\nReturn the notebook cell id.\n\n\n\nCell.source\ndef source():\nReturn the cell source as one string.\n\n\n\nCell.metadata\ndef metadata():\nReturn mutable cell metadata.\n\n\n\nCell.code\ndef code():\nReturn source without nbdev directives.\n\n\n\nCell.directive\ndef directive(\n    name:NoneType=None\n):\nReturn the first directive, or the value for one directive name.\n\n\n\nCell.directives\ndef directives():\nReturn nbdev directive lines without the leading marker.\n\n\n\nCell.heading_title\ndef heading_title(\n    levels:tuple=(2,)\n):\nReturn the first markdown heading title whose level is allowed.\n\n\n\nCell.class_names\ndef class_names():\nReturn semantic class names for the cell.\n\n\n\nCell.prefix\ndef prefix(\n    show_ids:bool=False\n):\nReturn the compact rendered cell prefix used in context output.\n\nsource\n\n\nCell.to_xml\ndef to_xml(\n    include_output:bool=False\n):\nReturn an XML-shaped cell view for LLM context; directives become the semantic type.\n\nsource\n\n\nCell.to_record\ndef to_record():\nReturn a small structured record for this cell.\n\n\n\nChapter.all\ndef all(\n    cells, levels:tuple=(2,), fallback:NoneType=None\n):\nReturn all chapter models opened by markdown headings.\n\n\n\nChapter.from_span\ndef from_span(\n    span, cells:NoneType=None\n):\nBuild a chapter model from a span dictionary.\n\n\n\nChapter.body\ndef body():\nReturn cells inside this chapter span.\n\n\n\nChapter.to_span\ndef to_span():\nReturn the dictionary span shape used by existing APIs.\n\n\n\nChapter.to_record\ndef to_record():\nReturn structured chapter metadata.\n\n\n\nChapter.to_xml\ndef to_xml(\n    include_cells:bool=True\n):\nReturn an XML-shaped chapter view for LLM context.\n\n\n\nNotebook.cells\ndef cells():\nReturn notebook cells as cell models.\n\n\n\nNotebook.from_path\ndef from_path(\n    path\n):\nRead a notebook path into a notebook document model.\n\nsource\n\n\nfind_cell_by_id\ndef find_cell_by_id(\n    cells, cell_id\n):\nCall self as a function.\n\n\n\nNotebook.cell\ndef cell(\n    cell_id\n):\nReturn one cell model by stable id.\n\nsource\n\n\nnotebook_hash\ndef notebook_hash(\n    nb\n):\nReturn a stable source hash for a notebook’s ordered cell contents.\n\nsource\n\n\nmatches_filter\ndef matches_filter(\n    source, pattern\n):\nCall self as a function.\n\nsource\n\n\nchapter_spans\ndef chapter_spans(\n    cells, levels:tuple=(2,), fallback:NoneType=None\n):\nReturn contiguous notebook cell spans opened by markdown headings.\n\nsource\n\n\none_chapter\ndef one_chapter(\n    cells, chapter, create:bool=False\n):\nCall self as a function.\n\n\n\nNotebook.chapter\ndef chapter(\n    title, create:bool=False\n):\nReturn one chapter model by title or regex.\n\n\n\nNotebook.notebook_source_hash\ndef notebook_source_hash():\nReturn the notebook’s ordered source hash.\n\nsource\n\n\nensure_cell_ids\ndef ensure_cell_ids(\n    nb\n):\nAdd nbdev-style ids to notebook cells missing id, mutating and returning nb.\n\nsource\n\n\nvalidate_code_cells\ndef validate_code_cells(\n    cells\n):\nCall self as a function.\n\nsource\n\n\nexported_py_path\ndef exported_py_path(\n    nb_path, nb:NoneType=None\n):\nReturn the generated Python file path for an nbdev notebook, if it has one.\n\nsource\n\n\ncommit_notebook\ndef commit_notebook(\n    path, trial, before:NoneType=None, affected_cell_ids:NoneType=None, validate_code:bool=True, dry_run:bool=False,\n    export:bool=True, writer:function=write_nb\n):\nValidate, stamp, write, export, and rollback one notebook as one commit.\n\n\n\nNotebook.commit\ndef commit(\n    **kwargs\n):\nCommit this notebook model through the shared atomic writer.\n\n\nNotebook document helpers\nNotebookDocument.cell returns one cell model by id. chapter resolves a heading-owned span, notebook_source_hash gives the model’s stable source hash, and commit sends the current model through commit_notebook.\n\ndocument = NotebookDocument(new_nb([mk_cell(\"## Alpha\", cell_type=\"markdown\"), mk_cell(\"answer = 42\")]), path=\"demo.ipynb\")\n(document.cell(document.nb.cells[1].id).source, document.chapter(\"Alpha\").title, document.notebook_source_hash())\n\nThe notebook domain model gives callers small, navigable objects: Notebook.cell finds a stable cell id, Notebook.chapter returns a dynamic Chapter, Notebook.notebook_source_hash summarizes ordered source, and Notebook.commit writes through the same atomic path as the editing tools.\n\n\n\nNotebook.to_xml\ndef to_xml(\n    levels:tuple=(2,), include_cells:bool=True\n):\nReturn an XML-shaped notebook view for LLM context.\n\n\n\nNotebook.chapters\ndef chapters(\n    levels:tuple=(2,), fallback:NoneType=None\n):\nReturn notebook chapters as chapter models.\n\nsource\n\n\nNotebookSymbol.from_record\ndef from_record(\n    record, data:NoneType=None\n):\nBuild a symbol model from a graph or definition record.\n\nsource\n\n\nNotebookSymbol.to_record\ndef to_record():\nReturn a small structured record for this symbol.\n\nsource\n\n\nNotebookSymbol.to_xml\ndef to_xml():\nReturn an XML-shaped symbol view for LLM context.\n\nsource\n\n\ncell_metadata\ndef cell_metadata(\n    cell\n):\nCall self as a function.\n\nsource\n\n\nnotebook_metadata\ndef notebook_metadata(\n    nb\n):\nCall self as a function.\n\nsource\n\n\nnbskill_cell_metadata\ndef nbskill_cell_metadata(\n    cell, create:bool=True\n):\nReturn the shared nbskill metadata block for a cell.\n\nsource\n\n\nfile_hash\ndef file_hash(\n    path\n):\nCall self as a function.\n\nassert source_without_directives(\"#| export\\nx = 1\\n#| default_exp demo\") == \"x = 1\"\nwith write_demo_notebook(\"foundation_notebook_paths.ipynb\") as nb_path:\n    checkpoint = nb_path.parent / \".ipynb_checkpoints\" / \"skip.ipynb\"\n    checkpoint.parent.mkdir(parents=True, exist_ok=True)\n    write_nb(new_nb([mk_cell(\"assert True\")]), checkpoint)\n    try:\n        paths = notebook_paths(nb_path.parent)\n        assert nb_path in paths\n        assert checkpoint not in paths\n    finally:\n        remove_demo_path(checkpoint.parent)\ngenerated = demo_path(\"generated_owner_sample.py\")\ntry:\n    generated.write_text(\n        '\\n'.join([\n            '\\\"\\\"\\\"Generated module documentation.',\n            'It can span several lines',\n            'before the nbdev header.',\n            '\\\"\\\"\\\"',\n            '',\n            '# AUTOGENERATED! DO NOT EDIT! File to edit: ../00_foundation.ipynb.',\n        ]) + '\\n',\n        encoding=\"utf-8\",\n    )\n    assert generated_owner(generated).name == \"00_foundation.ipynb\"\n    assert file_line_count(generated) == 6\nfinally:\n    remove_demo_path(generated)\nassert cap_text(\"abcdef\", limit=3).startswith(\"abc\")\nassert cap_text(\"abcdef\", max_output_chars=3)[\"truncated\"] is True\n\n\nsource\n\n\nstamp_export_metadata\ndef stamp_export_metadata(\n    nb, py_path\n):\nCall self as a function.\n\n\nProject-scoped nbdev export\nrun_nbdev_export_from_project and export_notebook deliberately run nbdev export in an isolated process under a project-scoped lock. This looks heavier than calling nbdev_export directly, but it protects the MCP server when several repositories on the same computer are edited at the same time.\nThe two failures this avoids are subtle. First, nbdev export depends on project context; changing the server process cwd with os.chdir is global state, so concurrent exports from different projects can race. Second, an MCP client timeout does not stop a Python worker thread, so a timed-out edit can keep exporting in the background and consume memory. A child process is a boundary the caller can terminate, while the lock only serializes exports for the same project. Different projects get different lock files and can still make progress independently.\n\nsource\n\n\nrun_nbdev_export_from_project\ndef run_nbdev_export_from_project(\n    nb_path\n):\nRun nbdev export in a project-scoped process without changing server cwd.\n\nsource\n\n\nexport_notebook\ndef export_notebook(\n    nb, nb_path, writer:function=write_nb\n):\nExport an nbdev notebook and stamp export metadata when possible.\n\n# Export a real nbdev notebook without changing the MCP server's cwd.\nrun_nbdev_export_from_project(\"nbs/00_foundation.ipynb\")\n\n\n\nStable ids and notebook commits\nensure_cell_ids fills in missing notebook cell ids. notebook_hash changes when a cell’s id or source changes. commit_notebook validates and records a proposed notebook change, or writes and exports it when dry_run is false.\n\ntrial = new_nb([mk_cell(\"value = 1\")])\nensure_cell_ids(trial)\ncommit_notebook(\"nbs/data/commit_notebook_example.ipynb\", trial, dry_run=True, export=False)[\"planned_hash\"] == notebook_hash(trial)\n\nnotebook_hash and commit_notebook form the shared commit primitive. Callers build a trial notebook, pass the original as before, and get read-back hashes after the write succeeds. The commit boundary rejects malformed notebook structure and duplicate cell ids before it writes. Changed code-cell outputs are cleared before the notebook is committed.\n\nsource\n\n\nparse_one_cell\ndef parse_one_cell(\n    text, default_type:str='code'\n):\nCall self as a function.\n\nsource\n\n\nfind_cell_by_text\ndef find_cell_by_text(\n    cells, old_str\n):\nCall self as a function.\n\nsource\n\n\nreplace_cell\ndef replace_cell(\n    nb, idx, new_cell\n):\nCall self as a function.\n\nsource\n\n\nclear_outputs\ndef clear_outputs(\n    cell\n):\nCall self as a function.\n\nsource\n\n\nload_cells_text\ndef load_cells_text(\n    cells:str='', cells_file:NoneType=None\n):\nReturn direct Python text or the contents of an explicit text file.\n\nsource\n\n\nparse_cells\ndef parse_cells(\n    cells, default_type:str='code'\n):\nCall self as a function.\n\n\nNaming cells by behavior\nThe reading and MCP layers present cells by meaning, not just by code or markdown. This section detects imports, private helpers, exported code, tests, examples, docs, section headers, and mixed cells so callers can filter notebooks at a useful level. A test that produces a useful output is intentionally both a test and an example; that pairing is not an unclean mixed cell.\n\nsource\n\n\nfirst_line\ndef first_line(\n    source\n):\nCall self as a function.\n\nsource\n\n\ncell_prefix\ndef cell_prefix(\n    idx, cell, show_ids:bool=False\n):\nCall self as a function.\n\nsource\n\n\nis_exported_code_cell\ndef is_exported_code_cell(\n    cell\n):\nCall self as a function.\n\nsource\n\n\noutput_value_text\ndef output_value_text(\n    value\n):\nReturn a notebook output value as plain text.\n\nsource\n\n\noutput_text\ndef output_text(\n    output\n):\nReturn a notebook output object as plain text.\n\nsource\n\n\ncell_output_text\ndef cell_output_text(\n    cell\n):\nReturn all visible outputs for cell as compact plain text.\n\nsource\n\n\ncell_semantic_warnings\ndef cell_semantic_warnings(\n    cell\n):\nReturn semantic warning codes for one cell.\n\nsource\n\n\nstamp_notebook_metadata\ndef stamp_notebook_metadata(\n    nb, exported_py_path:NoneType=None\n):\nCall self as a function.\n\nfrom fastcore.nbio import read_nb\n\nwith write_demo_notebook(\"commit_notebook_example.ipynb\", cells=[mk_cell(chr(10).join([\"value = 1\", \"print(value)\"]))]) as commit_path:\n    before_nb = read_nb(commit_path)\n    trial_nb = read_nb(commit_path)\n    print(notebook_hash(before_nb))\n    trial_nb.cells[0].source = chr(10).join([\"value = 2\", \"print(value)\"])\n    trial_nb.cells[0].outputs = [dict(output_type=\"stream\", name=\"stdout\", text=\"stale\" + chr(10))]\n    result = commit_notebook(commit_path, trial_nb, before=before_nb, affected_cell_ids=[trial_nb.cells[0].id], export=False)\n    print(result[\"changed\"], result[\"affected_cell_ids\"])\n    print(read_nb(commit_path).cells[0].outputs)\n\n35c2147e7175\nTrue ['63aa22f7']\n[]\n\n\n\nsource\n\n\ncell_class_names\ndef cell_class_names(\n    cell\n):\nCall self as a function.\n\nwith write_demo_notebook(\"00_foundation_context.ipynb\") as path:\n    assert path.exists()\n    assert is_valid_ipynb(path)\nassert not path.exists()\n\nparse_cells is the little translator between a friendly edit format and real notebook cells. The dashed line means “start a new cell”, while %%markdown and %%code make the intended cell type explicit.\n\n_demo_text = \"\\n\".join([\"%%markdown\", \"## Demo\", \"---\", \"%%code\", \"value = 42\"])\n_demo_cells = parse_cells(_demo_text)\nfor idx, cell in enumerate(_demo_cells):\n    print(cell_prefix(idx, cell))\n    print(\"  type:\", cell.cell_type)\n    print(\"  first line:\", first_line(cell_source(cell)))\n\nCell id=25790bac: markdown\n  type: markdown\n  first line: ## Demo\nCell id=38f08303: code\n  type: code\n  first line: value = 42\n\n\n\nsource\n\n\ncell_matches_type\ndef cell_matches_type(\n    cell, cell_type\n):\nCall self as a function.\n\nsource\n\n\nCell.output_text\ndef output_text():\nReturn all visible outputs for this cell as compact plain text.\n\nsource\n\n\nCell.semantic_type\ndef semantic_type():\nReturn the primary reader-facing semantic type for this cell.\n\nsource\n\n\nCell.context_record\ndef context_record():\nReturn the context record shape used by read helpers, with directives stripped.\n\n\nVisible cell output\noutput_value_text, output_text, and cell_output_text turn Jupyter output payloads into plain text. Cell.output_text, Cell.semantic_type, and Cell.context_record apply the same rules to a cell model used by readers.\n\noutput_cell = mk_cell(\"print('ok')\")\noutput_cell.outputs = [dict(output_type=\"stream\", name=\"stdout\", text=\"ok\\n\")]\noutput_model = Cell(output_cell, idx=4)\n(output_value_text([\"o\", \"k\"]), output_text(output_cell.outputs[0]), cell_output_text(output_cell), output_model.output_text(), output_model.semantic_type(), output_model.context_record()[\"cell_idx\"])\n\n\nsource\n\n\nwith_context\ndef with_context(\n    cells, items, include:bool=False\n):\nCall self as a function.\n\n\nChapters as editing scopes\nChapters are the notebook-native version of a module section. A heading cell opens a span, and everything until the next heading belongs to that span. Write, execute, and read tools use these spans so users can say “the query language chapter” instead of counting cell indexes by hand.\n\nsource\n\n\nheading_title\ndef heading_title(\n    cell, levels:tuple=(2,)\n):\nReturn the first markdown heading title whose level is allowed.\n\nsource\n\n\nchapter_index_set\ndef chapter_index_set(\n    cells, chapter\n):\nCall self as a function.\n\nwith write_demo_notebook(\"domain_model_example.ipynb\", cells=[mk_cell(\"## Alpha\", cell_type=\"markdown\"), mk_cell(\"answer = 42\")]) as model_path:\n    model_doc = Notebook.from_path(model_path)\n    model_cell = model_doc.cell(model_doc.nb.cells[1].id)\n    model_chapter = model_doc.chapter(\"Alpha\")\n    print(model_cell.source)\n    print(model_chapter.to_record())\n    print(model_doc.notebook_source_hash())\n    print(model_doc.commit(before=model_doc.nb, export=False)[\"changed\"])\n\nanswer = 42\n{'title': 'Alpha', 'start': 0, 'end': 2, 'cell_count': 2}\na584b5a3979c\nFalse\n\n\nOn a real notebook, chapter spans become a tiny table of contents with cell ranges. Here index.ipynb is being parsed by the foundation helpers that the reading tools themselves rely on.\n\n_index_cells = read_nb(\"index.ipynb\").cells\nfor span in chapter_spans(_index_cells)[:5]: print(f\"{span['title']}: cells {span['start']}..{span['end'] - 1}\")\n\nprint(\"reading indexes:\", sorted(chapter_index_set(_index_cells, \"Reading\"))[:5])\n\nThe problem this project solves: cells 2..2\nHow the notebooks fit together: cells 3..3\nProduction readiness: cells 4..5\nA tiny notebook to work on: cells 6..7\nReading: choose the smallest useful context: cells 8..9\nreading indexes: [8, 9]\n\n\n\ncustom_map = demo_path(\"00_foundation_errors.json\")\n_old_failure_map = os.environ.get(\"NBSKILL_FAILURE_MAP\")\ntry:\n    os.environ[\"NBSKILL_FAILURE_MAP\"] = str(custom_map)\n    assert failure_map_path() == custom_map\nfinally:\n    if _old_failure_map is None: os.environ.pop(\"NBSKILL_FAILURE_MAP\", None)\n    else: os.environ[\"NBSKILL_FAILURE_MAP\"] = _old_failure_map\n\n\ncells = parse_cells(\"%%markdown\\n## Demo\\n---\\n%%code\\nvalue = 42\")\nassert len(cells) == 2\nassert cells[0].cell_type == \"markdown\"\nassert cells[1].source == \"value = 42\"\none = parse_one_cell(\"%%code\\nvalue = 99\")\nassert one.cell_type == \"code\"\nassert one.source == \"value = 99\"\nencoded = \"def f():\" + chr(92) + \"n    return 1\"\nassert load_cells_text(\"%%code\\\\nvalue = 1\") == \"%%code\\\\nvalue = 1\"\nassert load_cells_text(encoded) == encoded\n\nHere are the labels on toy cells. This is deliberately small, but it is the same classification that lets the reading and review tools distinguish examples from tests without asking a model to guess.\n\npublic_cell = mk_cell(\"#| export\\ndef public():\\n    return 1\")\nprivate_cell = mk_cell(\"#| export\\ndef _private():\\n    return 1\")\nexample_cell = mk_cell(\"value = 1\")\nexample_cell.outputs = [dict(output_type=\"stream\", name=\"stdout\", text=\"1\\n\")]\nfor label, cell in {\"public\": public_cell, \"private\": private_cell, \"example\": example_cell}.items():\n    print(f\"{label:8} -&gt; {', '.join(cell_class_names(cell))}\")\n\nThe tests below define the complete semantic contract: public and private exported symbols, multicells, output-bearing tests, examples, documentation, titles, metadata refresh, and the only allowed mixed role (test_code plus example_code).\nA semantic cell gets explicit source-and-output roles. Imports distinguish exported_import_cell from internal_import_cell; a visible test is the only allowed test_code + example_code combination. multicell is reserved for a cell with multiple differently named top-level functions, public or private. Every other mixed role is a writing warning.\nMetadata stamping is separate from classification: it records the generated Python artifact and caches the semantic labels that downstream tools can reuse.\nPython API helpers return values directly and raise ValueError for invalid inputs; notebook execution stays independent of process arguments.",
    "crumbs": [
      "Foundation"
    ]
  },
  {
    "objectID": "01_read.html",
    "href": "01_read.html",
    "title": "nbskill",
    "section": "",
    "text": "Readable notebook views plus implementation-oriented context for co-creation.\nThe public read API is the unified context(target, scope=\".\"). It covers repository orientation, one notebook, one chapter, one cell id, or one concrete implementation without exposing raw notebook JSON. Its filters choose the useful slice: summary, full source, chapter, cell type, query, regular expressions, and nearby context lines.\nNotebook chapters use a code-first rhythm: exported code, trailing Markdown explanation, example cells, focused test cells, then the next exported code. The reader preserves that source order and associates a symbol with the cells that follow it.\nReading is the first problem to solve for notebook automation. An agent should not have to inspect raw .ipynb JSON just to learn what a project, notebook, chapter, or implementation contains, and a human reviewer should not have to scroll through outputs and metadata to find the code.\nThis notebook builds compact context views for four common questions: what project is this, what is in this file, what belongs to this chapter, and what should I know before changing this symbol.\nFor a chapter, read the story in this order:\n## Chapter\n- exported code\n- trailing explanation\n- example(s)\n- test(s)\n- exported code\n- trailing explanation\n- example(s)\n- test(s)\n- ...\nSymbol readers use the same rhythm: implementation first, then trailing Docs, then the following example and test cells. They stop the local group at the next exported code cell so one symbol’s explanation does not leak into the next one.\nThe reader is intentionally a triage tool before it is a renderer. Start with context(target, scope=\".\"); use target=\"project\", a notebook path/name, a chapter title, a cell id, or a Python symbol.\ncontext(\"project\", scope=\"nbs\")\ncontext(\"nbs/02_write.ipynb\")\ncontext(\"write_nb\", scope=\"nbs/02_write.ipynb\")",
    "crumbs": [
      "Reading and inspection"
    ]
  },
  {
    "objectID": "01_read.html#reading-and-inspection",
    "href": "01_read.html#reading-and-inspection",
    "title": "nbskill",
    "section": "",
    "text": "Readable notebook views plus implementation-oriented context for co-creation.\nThe public read API is the unified context(target, scope=\".\"). It covers repository orientation, one notebook, one chapter, one cell id, or one concrete implementation without exposing raw notebook JSON. Its filters choose the useful slice: summary, full source, chapter, cell type, query, regular expressions, and nearby context lines.\nNotebook chapters use a code-first rhythm: exported code, trailing Markdown explanation, example cells, focused test cells, then the next exported code. The reader preserves that source order and associates a symbol with the cells that follow it.\nReading is the first problem to solve for notebook automation. An agent should not have to inspect raw .ipynb JSON just to learn what a project, notebook, chapter, or implementation contains, and a human reviewer should not have to scroll through outputs and metadata to find the code.\nThis notebook builds compact context views for four common questions: what project is this, what is in this file, what belongs to this chapter, and what should I know before changing this symbol.\nFor a chapter, read the story in this order:\n## Chapter\n- exported code\n- trailing explanation\n- example(s)\n- test(s)\n- exported code\n- trailing explanation\n- example(s)\n- test(s)\n- ...\nSymbol readers use the same rhythm: implementation first, then trailing Docs, then the following example and test cells. They stop the local group at the next exported code cell so one symbol’s explanation does not leak into the next one.\nThe reader is intentionally a triage tool before it is a renderer. Start with context(target, scope=\".\"); use target=\"project\", a notebook path/name, a chapter title, a cell id, or a Python symbol.\ncontext(\"project\", scope=\"nbs\")\ncontext(\"nbs/02_write.ipynb\")\ncontext(\"write_nb\", scope=\"nbs/02_write.ipynb\")",
    "crumbs": [
      "Reading and inspection"
    ]
  },
  {
    "objectID": "01_read.html#reader-api",
    "href": "01_read.html#reader-api",
    "title": "nbskill",
    "section": "Reader API",
    "text": "Reader API\nStart with the unified context reader. Select the target and add only the filters needed for the agent’s next action.\n\nProduction contract\nThe unified context reader is the production entry point. It preserves stable notebook paths and cell ids, supports compact summaries and full source, and narrows the same result by chapter, cell type, query, or regular expressions. Lower-level helpers are implementation details, not additional agent-facing read surfaces.\n\nwith write_demo_notebook(\"01_read_example.ipynb\") as path:\n    write_nb(new_nb([\n        mk_cell(\"# Reader\\nFile preamble.\", cell_type=\"markdown\"),\n        mk_cell(\"preamble = True\", cell_type=\"code\"),\n        mk_cell(\"## Demo\", cell_type=\"markdown\"),\n        mk_cell(\"#| export\\ndef answer():\\n    \\\"\\\"\\\"Return the demo answer.\\\"\\\"\\\"\\n    return 42\", cell_type=\"code\"),\n        mk_cell(\"This explains why the demo answer exists.\", cell_type=\"markdown\"),\n        mk_cell(\"answer()\", cell_type=\"code\"),\n        mk_cell(\"assert answer() == 42\", cell_type=\"code\"),\n    ]), path)\n    print(\"project_context\")\n    _example_project_context(str(path))\n    print(\"\\nfile_context\")\n    _example_file_context(str(path), include_re=\"answer\")\n    print(\"\\nchapter_context\")\n    _example_chapter_context(str(path), name=\"Demo\")\n    print(\"\\ncontext symbol\")\n    _example_context(f\"{path}#answer\", scope=str(path), mode=\"review\")\n\n\n\nOutput shapes\nThe unified context reader serves the useful attention levels: view=\"summary\" maps cells compactly, view=\"full\" keeps source in order, and chapter, cell-type, query, and regex arguments narrow the same notebook-aware result. A qualified target such as notebook.ipynb#cell-id hands an agent an exact stable cell.\nUse the smallest view that answers the next question: summary to orient, a filtered full view to implement, and a qualified cell target to edit. Structured returns keep cell ids and indexes so an edit can target the exact source cell.",
    "crumbs": [
      "Reading and inspection"
    ]
  },
  {
    "objectID": "01_read.html#query-and-selection",
    "href": "01_read.html#query-and-selection",
    "title": "nbskill",
    "section": "Query and selection",
    "text": "Query and selection\nSelectors turn short human queries into stable notebook, chapter, cell, and symbol matches.\n\nA small query language\nAutomation needs stable selectors, but humans need short commands. The query helpers accept aliases like id, type, chapter, and contains, then normalize them into one internal selection shape.",
    "crumbs": [
      "Reading and inspection"
    ]
  },
  {
    "objectID": "01_read.html#public-notebook-context",
    "href": "01_read.html#public-notebook-context",
    "title": "nbskill",
    "section": "Public notebook context",
    "text": "Public notebook context\nThis is the main notebook-facing surface: it combines the smaller readers and returns a structured result while optionally printing the rendered text.\n\nThe public notebook reader\nThe public reader is context(target, scope=\".\"). The target can be project, a notebook path, a cell id, a chapter title, a Python symbol, or a literal search string when no structured target resolves. Add view, chapter, cell_type, query, include_re, or exclude_re to shape the same result.\n\nsource\n\n\nproject_context\ndef project_context(\n    path:str='.', # Project directory, notebook path, or notebook glob root\n    verbose:bool=True, # Print rendered context; pass False to only return structured data\n):\nShow README sections, notebook filenames, and notebook file docstrings.\n\nsource\n\n\nchapter_context\ndef chapter_context(\n    path:str, # Notebook path\n    query:str | None=None, # Query for any cell inside the chapter\n    name:str | None=None, # Chapter title string or regex\n    any_cell_id:str | None=None, # Any cell id inside the chapter\n    verbose:bool=True, # Print rendered context; pass False to only return structured data\n    overview:bool=False, # Show only notebook head and chapter intro markdown\n    cell_type:str | None=None, # Optional cell type or semantic type filter for deep dives\n    include_re:str | None=None, # Optional regex that matched cell source must include\n    exclude_re:str | None=None, # Optional regex that matched cell source must not include\n):\nShow one chapter in source order, with optional overview and cell filters.",
    "crumbs": [
      "Reading and inspection"
    ]
  },
  {
    "objectID": "01_read.html#python-source-context",
    "href": "01_read.html#python-source-context",
    "title": "nbskill",
    "section": "Python source context",
    "text": "Python source context\nNotebook work often crosses into generated modules. The AST readers keep that handoff compact and navigable.\nPython files get a compact AST-backed reader too. python_file_context summarizes public definitions without dumping full source, while python_symbol_context opens one symbol and shows the local callers and callees that matter for an edit.\n\nsource\n\npython_file_context\ndef python_file_context(\n    path:str, # Python file path\n    include_private:bool=False, # Include private definitions in the summary\n    verbose:bool=True, # Print rendered context; pass False to only return structured data\n):\nShow a compact AST summary for one Python file.\n\nsource\n\n\npython_symbol_context\ndef python_symbol_context(\n    path:str, # Python file path\n    symbol:str, # Function, class, method, or assignment symbol to inspect\n    verbose:bool=True, # Print rendered context; pass False to only return structured data\n):\nShow source, local callers, and callees for one Python-file symbol.\n\nwith write_demo_notebook(\"01_read_python_context_example.py\") as path:\n    path.write_text(\n        \"PUBLIC_VALUE = 1\\n\\n\"\n        \"def helper(value):\\n\"\n        \"    \\\"\\\"\\\"Prepare a value.\\n\\n    Hidden detail.\\\"\\\"\\\"\\n\"\n        \"    return value + PUBLIC_VALUE\\n\\n\"\n        \"def add(a, b):\\n\"\n        \"    \\\"\\\"\\\"Add two values.\\n\\n    Hidden detail.\\\"\\\"\\\"\\n\"\n        \"    return helper(a) + b\\n\\n\"\n        \"def uses_add(value):\\n\"\n        \"    return add(value, 1)\\n\",\n        encoding=\"utf-8\",\n    )\n    py_file = python_file_context(str(path), verbose=False)\n    py_symbol = python_symbol_context(str(path), \"add\", verbose=False)\npy_file[\"public_symbols\"][0][\"symbol\"], py_file[\"definitions\"][1][\"docstring\"], py_symbol[\"callers\"][0][\"symbol\"], py_symbol[\"callees\"]\n\n\nsource\n\n\nfile_context\ndef file_context(\n    path:str, # Notebook or Python file path\n    include_re:str | None=None, # Optional regex for markdown and definition items to include\n    exclude_re:str | None=None, # Optional regex for markdown and definition items to exclude\n    verbose:bool=True, # Print rendered context; pass False to only return structured data\n):\nShow compact context for one notebook or Python file; notebook cells stay in source order.\nFiltered context is the project-wide escape hatch for questions that are too broad for one target but still need notebook-aware cells instead of raw JSON. It searches notebooks in scope, keeps stable cell ids in the result, and caps both the number of matches and each source snippet so agents can ask broad questions without flooding their context.\ncontext combines discovery and reading. Use context(path, view=\"summary\") for one compact row per cell, cell_type=\"markdown\" or cell_type=\"code\" for role-specific slices, chapter=\"Name\" for one section, and query=\"regex=term\" or include_re=\"term\" for targeted search. context_lines=1 adds neighboring cell summaries without opening the whole notebook.\n\ndef _write_read_sample_notebook(path):\n    double_example = mk_cell(\"double(3)\", cell_type=\"code\")\n    double_example.outputs = [{\n        \"output_type\": \"execute_result\",\n        \"execution_count\": 1,\n        \"metadata\": {},\n        \"data\": {\"text/plain\": \"6\"},\n    }]\n    add_example = mk_cell(\"add(2, 3)\", cell_type=\"code\")\n    add_example.outputs = [{\n        \"output_type\": \"execute_result\",\n        \"execution_count\": 1,\n        \"metadata\": {},\n        \"data\": {\"text/plain\": \"5\"},\n    }]\n    calculator_example = mk_cell(\"Calculator(10).total(2)\", cell_type=\"code\")\n    calculator_example.outputs = [{\n        \"output_type\": \"execute_result\",\n        \"execution_count\": 1,\n        \"metadata\": {},\n        \"data\": {\"text/plain\": \"22\"},\n    }]\n    error_cell = mk_cell(\"raise RuntimeError('boom')\", cell_type=\"code\")\n    error_cell.outputs = [{\n        \"output_type\": \"error\",\n        \"ename\": \"RuntimeError\",\n        \"evalue\": \"boom\",\n        \"traceback\": [\"RuntimeError: boom\"],\n    }]\n    nb = new_nb([\n        mk_cell(\"# Sample tool\\nNotebook-level note.\", cell_type=\"markdown\"),\n        mk_cell(\"More file-level context.\", cell_type=\"markdown\"),\n        mk_cell(\"import math\", cell_type=\"code\"),\n        mk_cell(\"## Math\\nThis chapter demonstrates code-first groups.\", cell_type=\"markdown\"),\n        mk_cell(\n            \"#| export\\ndef double(value):\\n\"\n            \"    \\\"\\\"\\\"Double a value.\\\"\\\"\\\"\\n\"\n            \"    return value * 2\",\n            cell_type=\"code\",\n        ),\n        mk_cell(\"Double a value by multiplying it by two.\", cell_type=\"markdown\"),\n        double_example,\n        mk_cell(\"assert double(3) == 6\", cell_type=\"code\"),\n        mk_cell(\n            \"#| export\\ndef add(a, b):\\n\"\n            \"    \\\"\\\"\\\"Add values.\\\"\\\"\\\"\\n\"\n            \"    return double(a) + b\",\n            cell_type=\"code\",\n        ),\n        mk_cell(\"Add the doubled first value to the second.\", cell_type=\"markdown\"),\n        add_example,\n        mk_cell(\"assert add(1, 2) == 4\", cell_type=\"code\"),\n        mk_cell(\n            \"#| export\\nclass Calculator:\\n\"\n            \"    \\\"\\\"\\\"Calculate values.\\\"\\\"\\\"\\n\"\n            \"    def __init__(self, base):\\n\"\n            \"        \\\"\\\"\\\"Store the base value.\\\"\\\"\\\"\\n\"\n            \"        self.base = base\\n\\n\"\n            \"    def total(self, value):\\n\"\n            \"        \\\"\\\"\\\"Add value to the base.\\\"\\\"\\\"\\n\"\n            \"        return add(self.base, value)\",\n            cell_type=\"code\",\n        ),\n        mk_cell(\"Calculator keeps a base and adds a value.\", cell_type=\"markdown\"),\n        calculator_example,\n        mk_cell(\"assert Calculator(10).total(2) == 22\", cell_type=\"code\"),\n        mk_cell(\"### Detail\\nNested rationale mentioning add.\", cell_type=\"markdown\"),\n        mk_cell(\"detail_value = add(2, 4)\", cell_type=\"code\"),\n        error_cell,\n    ])\n    write_nb(nb, path)\n    return nb\n\n\nwith write_demo_notebook(\"01_read_filter_context_example.ipynb\") as path:\n    nb = new_nb([mk_cell(\"detail_value = 6\"), mk_cell(\"other_value = 1\")])\n    write_nb(nb, path)\n    filtered = _context_filtered(str(path), query=\"type=code regex=detail_value\", verbose=False)\nfiltered[\"total_matches\"], filtered[\"matches\"][0][\"cell_id\"]\n\nUse view=\"cell\" when the selected source needs stable line numbers. Queries such as errors, export, headers, chapter=Name, and regex=term retain the existing notebook selectors while keeping the public surface on context.",
    "crumbs": [
      "Reading and inspection"
    ]
  },
  {
    "objectID": "01_read.html#exact-cell-context",
    "href": "01_read.html#exact-cell-context",
    "title": "nbskill",
    "section": "Exact cell context",
    "text": "Exact cell context\nUse context(\"notebook.ipynb#cell-id\", mode=\"exact\") when an edit needs the complete selected cell without docs, callers, or truncation.\n\nwith write_demo_notebook(\"01_read_cell_search_examples.ipynb\") as path:\n    nb = _write_read_sample_notebook(path)\n    summary = _context_filtered(str(path), view=\"summary\", verbose=False)\n    numbered = _context_filtered(str(path), query=f\"id={nb.cells[8].id}\", view=\"cell\", verbose=False)\n    errors = _context_filtered(str(path), query=\"errors\", verbose=False)\n    exports = _context_filtered(str(path), query=\"export\", view=\"summary\", verbose=False)\n    outline = _context_filtered(str(path), query=\"headers\", view=\"summary\", verbose=False)\n    section = _context_filtered(str(path), query=\"chapter=Detail\", verbose=False)\n    grep = _context_filtered(str(path), query=\"regex=detail_value\", view=\"summary\", before=1, after=1, verbose=False)\nsummary[\"matches\"][0][\"summary\"], numbered[\"text\"].splitlines()[10], errors[\"matches\"][0][\"error\"], len(exports[\"matches\"]), len(outline[\"matches\"]), section[\"total_matches\"], grep[\"matches\"][0][\"before\"][0][\"cell_id\"]\n\n\nReading through the model\nNotebook.context and Notebook.symbol remain convenience methods, while the module-level context function is the canonical reader and filter surface.\n\nsource\n\n\nNotebook.context\ndef context(\n    **kw\n):\nRead this notebook through the canonical context reader.\n\nsource\n\n\nNotebook.symbol\ndef symbol(\n    symbol, **kw\n):\nRead implementation context through the canonical context reader.\n\nsource\n\n\ncontext\ndef context(\n    target:str='project', scope:str='.', targets:list[str] | None=None, mode:str='auto', around:int=0,\n    view:str | None=None, query:NoneType=None, include_re:str | None=None, exclude_re:str | None=None,\n    max_matches:int=50, max_chars_per_cell:int=1200, context_lines:int=0, chapter:str | None=None,\n    cell_type:str | None=None, include_output:bool=False, line_numbers:bool=False, verbose:bool=True\n):\nRead one notebook-aware target, optionally narrowed by notebook filters.\n\nwith write_demo_notebook(\n    \"01_read_context_filters.ipynb\",\n    cells=[\n        mk_cell(\"# Notebook\", cell_type=\"markdown\"),\n        mk_cell(\"## Addition\", cell_type=\"markdown\"),\n        mk_cell(\"#| export\\ndef add(a, b):\\n    return a + b\"),\n        mk_cell(\"Add two values.\", cell_type=\"markdown\"),\n        mk_cell(\"add(1, 2)\"),\n    ],\n) as path:\n    summary = context(str(path), scope=str(path), view=\"summary\", verbose=False)\n    markdown = context(str(path), scope=str(path), view=\"full\", cell_type=\"markdown\", verbose=False)\n    code = context(str(path), scope=str(path), view=\"full\", cell_type=\"code\", verbose=False)\n    chapter = context(str(path), scope=str(path), view=\"full\", chapter=\"Addition\", verbose=False)\n\nassert summary[\"selection\"][\"view\"] == \"summary\"\nassert len(summary[\"selection\"][\"matches\"]) == 5\nassert all(item[\"cell_type\"] == \"markdown\" for item in markdown[\"selection\"][\"matches\"])\nassert all(item[\"cell_type\"] == \"code\" for item in code[\"selection\"][\"matches\"])\nassert all(item[\"cell_type\"] in {\"markdown\", \"code\"} for item in chapter[\"selection\"][\"matches\"])\nassert any(\"def add\" in item[\"source\"] for item in code[\"selection\"][\"matches\"])\n\n\n\nContext modes and edit handoff\nUse mode=\"auto\", \"overview\", \"edit\", or \"review\"; use resolution to choose summary, implementation, or full depth. Overview returns notebook identity, headings, counts, and a next action.\nmode=\"edit\" is the bounded handoff for a named symbol: the exact source and expected_hash, followed by its trailing Docs, local examples/tests, and direct caller/callee metadata. These sections are tied to the resolved symbol, so an agent can prepare an edit without widening the search. Use around only when physical neighbor cells are also needed.\n\nwith write_demo_notebook(\n    \"01_read_context_example.ipynb\",\n    cells=[\n        mk_cell(\"#| export\\ndef add(a, b):\\n    return a + b\"),\n        mk_cell(\"Add two values.\", cell_type=\"markdown\"),\n        mk_cell(\"add(2, 3)\"),\n    ],\n) as path:\n    result = context(f\"{path}#add\", scope=str(path), mode=\"edit\")\n    selection = result[\"selection\"]\n    print(selection[\"cell_id\"], selection[\"expected_hash\"])\n    print(selection[\"docs\"][0][\"source\"], selection[\"examples\"][0][\"source\"])\n\n\nA compact context result\nThe public reader returns structured data and can also render the first useful line for a human.\n\nwith write_demo_notebook(\"01_read_output.ipynb\") as path:\n    result = context(\"project\", scope=str(path))\n    print(result[\"kind\"])\n    print(result[\"text\"].splitlines()[0])\n\ncontext keeps the common call useful for implementation work. For example, context(\"add\", scope=\"nbs/01_read.ipynb\") resolves the symbol and prints the implementation, trailing Docs, examples/tests, callers, and callees; mode=\"overview\" keeps only the implementation source. A notebook target such as context(\"nbs/01_read.ipynb\") prints every cell in source order, while mode=\"overview\" keeps the main notebook description, chapter headings, and function signatures.\n\ncontext(\"add\", scope=\"nbs/01_read.ipynb\")\n\n\n\n\nSymbol documentation\ncontext(path + \"#\" + symbol, mode=\"review\") answers the symbol-first question: “what should I know before changing this implementation?” It returns the implementation, nearby Docs and examples/tests, plus caller and callee summaries in one structured result.",
    "crumbs": [
      "Reading and inspection"
    ]
  },
  {
    "objectID": "07_mcp.html",
    "href": "07_mcp.html",
    "title": "nbskill",
    "section": "",
    "text": "Expose nbskill notebook operations as native MCP tools. This is the preferred integration for careful single-notebook reads and edits because multiline notebook cells travel as structured tool arguments rather than shell-quoted strings.\nThe server now favors verifiable context: batch edits include read-back hashes, symbol graph calls include complete structured usage data, and generated-file warnings are reserved for changes that touch exported notebook code.\nThe command-line functions are useful on their own, but coding agents work best when the same operations are available as structured tools. This notebook exposes the project through a FastMCP server while keeping the server layer thin and predictable.\n\n\nThe MCP server exposes the production core through stable structured tools. Tool schemas exclude uncommon Python-only operations, responses include concise text plus structured content, diagnostics are scoped to touched notebooks when possible, edit tools report feedback without raw notebook JSON, and experimental tools are omitted from default production use.\nThe MCP server should stay boring on purpose. Each tool accepts structured arguments, captures printed output, uses notebook locks where file operations can collide, and delegates the actual work to the same functions tested elsewhere.\n\ndef demo_tool():\n    print(\"captured output\")\n\n\n\nThe first example uses the imported public surface so readers can see the text returned by capture_call and the server type without starting a transport.\n\n\n\n\nFastMCP 3 reload executes the selected file as an un-packaged module. nbskill.mcp therefore declares its package when needed before its relative imports run; regular package imports already have the correct package and are unchanged.\n\n\n\nThe MCP tools should return text, not leak stdout and stderr into the server process. These helpers capture each underlying function call and convert its visible result into one response string.\n\n\nas_text keeps every tool wrapper on one response contract. It turns an absent result into empty output while leaving ordinary values readable.\n\nsource\n\n\n\n\ndef as_text(\n    value\n):\nReturn an empty string for None, otherwise convert value to text.\n\nas_text(None), as_text(42)\n\n\n\ncapture_call captures the output a command would have printed. This keeps MCP responses useful without letting a tool write into the server transport.\n\nsource\n\n\n\n\ndef capture_call(\n    func, **kwargs\n):\nRun func and return its captured visible output.\n\ncapture_call(lambda: \"ready\")\n\n\nsource\n\n\n\ndef capture_notebook_call(\n    func, *paths, **kwargs\n):\nCapture a call while holding per-notebook locks for paths.\n\n\n\nedit_notebook_tool and exec_nb_tool run notebook mutation and execution in importable multiprocessing workers instead of doing the work directly in MCP worker threads. This is intentional. If an MCP client times out, cancelling an asyncio.to_thread wait does not stop the underlying Python thread; the old work can keep exporting notebooks and consuming memory after the client has already given up.\nA process gives the server a boundary it can terminate. The worker target is a normal module function, not a string passed to python -c, so the code is readable, importable under spawn, and covered by notebook tests. On POSIX the worker starts a process group with setsid, which lets timeout cleanup terminate nested export work too.\nEach child owns the notebook locks for its actual transaction. The parent must not retain a process-scoped notebook lock while waiting for that child: the worker would block on the parent and both sides would wait until timeout. edit_notebook_tool caps tool_timeout at 110 seconds so it can return a structured timeout before clients with a 120 second transport deadline give up.",
    "crumbs": [
      "MCP server"
    ]
  },
  {
    "objectID": "07_mcp.html#mcp-server",
    "href": "07_mcp.html#mcp-server",
    "title": "nbskill",
    "section": "",
    "text": "Expose nbskill notebook operations as native MCP tools. This is the preferred integration for careful single-notebook reads and edits because multiline notebook cells travel as structured tool arguments rather than shell-quoted strings.\nThe server now favors verifiable context: batch edits include read-back hashes, symbol graph calls include complete structured usage data, and generated-file warnings are reserved for changes that touch exported notebook code.\nThe command-line functions are useful on their own, but coding agents work best when the same operations are available as structured tools. This notebook exposes the project through a FastMCP server while keeping the server layer thin and predictable.\n\n\nThe MCP server exposes the production core through stable structured tools. Tool schemas exclude uncommon Python-only operations, responses include concise text plus structured content, diagnostics are scoped to touched notebooks when possible, edit tools report feedback without raw notebook JSON, and experimental tools are omitted from default production use.\nThe MCP server should stay boring on purpose. Each tool accepts structured arguments, captures printed output, uses notebook locks where file operations can collide, and delegates the actual work to the same functions tested elsewhere.\n\ndef demo_tool():\n    print(\"captured output\")\n\n\n\nThe first example uses the imported public surface so readers can see the text returned by capture_call and the server type without starting a transport.\n\n\n\n\nFastMCP 3 reload executes the selected file as an un-packaged module. nbskill.mcp therefore declares its package when needed before its relative imports run; regular package imports already have the correct package and are unchanged.\n\n\n\nThe MCP tools should return text, not leak stdout and stderr into the server process. These helpers capture each underlying function call and convert its visible result into one response string.\n\n\nas_text keeps every tool wrapper on one response contract. It turns an absent result into empty output while leaving ordinary values readable.\n\nsource\n\n\n\n\ndef as_text(\n    value\n):\nReturn an empty string for None, otherwise convert value to text.\n\nas_text(None), as_text(42)\n\n\n\ncapture_call captures the output a command would have printed. This keeps MCP responses useful without letting a tool write into the server transport.\n\nsource\n\n\n\n\ndef capture_call(\n    func, **kwargs\n):\nRun func and return its captured visible output.\n\ncapture_call(lambda: \"ready\")\n\n\nsource\n\n\n\ndef capture_notebook_call(\n    func, *paths, **kwargs\n):\nCapture a call while holding per-notebook locks for paths.\n\n\n\nedit_notebook_tool and exec_nb_tool run notebook mutation and execution in importable multiprocessing workers instead of doing the work directly in MCP worker threads. This is intentional. If an MCP client times out, cancelling an asyncio.to_thread wait does not stop the underlying Python thread; the old work can keep exporting notebooks and consuming memory after the client has already given up.\nA process gives the server a boundary it can terminate. The worker target is a normal module function, not a string passed to python -c, so the code is readable, importable under spawn, and covered by notebook tests. On POSIX the worker starts a process group with setsid, which lets timeout cleanup terminate nested export work too.\nEach child owns the notebook locks for its actual transaction. The parent must not retain a process-scoped notebook lock while waiting for that child: the worker would block on the parent and both sides would wait until timeout. edit_notebook_tool caps tool_timeout at 110 seconds so it can return a structured timeout before clients with a 120 second transport deadline give up.",
    "crumbs": [
      "MCP server"
    ]
  },
  {
    "objectID": "07_mcp.html#diagnostics-and-log-reports",
    "href": "07_mcp.html#diagnostics-and-log-reports",
    "title": "nbskill",
    "section": "Diagnostics and log reports",
    "text": "Diagnostics and log reports\nThe server records enough information to diagnose failures after a client disconnects. Log parsing stays separate from transport middleware so reports can be tested with small JSONL fixtures.\nMCP postmortems need every tool call to leave enough evidence to answer three questions later: did it finish, how long did it take, and how much output did it produce. The middleware logs these fields at the server boundary so individual tools do not each need bespoke timing code.\nThe log reporter intentionally accepts older log files. Older rows do not have call ids or output lengths, so the report pairs what it can and calls out missing evidence as a problem instead of pretending the history is cleaner than it is.\n\nReport a log window\nmcp_log_report pairs start and end events, then groups incomplete or malformed records into actionable problems. A missing path is a useful small example because it exercises the same report shape without needing a running server.\n\nsource\n\n\nmcp_log_report\ndef mcp_log_report(\n    path:NoneType=None, limit:int=20, until_line:NoneType=None, until_ts:NoneType=None\n):\nSummarize nbskill MCP JSONL logs with metrics and actionable problems.\n\nRender a concise report\nformat_mcp_log_report turns the structured diagnostics into terminal-friendly text, so operators can inspect a log window without parsing JSON.\n\nsource\n\n\n\nformat_mcp_log_report\ndef format_mcp_log_report(\n    report, limit:int=20, problems_only:bool=False\n):\nRender mcp_log_report as a concise terminal-friendly summary.\n\nformat_mcp_log_report(mcp_log_report(None))\n\n\nProject-scoped failure history\nThe failure map is shared by every local project. Doctor only reports failures whose recorded working directory or target path belongs to the project under review.\n_MCP_READ_CONTEXT_TOOL_CATALOG = { ‘context’: { ‘feature’: ‘read_context’, ‘usefulness’: ‘core’, ‘tags’: (‘read’, ‘context’, ‘project’, ‘notebook’, ‘cell’, ‘symbol’, ‘search’, ‘filter’, ‘graph’), ‘description’: ‘Read and filter one notebook-aware context surface: project, notebook, chapter, cell, symbol, or matching cells.’, ‘when_to_use’: “Use first. Choose view=‘summary’ for orientation or view=‘full’ for source; add chapter, cell_type, query, include_re, or exclude_re to narrow the same result.”, ‘combine_with’: “Use a qualified target or stable cell id with context before edit_notebook; use include_graph=True only for project placement or reuse.”, }, }\n\nsource\n\n\n\nmcp_tool_result\ndef mcp_tool_result(\n    tool, arguments, full_output, max_output_chars:int=12000, detail:str='summary', warnings:NoneType=None,\n    hints:NoneType=None, status:str='completed', **structured\n):\nReturn one visible MCP summary and compact structured data.\n\n\nCompact MCP results\nNormal calls place one human summary in visible MCP text and place each structured fact once. detail=\"debug\" adds raw diagnostics and arguments for troubleshooting. Failures state the type, cause, and next action without returning a Python traceback.\n\nnormal = mcp_tool_result(\"demo\", {}, \"stored only in debug\")\ndebug = mcp_tool_result(\"demo\", {}, \"stored only in debug\", detail=\"debug\")\nprint(normal.content[0].text)\nprint(debug.structured_content[\"debug\"][\"raw_output\"])\n\n\nsource\n\n\nnbskill_status\ndef nbskill_status():\nReport nbskill version, MCP command setup, and reconnect hints.\n\nnbskill_status()[\"version\"]\n\n\n\nRegistering notebook tools\nThe wrapper should preserve useful structure, not flatten everything into text. context uses overview for compact versus focused detail, and project-level calls can attach the notebook knowledge graph with include_graph=True. Cell or symbol targets include symbol graph payloads so MCP clients can inspect impact without parsing prose.\nEdit and execution wrappers also adapt to MCP-specific transport. update_cell accepts new_lines so exact source can be sent directly without temporary files, and unsafe exec_nb runs in an isolated process so the MCP server can terminate runaway execution without blocking worker threads.\n\n\nSmall MCP surface\nThe public MCP server exposes nine tools for the normal notebook workflow: context, edit_notebook, exec_nb, verify_change, doctor, diff_nb, create_notebook, reference, and healthcheck.\ncontext is the only read surface: start with view=\"summary\", then narrow by chapter, cell_type, query, include_re, exclude_re, or context_lines. Use doctor(scopes=\"style\") for style diagnostics; there is no separate style tool.\nedit_notebook supports replace_cell, insert_cells, delete_cells, move_cells, explode_cells, replace_lines, insert_lines, delete_lines, replace_text, and replace_texts. Existing cells use cell_id, cell_ids, or target=\"all\"; whole-cell payloads use source or source_lines, structural edits use cells plus anchor_id/where, and text edits are literal. Add expected_hash when stale context must fail, and use dry_run=True to preview.\n\nsource\n\n\ndoctor_tool\nasync def doctor_tool(\n    path:str='.', scopes:str='error,warning',\n    ctx:Context=&lt;fastmcp.server.dependencies._OptionalCurrentContext object at 0x7f2c107ef3b0&gt;\n)-&gt;ToolResult:\nReport notebook errors and warnings for one path.\nThe context MCP wrapper stays easier to review when argument normalization, the core read call, and optional graph attachment are separate steps. The public tool still exposes the same schema and response shape.\n\n\nLayered context output\ncontext now chooses a useful resolution before considering truncation. The default gives a compact identity and implementation outline; callers explicitly request source or the complete raw context when they need it.\n\nsource\n\n\nedit_notebook_tool\nasync def edit_notebook_tool(\n    path:str, edits:list, dry_run:bool=False,\n    ctx:Context=&lt;fastmcp.server.dependencies._OptionalCurrentContext object at 0x7f2c106787d0&gt;\n)-&gt;ToolResult:\nApply deterministic notebook edits atomically after context identifies target cells.\n\nsource\n\n\ncreate_notebook_tool\nasync def create_notebook_tool(\n    path:str, name:str | None=None, template:str='nbdev', default_exp:str | None=None, dry_run:bool=False,\n    ctx:Context=&lt;fastmcp.server.dependencies._OptionalCurrentContext object at 0x7f2c1067aa50&gt;\n)-&gt;ToolResult:\nCall self as a function.\n\nawait edit_notebook_tool(\n    path=\"nbs/example.ipynb\",\n    edits=[dict(op=\"replace_text\", target=\"all\", old=\"old\", new=\"new\")],\n    dry_run=True,\n)\n\nexec_nb_tool always uses safe execution and accepts no caller-supplied bypasses. If a new cell is blocked, nbskill asks the connected MCP client to approve its exact source hash, then makes one internal retry. A client without approval prompts receives a refusal.\n\nsource\n\n\nexec_nb_tool\nasync def exec_nb_tool(\n    path:str, up2id:int | str | None=None, chapter:str | None=None, timeout:int=30, show_output:bool=True,\n    check_only:bool=False, tool_timeout:float=150.0, detail:str='summary',\n    ctx:Context=&lt;fastmcp.server.dependencies._OptionalCurrentContext object at 0x7f2bf5804920&gt;\n)-&gt;ToolResult:\nExecute a notebook and return visible outputs/errors. Use check_only=True to avoid writing outputs.\n\nwith write_tool_notebook(\"07_mcp_tool.ipynb\") as path:\n    await exec_nb_tool(str(path), check_only=True)\n\n\n\nVerify explicitly affected notebooks\nverify_change accepts only named notebooks. It reviews each diff, runs a safe in-memory check through the last changed code cell unless an up2id override is supplied, and scopes doctor to that notebook. The reports stay separate so one failure does not hide the results for the others.\n\nsource\n\n\nverify_change_tool\nasync def verify_change_tool(\n    paths:list, # Explicit affected notebook paths\n    up2ids:dict[str, int | str] | None=None, # Optional execution targets keyed by path\n    ref_a:str | None='HEAD', # First diff ref\n    ref_b:str | None=None, # Second diff ref\n    timeout:int=30, # Per-cell execution timeout\n    tool_timeout:float=150.0, # Per-notebook MCP execution timeout\n    scopes:str='error,warning', # Doctor scopes\n    detail:str='summary', # MCP response detail\n    ctx:Context=&lt;fastmcp.server.dependencies._OptionalCurrentContext object at 0x7f2c10303bf0&gt;\n)-&gt;ToolResult:\nVerify named notebooks with diff_nb, check-only execution, and doctor.\nverify_change_tool keeps the notebook boundaries visible. Supply up2ids when a change needs a narrower execution target than the last changed code cell.",
    "crumbs": [
      "MCP server"
    ]
  },
  {
    "objectID": "07_mcp.html#server-lifecycle",
    "href": "07_mcp.html#server-lifecycle",
    "title": "nbskill",
    "section": "Server lifecycle",
    "text": "Server lifecycle\nTool registration is pure construction. Starting the server and enabling FastMCP reload are separate so notebook examples can inspect the public surface without opening a long-running transport.\n\nRunning the server\nThe MCP entry point chooses the transport and starts FastMCP.\n\nsource\n\n\nmain\ndef main():\nRun the public nbskill MCP server over stdio.\n\n\nMCP transport-friendly edits\nMCP clients send exact source directly. source_lines avoids JSON-string plans and temporary files, which is useful when the source being written contains escaped notebook text. Unsafe notebook execution remains isolated so timeouts can terminate runaway work.\n\nmcp_tools = {tool.name: tool for tool in await mcp.list_tools()}\nassert not {\"convert\", \"problem_memory\", \"visible_text_inventory\"} & set(mcp_tools)\nedit_parameters = str(mcp_tools[\"edit_notebook\"].parameters)\nassert \"edits\" in edit_parameters and \"dry_run\" in edit_parameters\nassert \"feedback\" not in edit_parameters and \"default_cell_type\" not in edit_parameters\ncontext_parameters = str(mcp_tools[\"context\"].parameters)\nassert all(name in context_parameters for name in (\"target\", \"scope\", \"view\", \"chapter\", \"cell_type\", \"query\", \"include_re\", \"exclude_re\", \"context_lines\", \"max_matches\", \"max_chars_per_cell\", \"include_output\", \"line_numbers\", \"include_graph\"))\nassert not any(name in context_parameters for name in (\"targets\", \"mode\", \"around\"))\nassert \"filter_context\" not in mcp_tools\nreference_parameters = str(mcp_tools[\"reference\"].parameters)\nassert \"query\" in reference_parameters and \"top_k\" in reference_parameters and \"action\" not in reference_parameters\ndoctor_parameters = str(mcp_tools[\"doctor\"].parameters)\nassert \"scopes\" in doctor_parameters and \"detail\" not in doctor_parameters and \"fix\" not in doctor_parameters\nassert \"style_check\" not in mcp_tools\ndiff_parameters = str(mcp_tools[\"diff_nb\"].parameters)\nassert \"max_output_chars\" in diff_parameters\nassert \"edit_cell\" not in mcp_tools\nprint(\"direct MCP schema exposes one context reader and doctor-owned diagnostics\")\n\n\nwith write_demo_notebook(\"07_mcp_insert_cells.ipynb\") as edit_nb:\n    edit_nb = edit_nb.resolve()\n    nb = new_nb([mk_cell('payload = \"old\"')])\n    anchor_id = nb.cells[0].id\n    _write_raw_nb(nb, edit_nb)\n    result = await mcp.call_tool(\"edit_notebook\", {\"path\": str(edit_nb), \"edits\": [dict(op=\"insert_cells\", anchor_id=anchor_id, cells=[dict(source_lines=[\"inserted = True\"])])]})\n    assert result.structured_content[\"changed\"] is True\n    assert _read_raw_nb(edit_nb).cells[1].source == \"inserted = True\"\n    print(\"direct MCP edit_notebook inserts structured cells\")\n\n\nwith write_demo_notebook(\"07_mcp_edit_notebook.ipynb\") as edit_nb:\n    edit_nb = edit_nb.resolve()\n    nb = new_nb([mk_cell('payload = \"old\"\\nother = 1')])\n    edit_cell_id = nb.cells[0].id\n    _write_raw_nb(nb, edit_nb)\n    exact_source = 'payload = \"line 1' + chr(92) + 'nline 2\"'\n    edit_result = await mcp.call_tool(\"edit_notebook\", {\"path\": str(edit_nb), \"edits\": [dict(op=\"replace_cell\", cell_id=edit_cell_id, source_lines=[exact_source])]})\n    assert edit_result.structured_content[\"changed\"] is True\n    assert _read_raw_nb(edit_nb).cells[0].source == exact_source\n    range_result = await mcp.call_tool(\"edit_notebook\", {\"path\": str(edit_nb), \"edits\": [dict(op=\"replace_lines\", cell_id=edit_cell_id, start_line=1, end_line=1, replacement_lines=['payload = \"range\"'])]})\n    assert range_result.structured_content[\"changed\"] is True\n    assert _read_raw_nb(edit_nb).cells[0].source == 'payload = \"range\"'\n    feedback_result = await mcp.call_tool(\"edit_notebook\", {\"path\": str(edit_nb), \"edits\": [dict(op=\"replace_cell\", cell_id=edit_cell_id, source_lines=[\"print('mcp feedback')\"])]})\n    assert feedback_result.structured_content[\"changed\"] is True\n    assert _read_raw_nb(edit_nb).cells[0].outputs == []\n    print(\"direct MCP edit_notebook preserves escaped newlines\")\n\n\nwith write_demo_notebook(\"07_mcp_exec_nb.ipynb\") as exec_nb_path:\n    nb = new_nb([mk_cell(\"print('mcp exec')\")])\n    nb.cells[0].execution_count = 1\n    _write_raw_nb(nb, exec_nb_path)\n    result = await mcp.call_tool(\"exec_nb\", {\"path\": str(exec_nb_path.resolve()), \"timeout\": 5, \"detail\": \"debug\"})\n    assert \"mcp exec\" in result.content[0].text\n    print(\"MCP exec_nb ran with focused defaults\")\n\n\nassert mcp is not None\n\n\ntools = {tool.name: tool for tool in await mcp.list_tools()}\n\n\nexpected_tools = {\n    \"context\", \"create_notebook\", \"diff_nb\", \"doctor\", \"edit_notebook\",\n    \"exec_nb\", \"healthcheck\", \"reference\", \"verify_change\",\n}\nassert set(tools) == expected_tools\n\n\nassert \"update_cell\" not in tools\n\n\nassert \"batch_edit_nb\" not in tools\n\n\nassert \"problem_memory\" not in tools\nassert not {\n    \"project_context\", \"file_context\", \"chapter_context\", \"symbol_context\",\n    \"symbol_graph\", \"private_symbol_report\",\n    \"py2nb\", \"py2nbs\", \"py2nbdev\", \"new_nbdev_notebook\",\n    \"store_knowledge\", \"add_behaviour_steering\", \"get_knowledge\",\n    \"reference_add\", \"reference_list\", \"reference_ingest\", \"reference_query\",\n} & set(tools)\n\n\nassert \"read_nb\" not in tools\n\n\ncontext_parameters = str(tools[\"context\"].parameters)\nassert all(name in context_parameters for name in (\"target\", \"scope\", \"view\", \"include_graph\"))\nassert not any(name in context_parameters for name in (\"detail\", \"verbose\"))\n\n\nassert \"show_owner\" in str(tools[\"diff_nb\"].parameters)\n\n\nassert \"cell_id\" in str(tools[\"diff_nb\"].parameters)\n\n\nassert \"after_id\" in str(tools[\"diff_nb\"].parameters)\n\n\nexec_params = str(tools[\"exec_nb\"].parameters)\nassert \"check_only\" in exec_params\nassert all(name not in exec_params for name in (\"allow_new\", \"safe\", \"allow\", \"ok_dests\"))\n\n\nassert \"edits\" in str(tools[\"edit_notebook\"].parameters)\n\n\nassert \"feedback_safe\" not in str(tools[\"edit_notebook\"].parameters)\n\n\nedit_meta = _MCP_CELL_EDIT_TOOL_CATALOG[\"edit_notebook\"]\nassert \"edits\" in str(tools[\"edit_notebook\"].parameters)\nassert all(op in edit_meta[\"description\"] for op in (\n    \"replace_cell\", \"insert_cells\", \"delete_cells\", \"move_cells\", \"explode_cells\",\n    \"replace_lines\", \"insert_lines\", \"delete_lines\", \"replace_text\", \"replace_texts\",\n))\n\n\nassert \"scopes\" in str(tools[\"doctor\"].parameters)\n\n\nassert \"include_graph\" in str(tools[\"context\"].parameters)\nassert \"action\" not in str(tools[\"reference\"].parameters)\n\n\nassert \"dry_run\" in str(tools[\"edit_notebook\"].parameters)\nassert \"dry_run\" not in str(tools[\"reference\"].parameters)\n\n\nout = StringIO()\nwith _demo_root(\"07_mcp_context_graph\") as graph_root:\n    lib_nb = graph_root / \"lib.ipynb\"\n    call_nb = graph_root / \"call.ipynb\"\n    _write_raw_nb(new_nb([mk_cell(\"#| export\\ndef thing():\\n    return 1\")]), lib_nb)\n    _write_raw_nb(new_nb([mk_cell(\"from nbskill.lib import thing\\nvalue = thing()\")]), call_nb)\n    context_args = dict(target=\"thing\", scope=str(graph_root))\n    with redirect_stdout(out): context_result = await mcp.call_tool(\"context\", context_args)\n    graph_payload = context_result.structured_content[\"context\"][\"symbol_graphs\"][0]\n    assert graph_payload[\"caller_usages\"][0][\"line\"] == \"value = thing()\"\n    assert out.getvalue() == \"\"\n    print(\"structured context symbol usages:\", len(graph_payload[\"caller_usages\"]))\n\n\nasync def _exercise_mcp_edit_tools(edit_root):\n    edit_root.mkdir()\n    edit_nb = (edit_root / \"edit.ipynb\").resolve()\n    nb = new_nb([mk_cell('payload = \"old\"\\nvalue = 1')])\n    cell_id = nb.cells[0].id\n    _write_raw_nb(nb, edit_nb)\n    exact_source = 'payload = \"line 1' + chr(92) + 'nline 2\"'\n    dry_result = await mcp.call_tool(\"edit_notebook\", {\"path\": str(edit_nb), \"edits\": [dict(op=\"replace_cell\", cell_id=cell_id, source_lines=[exact_source])], \"dry_run\": True})\n    assert dry_result.structured_content[\"changed\"] is True\n    assert dry_result.structured_content[\"dry_run\"] is True\n    assert dry_result.structured_content[\"affected_cell_ids\"] == [cell_id]\n    assert dry_result.structured_content[\"before_hash\"] == dry_result.structured_content[\"after_hash\"]\n    assert dry_result.structured_content[\"planned_hash\"] != dry_result.structured_content[\"before_hash\"]\n    assert _read_raw_nb(edit_nb).cells[0].source == 'payload = \"old\"\\nvalue = 1'\n\n    edit_result = await mcp.call_tool(\"edit_notebook\", {\"path\": str(edit_nb), \"edits\": [dict(op=\"replace_cell\", cell_id=cell_id, source_lines=[exact_source])]})\n    assert edit_result.structured_content[\"changed\"] is True\n    assert _read_raw_nb(edit_nb).cells[0].source == exact_source\n\n    range_result = await mcp.call_tool(\"edit_notebook\", {\"path\": str(edit_nb), \"edits\": [dict(op=\"replace_lines\", cell_id=cell_id, start_line=1, end_line=1, replacement_lines=['payload = \"range\"'])]})\n    assert range_result.structured_content[\"changed\"] is True\n    assert _read_raw_nb(edit_nb).cells[0].source == 'payload = \"range\"'\n\n    insert_result = await mcp.call_tool(\"edit_notebook\", {\"path\": str(edit_nb), \"edits\": [dict(op=\"insert_cells\", anchor_id=cell_id, cells=[dict(cell_type=\"code\", source_lines=[\"inserted = True\"])])]})\n    assert insert_result.structured_content[\"changed\"] is True\n    inserted_id = _read_raw_nb(edit_nb).cells[1].id\n    assert _read_raw_nb(edit_nb).cells[1].source == \"inserted = True\"\n\n    move_result = await mcp.call_tool(\"edit_notebook\", {\"path\": str(edit_nb), \"edits\": [dict(op=\"move_cells\", cell_ids=[inserted_id], anchor_id=cell_id, where=\"before\")]})\n    assert move_result.structured_content[\"changed\"] is True\n    assert _read_raw_nb(edit_nb).cells[0].id == inserted_id\n\n    delete_result = await mcp.call_tool(\"edit_notebook\", {\"path\": str(edit_nb), \"edits\": [dict(op=\"delete_cells\", cell_ids=[inserted_id])]})\n    assert delete_result.structured_content[\"changed\"] is True\n\n\nwith _demo_root(\"07_mcp_edit_tools\", mkdir=False) as edit_root:\n    await _exercise_mcp_edit_tools(edit_root)\n    print(\"direct MCP edit tools used source_lines without JSON plan text\")\n\n\nstyle_doctor = _doctor_report(\".\", scopes=\"style\", max_output_chars=200, max_diagnostics=5)\nassert style_doctor[\"style\"] is not None\nassert \"chkstyle\" in style_doctor[\"style\"]\n\n\n\nMCP log problem visibility\nShort terminal reports still need to account for the whole log. mcp_log_report keeps full problem counts and grouped classifications even when problems is limited, so a compact report cannot hide systemic failures.\n\ndebug = mcp_tool_result(\"context\", {\"target\": \"nbs/example.ipynb\"}, long_output, detail=\"debug\")\nassert debug.structured_content[\"debug\"][\"arguments\"] == {\"target\": \"nbs/example.ipynb\"}\nassert debug.structured_content[\"debug\"][\"raw_output\"] == long_output\nassert \"Result:\" in debug.content[0].text\n\nshape_cases = [\n    (\n        \"context\",\n        {\"target\": \"foo\", \"scope\": \"nbs\"},\n        \"full context\",\n        {\"context\": {\"resolved_kind\": \"symbol\", \"path\": \"nbs/a.ipynb\", \"symbol\": \"foo\", \"cells\": [dict(path=\"nbs/a.ipynb\", cell_id=\"abc123\", source=\"def foo(): pass\")]}},\n        (\"context completed\", \"resolved=symbol\", \"path=nbs/a.ipynb\", \"symbol=foo\", \"cells=1\", \"id=abc123\"),\n    ),\n    (\n        \"context\",\n        {\"target\": \"nbs\", \"include_re\": \"foo\", \"view\": \"full\"},\n        \"full matches\",\n        {\"context\": {\"resolved_kind\": \"filter\", \"selection\": {\"kind\": \"context_filter\", \"matches\": [dict(path=\"nbs/a.ipynb\", cell_id=\"abc123\", cell_idx=7, source=\"#| export\\ndef foo(): pass\\nclass Bar:\\n    def baz(self): pass\")], \"total_matches\": 3}}},\n        (\"context completed\", \"resolved=filter\", \"matches=1 shown of 3\", \"id=abc123\", \"def foo(): pass\", \"class Bar:\", \"def baz(self): pass\"),\n    ),\n    (\n        \"diff_nb\",\n        {\"path\": \"nbs/a.ipynb\"},\n        \"--- code cell abc123 ---\\n-old\\n+new\\n\",\n        {},\n        (\"diff_nb completed\", \"changed_cells=1\", \"added_lines=1\", \"deleted_lines=1\", \"cells=abc123\"),\n    ),\n    (\n        \"doctor\",\n        {\"path\": \"nbs/a.ipynb\"},\n        \"long doctor output\",\n        {\"doctor\": {\"error_count\": 1, \"warning_count\": 1}},\n        (\"doctor completed\", \"errors=1 warnings=1\"),\n    ),\n    (\n        \"reference\",\n        {\"action\": \"query\", \"query\": \"foo\"},\n        \"{}\",\n        {\"reference\": {\"backend\": \"local\", \"hits\": [dict(module=\"pkg.mod\", symbol=\"thing\", dependency_status=\"direct\", score=0.91)]}},\n        (\"reference completed\", \"hits=1 backend=local\", \"pkg.mod.thing: direct score=0.91\"),\n    ),\n    (\n        \"edit_notebook\",\n        {\"path\": \"nbs/a.ipynb\", \"dry_run\": True},\n        \"full diff\",\n        {\"edit_notebook\": {\"changed\": True, \"dry_run\": True, \"affected_cell_ids\": [\"abc123\"], \"diffs\": [dict(changed=True)]}},\n        (\"edit_notebook completed\", \"changed=True\", \"dry_run=True\", \"affected_cells=1\", \"cells=abc123\"),\n    ),\n]\n\nold_doctor_warnings = globals().get(\"_doctor_warnings\")\ntry:\n    globals()[\"_doctor_warnings\"] = lambda *args, **kwargs: []\n    for tool, arguments, raw, structured, fragments in shape_cases:\n        result = mcp_tool_result(tool, arguments, raw, **structured)\n        summary = result.content[0].text\n        assert \"Result:\" not in summary\n        assert not {\"summary\", \"full_output\", \"preview_output\"} & result.structured_content.keys()\n        for fragment in fragments:\n            assert fragment in summary, (tool, fragment, summary)\nfinally:\n    globals()[\"_doctor_warnings\"] = old_doctor_warnings\n\n\ncalls = []\n\n\nold_doctor_warnings = _doctor_warnings\n\n\ndef _capture_mcp_warning_scopes():\n    try:\n        def fake_doctor_warnings(path=\".\", scope_path=None, scope_cell_ids=None):\n            cell_scope = None if scope_cell_ids is None else set(scope_cell_ids)\n            calls.append((path, scope_path, cell_scope))\n            return []\n\n        globals()[\"_doctor_warnings\"] = fake_doctor_warnings\n        mcp_tool_result(\"healthcheck\", {}, \"ok\")\n        mcp_tool_result(\"context\", {\"target\": \"nbs\"}, \"Project context\")\n        mcp_tool_result(\"context\", {\"target\": \"nbs/chapter.ipynb\"}, \"Cell id=ch1\\nCell id=ch2\")\n        mcp_tool_result(\"diff_nb\", {\"path\": \"nbs/07_mcp.ipynb\"}, \"diff without cell ids\")\n        mcp_tool_result(\"diff_nb\", {\"path\": \"nbs/07_mcp.ipynb\"}, \"--- code cell abc123 ---\\nchanged\")\n        mcp_tool_result(\"edit_notebook\", {\"path\": \"nbs/update_scope.ipynb\", \"edits\": [dict(op=\"replace_cell\", cell_id=\"upd123\", source_lines=[\"x\"])]}, \"edit_notebook changed\")\n        mcp_tool_result(\"edit_notebook\", {\"path\": \"nbs/batch_a.ipynb\", \"edits\": [dict(op=\"replace_cell\", path=\"nbs/batch_b.ipynb\", cell_id=\"b1\", source_lines=[\"b\"])]}, \"edit_notebook changed\")\n    finally:\n        globals()[\"_doctor_warnings\"] = old_doctor_warnings\n\n\n_capture_mcp_warning_scopes()\n\n\nassert calls == [\n    (\"nbs/07_mcp.ipynb\", \"nbs/07_mcp.ipynb\", set()),\n    (\"nbs/07_mcp.ipynb\", \"nbs/07_mcp.ipynb\", {\"abc123\"}),\n]\n\n\nsample = [\n    _warning(\"other\", \"other notebook\", path=\"nbs/other.ipynb\"),\n    _warning(\"same_other_cell\", \"same notebook other cell\", path=\"nbs/07_mcp.ipynb\", cell_id=\"other-cell\"),\n    _warning(\"current\", \"current displayed cell\", path=\"nbs/07_mcp.ipynb\", cell_id=\"abc123\"),\n    _warning(\"notebook_export_missing\", \"current notebook\", path=\"nbs/07_mcp.ipynb\"),\n]\n\n\nfiltered = _filter_warnings_for_scope(sample, Path(\".\").resolve(), \"nbs/07_mcp.ipynb\", {\"abc123\"})\nassert [item[\"code\"] for item in filtered] == [\"current\", \"notebook_export_missing\"]\n\n\nassert not _cell_export_relevant({\"cell_type\": \"code\", \"source\": [\"print('demo')\"]})\nassert _cell_export_relevant({\"cell_type\": \"code\", \"source\": [\"#| export\\ndef exported():\\n    pass\"]})\nmodule_path = Path(\"nbskill/mcp.py\")\nif not module_path.exists(): module_path = Path(\"../nbskill/mcp.py\")\nowner = generated_owner(module_path)\nassert owner and owner.name == \"07_mcp.ipynb\"\nassert \"07_mcp.ipynb\" in _owner_output(module_path)\nassert git_root(\"nbs/07_mcp.ipynb\") == git_root(\".\")\nassert _rel_to_root(\"nbs/07_mcp.ipynb\", git_root(\".\")) == \"nbs/07_mcp.ipynb\"\n\n\nwith write_demo_notebook(\"07_mcp_insert_lines.ipynb\") as path:\n    path = path.resolve()\n    nb = new_nb([mk_cell(\"anchor = True\")])\n    anchor_id = nb.cells[0].id\n    _write_raw_nb(nb, path)\n    result = await mcp.call_tool(\n        \"edit_notebook\",\n        {\n            \"path\": str(path),\n            \"edits\": [dict(op=\"insert_cells\", anchor_id=anchor_id, cells=[dict(cell_type=\"code\", source_lines=['source = \"line 1\\\\nline 2\"'])])],\n        },\n    )\n    assert result.structured_content[\"changed\"] is True\n    assert _read_raw_nb(path).cells[1].source == 'source = \"line 1\\\\nline 2\"'\n\n\nwith write_demo_notebook(\"07_mcp_sample.ipynb\") as path:\n    _example_write_nb(\n        str(path),\n        \"%%code\\n\"\n        \"#| default_exp sample\\n\"\n        \"def sample():\\n\"\n        \"    return 'ok'\",\n        replace=True,\n    )\n    text = capture_notebook_call(_example_context, path, target=str(path))\n    assert \"def sample():\" in text\n    print(\"captured file context lines:\", len(text.splitlines()))\n\n\nassert as_text(None) == \"\"\nassert as_text({\"ok\": True}) == \"{'ok': True}\"\nassert capture_call(lambda: \"returned\") == \"returned\"\n\n\ndef _prints_and_returns():\n    print(\"printed\")\n    return \"returned\"\n\n\nassert capture_call(_prints_and_returns) == \"printed\"\n\n\ndef _prints_and_exits():\n    print(\"before exit\")\n    raise SystemExit(7)\n\n\ntry:\n    capture_call(_prints_and_exits)\nexcept RuntimeError as exc:\n    assert \"before exit\" in str(exc)\n    assert \"SystemExit: 7\" in str(exc)\nelse:\n    raise AssertionError(\"SystemExit should be converted to RuntimeError for MCP tools\")\n\n\noriginal_stdout = sys.stdout\noutputs = []\nerrors = []\nentered = threading.Event()\n\n\ndef _slow_print(label, delay, signal=None):\n    def inner():\n        if signal is not None: signal.set()\n        time.sleep(delay)\n        print(label)\n    return inner\n\n\ndef _capture_worker(label, delay, signal=None):\n    try:\n        outputs.append(capture_call(_slow_print(label, delay, signal=signal)))\n    except BaseException as exc:\n        errors.append(exc)\n\n\ncell_stdout = sys.stdout\nthreads = [\n    threading.Thread(target=_capture_worker, args=(\"first\", 0.03, entered)),\n    threading.Thread(target=_capture_worker, args=(\"second\", 0.01)),\n]\nthreads[0].start()\nassert entered.wait(1)\nthreads[1].start()\nfor thread in threads: thread.join()\n\nassert errors == []\nassert sorted(outputs) == [\"first\", \"second\"]\nassert sys.stdout is cell_stdout\n\n\ntools = {tool.name: tool for tool in await mcp.list_tools()}\nassert \"visible_text_inventory\" not in tools",
    "crumbs": [
      "MCP server"
    ]
  },
  {
    "objectID": "09_parallel.html",
    "href": "09_parallel.html",
    "title": "nbskill",
    "section": "",
    "text": "Shared locks for MCP and notebook helpers. Calls touching the same notebook are serialized, calls touching different notebooks can proceed independently, and notebook execution uses one global semaphore.\nParallelism is useful only if notebook writes remain deterministic. Each notebook lock combines an in-process re-entrant lock with an OS file lock, held from read through commit and export. That makes concurrent MCP and Python writes to the same notebook wait for the active transaction to finish.\nwith notebook_locks(\"nbs/01_read.ipynb\", \"nbs/02_write.ipynb\"):\n    ...\n\nwith execution_slot():\n    ...\n\n\nConcurrency is production infrastructure. Per-notebook locks must serialize operations on the same notebook, acquire multiple notebook locks in stable order, allow independent notebooks to proceed in parallel, and keep notebook execution behind one global execution slot.\n\nfrom contextlib import redirect_stdout\nfrom io import StringIO\nimport threading, time\nfrom nbskill.parallel import execution_slot as _example_execution_slot\nfrom nbskill.parallel import notebook_key as _example_notebook_key\nfrom nbskill.parallel import notebook_locks as _example_notebook_locks\nfrom nbskill.foundation import demo_path, remove_demo_path, write_demo_notebook\nimport subprocess, sys, time\n\n\nprint(_example_notebook_key(\"nbs/../nbs/01_read.ipynb\"))\nwith _example_notebook_locks(\"nbs/01_read.ipynb\", \"nbs/02_write.ipynb\"):\n    print(\"notebook locks acquired\")\nwith _example_execution_slot():\n    print(\"execution slot acquired\")\n\n\n\n\nThe lock registry and execution gate live in one small module so every notebook operation uses the same coordination policy. Safe in-process execution is serialized here; wrappers that launch a child process keep notebook locks around the target paths.\n\n\n\nNotebook paths can be relative, absolute, or not created yet. notebook_key normalizes them into a consistent lock key without requiring the file to already exist.\n\nsource\n\n\n\ndef notebook_key(\n    path\n):\nReturn a stable lock key for a notebook path.\n\n\n\nnotebook_locks acquires all requested notebook locks in sorted order. That lets operations touching multiple notebooks avoid deadlocks while still allowing unrelated notebooks to be edited in parallel.\n\nsource\n\n\n\ndef notebook_locks(\n    *paths\n):\nAcquire per-notebook locks in a stable order.\n\n\n\nNotebook execution mutates kernels and outputs, so execution is serialized globally. Read and write operations can still use per-notebook locks around their own files.\n\nsource\n\n\n\ndef execution_slot():\nSerialize notebook execution across parallel MCP calls.\n\nroot = demo_path(\"09_parallel_keys\")\ntry:\n    root.mkdir()\n    a = root / \"a.ipynb\"\n    b = root / \"sub\" / \"..\" / \"b.ipynb\"\n    assert notebook_key(a).endswith(\"a.ipynb\")\n    assert notebook_key(b).endswith(\"b.ipynb\")\n    assert notebook_key(None) is None\nfinally:\n    remove_demo_path(root)\n\n\nwith write_demo_notebook(\"09_parallel_same.ipynb\") as path:\n    entered = []\n    def enter_same_lock():\n        with notebook_locks(path):\n            entered.append(True)\n    with notebook_locks(path):\n        thread = threading.Thread(target=enter_same_lock)\n        thread.start()\n        time.sleep(0.05)\n        assert entered == []\n    thread.join(timeout=1)\n    assert entered == [True]\n\n\nactive = 0\nmax_active = 0\nguard = threading.Lock()\n\ndef hold_execution_slot():\n    global active, max_active\n    with execution_slot():\n        with guard:\n            active += 1\n            max_active = max(max_active, active)\n        time.sleep(0.05)\n        with guard:\n            active -= 1\n\nthreads = [threading.Thread(target=hold_execution_slot) for _ in range(3)]\nfor thread in threads: thread.start()\nfor thread in threads: thread.join()\nassert max_active == 1\n\n\nassert _example_notebook_key(\"nbs/../nbs/01_read.ipynb\") == _example_notebook_key(\"nbs/01_read.ipynb\")\n_acquired = False\nwith _example_notebook_locks(\"nbs/01_read.ipynb\", \"nbs/01_read.ipynb\", None):\n    _acquired = True\nassert _acquired",
    "crumbs": [
      "Parallel notebook operations"
    ]
  },
  {
    "objectID": "09_parallel.html#parallel-notebook-operations",
    "href": "09_parallel.html#parallel-notebook-operations",
    "title": "nbskill",
    "section": "",
    "text": "Shared locks for MCP and notebook helpers. Calls touching the same notebook are serialized, calls touching different notebooks can proceed independently, and notebook execution uses one global semaphore.\nParallelism is useful only if notebook writes remain deterministic. Each notebook lock combines an in-process re-entrant lock with an OS file lock, held from read through commit and export. That makes concurrent MCP and Python writes to the same notebook wait for the active transaction to finish.\nwith notebook_locks(\"nbs/01_read.ipynb\", \"nbs/02_write.ipynb\"):\n    ...\n\nwith execution_slot():\n    ...\n\n\nConcurrency is production infrastructure. Per-notebook locks must serialize operations on the same notebook, acquire multiple notebook locks in stable order, allow independent notebooks to proceed in parallel, and keep notebook execution behind one global execution slot.\n\nfrom contextlib import redirect_stdout\nfrom io import StringIO\nimport threading, time\nfrom nbskill.parallel import execution_slot as _example_execution_slot\nfrom nbskill.parallel import notebook_key as _example_notebook_key\nfrom nbskill.parallel import notebook_locks as _example_notebook_locks\nfrom nbskill.foundation import demo_path, remove_demo_path, write_demo_notebook\nimport subprocess, sys, time\n\n\nprint(_example_notebook_key(\"nbs/../nbs/01_read.ipynb\"))\nwith _example_notebook_locks(\"nbs/01_read.ipynb\", \"nbs/02_write.ipynb\"):\n    print(\"notebook locks acquired\")\nwith _example_execution_slot():\n    print(\"execution slot acquired\")\n\n\n\n\nThe lock registry and execution gate live in one small module so every notebook operation uses the same coordination policy. Safe in-process execution is serialized here; wrappers that launch a child process keep notebook locks around the target paths.\n\n\n\nNotebook paths can be relative, absolute, or not created yet. notebook_key normalizes them into a consistent lock key without requiring the file to already exist.\n\nsource\n\n\n\ndef notebook_key(\n    path\n):\nReturn a stable lock key for a notebook path.\n\n\n\nnotebook_locks acquires all requested notebook locks in sorted order. That lets operations touching multiple notebooks avoid deadlocks while still allowing unrelated notebooks to be edited in parallel.\n\nsource\n\n\n\ndef notebook_locks(\n    *paths\n):\nAcquire per-notebook locks in a stable order.\n\n\n\nNotebook execution mutates kernels and outputs, so execution is serialized globally. Read and write operations can still use per-notebook locks around their own files.\n\nsource\n\n\n\ndef execution_slot():\nSerialize notebook execution across parallel MCP calls.\n\nroot = demo_path(\"09_parallel_keys\")\ntry:\n    root.mkdir()\n    a = root / \"a.ipynb\"\n    b = root / \"sub\" / \"..\" / \"b.ipynb\"\n    assert notebook_key(a).endswith(\"a.ipynb\")\n    assert notebook_key(b).endswith(\"b.ipynb\")\n    assert notebook_key(None) is None\nfinally:\n    remove_demo_path(root)\n\n\nwith write_demo_notebook(\"09_parallel_same.ipynb\") as path:\n    entered = []\n    def enter_same_lock():\n        with notebook_locks(path):\n            entered.append(True)\n    with notebook_locks(path):\n        thread = threading.Thread(target=enter_same_lock)\n        thread.start()\n        time.sleep(0.05)\n        assert entered == []\n    thread.join(timeout=1)\n    assert entered == [True]\n\n\nactive = 0\nmax_active = 0\nguard = threading.Lock()\n\ndef hold_execution_slot():\n    global active, max_active\n    with execution_slot():\n        with guard:\n            active += 1\n            max_active = max(max_active, active)\n        time.sleep(0.05)\n        with guard:\n            active -= 1\n\nthreads = [threading.Thread(target=hold_execution_slot) for _ in range(3)]\nfor thread in threads: thread.start()\nfor thread in threads: thread.join()\nassert max_active == 1\n\n\nassert _example_notebook_key(\"nbs/../nbs/01_read.ipynb\") == _example_notebook_key(\"nbs/01_read.ipynb\")\n_acquired = False\nwith _example_notebook_locks(\"nbs/01_read.ipynb\", \"nbs/01_read.ipynb\", None):\n    _acquired = True\nassert _acquired",
    "crumbs": [
      "Parallel notebook operations"
    ]
  },
  {
    "objectID": "10_graph.html",
    "href": "10_graph.html",
    "title": "nbskill",
    "section": "",
    "text": "Statically match notebook definitions and calls so agents can see which cells define, call, and depend on a symbol.\nThis notebook adds a static map across notebooks. It does not execute project code; it parses cells with ast, records where symbols are defined, and reports which cells call those symbols.\nThat gives agents a fast way to answer questions like “where is this helper used?” before editing a private function or moving code between notebooks.\nThe graph is a navigation aid, not a runtime dependency analyzer. It answers practical maintenance questions: where is this symbol defined, who calls it, what does it call, can one symbol reach another through static calls, and are any notebooks importing private helpers that should stay local?\nsymbol_graph(path=\"nbs\", symbol=\"write_nb\")\nsymbol_connection(path=\"nbs\", start=\"symbol_connection\", end=\"_resolve_callees\")\nprivate_symbol_report(path=\"nbs\")\n\n\nThe graph tools are maintenance aids, not runtime dependency analyzers. Production behavior is limited to static definitions, callers, callee summaries, order warnings, and private-boundary reports with modest claims about accuracy.",
    "crumbs": [
      "Notebook symbol graph"
    ]
  },
  {
    "objectID": "10_graph.html#notebook-symbol-graph",
    "href": "10_graph.html#notebook-symbol-graph",
    "title": "nbskill",
    "section": "",
    "text": "Statically match notebook definitions and calls so agents can see which cells define, call, and depend on a symbol.\nThis notebook adds a static map across notebooks. It does not execute project code; it parses cells with ast, records where symbols are defined, and reports which cells call those symbols.\nThat gives agents a fast way to answer questions like “where is this helper used?” before editing a private function or moving code between notebooks.\nThe graph is a navigation aid, not a runtime dependency analyzer. It answers practical maintenance questions: where is this symbol defined, who calls it, what does it call, can one symbol reach another through static calls, and are any notebooks importing private helpers that should stay local?\nsymbol_graph(path=\"nbs\", symbol=\"write_nb\")\nsymbol_connection(path=\"nbs\", start=\"symbol_connection\", end=\"_resolve_callees\")\nprivate_symbol_report(path=\"nbs\")\n\n\nThe graph tools are maintenance aids, not runtime dependency analyzers. Production behavior is limited to static definitions, callers, callee summaries, order warnings, and private-boundary reports with modest claims about accuracy.",
    "crumbs": [
      "Notebook symbol graph"
    ]
  },
  {
    "objectID": "10_graph.html#parse-notebooks-and-collect-evidence",
    "href": "10_graph.html#parse-notebooks-and-collect-evidence",
    "title": "nbskill",
    "section": "1. Parse notebooks and collect evidence",
    "text": "1. Parse notebooks and collect evidence\nThe first chapter turns notebook cells into small, inspectable records. Each helper has one job so the order of the parser is visible to the reader.\n\nDiscovering definitions and calls\nThe parser records function, class, and method definitions, then walks call expressions inside each parsed tree. Both fully qualified names and short names are kept because notebooks often import helpers into local scope.\n\n\nBuilding graph records\nEach code cell can contribute definition records and caller records. The collected graph is deliberately simple dictionaries so reports, tests, and future tools can inspect it without a graph database.\n\n\nCell order warnings\nNotebook cells are executed in order, so a top-level call should not appear before the cell that defines or imports its callable. The order checker also looks inside function bodies for callable roots that are never defined or imported, which catches rarely exercised missing imports before runtime.\nThe checker treats names bound inside ordinary blocks as local to the cell. That matters for context-manager examples such as with write_demo_notebook(...) as path: and capture helpers such as out = StringIO(): those variables are setup, not missing imports.\n\nsource\n\n\n_CallRootVisitor.visit_FunctionDef\ndef visit_FunctionDef(\n    node\n):\nCall self as a function.\n\nsource\n\n\n_CallRootVisitor.visit_AsyncFunctionDef\ndef visit_AsyncFunctionDef(\n    node\n):\nCall self as a function.\n\nsource\n\n\n_CallRootVisitor.visit_Lambda\ndef visit_Lambda(\n    node\n):\nCall self as a function.\n\nsource\n\n\n_CallRootVisitor.visit_ListComp\ndef visit_ListComp(\n    node\n):\nCall self as a function.\n\nsource\n\n\n_CallRootVisitor.visit_SetComp\ndef visit_SetComp(\n    node\n):\nCall self as a function.\n\nsource\n\n\n_CallRootVisitor.visit_DictComp\ndef visit_DictComp(\n    node\n):\nCall self as a function.\n\nsource\n\n\n_CallRootVisitor.visit_GeneratorExp\ndef visit_GeneratorExp(\n    node\n):\nCall self as a function.\n\nsource\n\n\n_CallRootVisitor.visit_Call\ndef visit_Call(\n    node\n):\nCall self as a function.\n\nsource\n\n\nnotebook_order_problems\ndef notebook_order_problems(\n    path:str='nbs', nb:NoneType=None\n):\nReturn structured calls-before-definitions and missing callable import warnings.\n\nEditing without committing\nnotebook_order_problems accepts an in-memory notebook so edit tools can warn about a call that now precedes its definition before they commit the change.\n\norder_problems = notebook_order_problems(\"nbs/10_graph.ipynb\")\nassert isinstance(order_problems, list)\n\n\nsource\n\n\n\nnotebook_order_problem_lines\ndef notebook_order_problem_lines(\n    path:str='nbs'\n):\nReturn style-check lines for calls before definitions/imports and missing callable imports.",
    "crumbs": [
      "Notebook symbol graph"
    ]
  },
  {
    "objectID": "10_graph.html#resolve-and-report-relationships",
    "href": "10_graph.html#resolve-and-report-relationships",
    "title": "nbskill",
    "section": "2. Resolve and report relationships",
    "text": "2. Resolve and report relationships\nThe graph is approximate by design. Matching by exact or short symbol name is enough to surface likely callers and callees, which is the useful pre-edit signal for this project.\nA symbol connection follows definition-to-callee edges with a bounded breadth-first search. That keeps the answer practical: show the shortest static call chain that explains how one symbol can reach another, and stop before approximate short-name matching turns into noise.\n\nFormatting reports\nThe reporting helpers turn graph records into compact text for humans and complete structured data for automation. Text output stays short enough for agent context, while JSON output keeps every caller usage line so migration scripts do not have to scrape truncated prose.\n\nsource\n\n\nsymbol_graph_data\ndef symbol_graph_data(\n    path, symbol\n):\nReturn structured definitions, callers, and callees for a symbol.\n\nSymbols as graph entities\nA symbol becomes more useful when its caller and callee relationships travel with it. The graph layer patches those relationship methods onto NotebookSymbol because this notebook owns the static call graph.\n\nsource\n\n\n\nNotebookSymbol.definitions\ndef definitions():\nReturn definition records for this symbol.\n\nsource\n\n\nNotebookSymbol.callers\ndef callers():\nReturn call records that reference this symbol.\n\nsource\n\n\nNotebookSymbol.caller_usages\ndef caller_usages():\nReturn formatted caller usage snippets for this symbol.\n\nsource\n\n\nNotebookSymbol.callees\ndef callees():\nReturn symbols called by this symbol.\n\nsource\n\n\nNotebookSymbol.relationships_to_record\ndef relationships_to_record():\nReturn this symbol with caller and callee relationship data.\n\nsource\n\n\nNotebookSymbol.relationships_to_xml\ndef relationships_to_xml():\nReturn an XML-shaped symbol relationship view for LLM context.\n\n_graph_example_data = {\n    \"symbol\": \"helper\",\n    \"definitions\": [{\"symbol\": \"helper\", \"path\": \"demo.ipynb\", \"cell_id\": \"abc\", \"cell_idx\": 1, \"module\": \"demo\", \"kind\": \"function\"}],\n    \"callers\": [{\"path\": \"demo.ipynb\", \"cell_id\": \"def\", \"cell_idx\": 2, \"calls\": [\"helper\"]}],\n    \"caller_usages\": [{\"path\": \"demo.ipynb\", \"cell_id\": \"def\", \"lineno\": 1, \"line\": \"helper()\"}],\n    \"callees\": [\"leaf\"],\n    \"graph\": {\"definitions\": []},\n}\n_graph_example = NotebookSymbol.from_graph_data(_graph_example_data)\n{\n    \"definitions\": _graph_example.definitions(),\n    \"callers\": _graph_example.callers(),\n    \"caller_usages\": _graph_example.caller_usages(),\n    \"callees\": _graph_example.callees(),\n}\n\n\nsource\n\n\nsymbol_graph_public_data\ndef symbol_graph_public_data(\n    data\n):\nReturn the compact public view of symbol graph data.\n\nsource\n\n\nsymbol_usage_summary\ndef symbol_usage_summary(\n    path, symbols\n):\nReturn a compact caller/callee summary for one or more symbols.\n\nPublic graph reports\nsymbol_graph focuses on one symbol’s definitions, callers, caller usage lines, and callees. symbol_connection answers the next question: whether one symbol can reach another by following static callee edges, returning the shortest chain it finds. Default output is compact text for quick orientation; json_output=True returns structured data for programmatic refactors. private_symbol_report looks for cross-notebook calls to private helpers, which is useful when deciding whether a private function can be changed safely.\n\nsource\n\n\n\nsymbol_graph\ndef symbol_graph(\n    path:str='nbs', # Notebook file, directory, or glob to scan\n    symbol:str='', # Function, class, or Class.method to inspect\n    json_output:bool=False, # Print complete structured JSON instead of compact text\n):\nPrint definitions, callers, and callees for a notebook symbol.\n\nsource\n\n\nprivate_symbol_report\ndef private_symbol_report(\n    path:str='nbs', # Notebook file, directory, or glob to scan\n):\nPrint cross-notebook calls to imported private _ symbols.\n\nsource\n\n\nsymbol_connection\ndef symbol_connection(\n    path:str='nbs', # Notebook file, directory, or glob to scan\n    start:str='', # Symbol where traversal starts\n    end:str='', # Symbol to find by following callees\n    max_depth:int=6, # Maximum number of edges to follow\n    json_output:bool=False, # Print complete structured JSON instead of compact text\n):\nPrint the shortest static callee chain connecting two notebook symbols.\n\nroot = demo_path(\"10_graph_example\")\ntry:\n    root.mkdir()\n    write_nb(new_nb([\n        mk_cell(\"#| default_exp demo\"),\n        mk_cell(\"#| export\\ndef helper():\\n    return 1\\n\\ndef target():\\n    return helper()\"),\n        mk_cell(\"target()\"),\n    ]), root / \"demo.ipynb\")\n    _example_symbol_graph(str(root), \"helper\")\nfinally:\n    remove_demo_path(root)\n\n\nsource\n\n\nsymbol_catalog\ndef symbol_catalog(\n    path:str='nbs'\n):\nReturn notebook symbols with placement and reuse evidence.",
    "crumbs": [
      "Notebook symbol graph"
    ]
  },
  {
    "objectID": "10_graph.html#build-a-notebook-knowledge-graph",
    "href": "10_graph.html#build-a-notebook-knowledge-graph",
    "title": "nbskill",
    "section": "3. Build a notebook knowledge graph",
    "text": "3. Build a notebook knowledge graph\nnotebook_knowledge_graph_data exports a typed, deterministic graph for local notebook structure. Edges carry confidence and evidence so call/import facts stay separate from heuristic similarity links.",
    "crumbs": [
      "Notebook symbol graph"
    ]
  },
  {
    "objectID": "10_graph.html#definition-order-for-the-knowledge-graph",
    "href": "10_graph.html#definition-order-for-the-knowledge-graph",
    "title": "nbskill",
    "section": "Definition order for the knowledge graph",
    "text": "Definition order for the knowledge graph\nThe notebook executes from top to bottom. The catalog builder and its helpers therefore appear before the knowledge-graph builder, which calls symbol_catalog.\n\nsource\n\nnotebook_knowledge_graph_data\ndef notebook_knowledge_graph_data(\n    path:str='nbs'\n):\nReturn a typed graph of notebook, cell, symbol, import, and heading structure.\n\nsource\n\n\nnotebook_knowledge_graph\ndef notebook_knowledge_graph(\n    path:str='nbs', json_output:bool=False\n):\nPrint a typed notebook knowledge graph summary, or JSON when requested.",
    "crumbs": [
      "Notebook symbol graph"
    ]
  },
  {
    "objectID": "10_graph.html#offer-conservative-reuse-advice",
    "href": "10_graph.html#offer-conservative-reuse-advice",
    "title": "nbskill",
    "section": "4. Offer conservative reuse advice",
    "text": "4. Offer conservative reuse advice\nThe advisor turns graph evidence into conservative suggestions. It never moves code; it records why an existing symbol or notebook is worth inspecting before an agent writes another helper.\n\nsource\n\nreuse_advice\ndef reuse_advice(\n    goal, path:str='nbs', source:NoneType=None, top_k:int=5\n):\nReturn existing symbols and notebooks to inspect before implementing code.\n\nsource\n\n\nplacement_advice\ndef placement_advice(\n    path:str='nbs', symbol:NoneType=None, source:NoneType=None, notebook:NoneType=None, top_k:int=5\n):\nReturn ranked notebook candidates plus evidence for a symbol or source snippet.\n\nsource\n\n\nnotebook_advice_problems\ndef notebook_advice_problems(\n    path:str='nbs'\n):\nReturn reuse and placement advisory diagnostics for notebook style reports.\n\ndef _write_graph_test_notebooks(root):\n    root.mkdir()\n    lib = root / \"lib.ipynb\"\n    caller = root / \"caller.ipynb\"\n    write_nb(new_nb([\n        mk_cell(\"#| default_exp lib\"),\n        mk_cell(\"#| export\\ndef helper():\\n    return 1\\n\\ndef _secret():\\n    return helper()\\n\\ndef target():\\n    return helper()\"),\n    ]), lib)\n    write_nb(new_nb([mk_cell(\"from nbskill.lib import target, _secret\\nvalue = target()\\n_secret()\")]), caller)\n    return lib, caller",
    "crumbs": [
      "Notebook symbol graph"
    ]
  },
  {
    "objectID": "10_graph.html#explore-the-graph",
    "href": "10_graph.html#explore-the-graph",
    "title": "nbskill",
    "section": "5. Explore the graph",
    "text": "5. Explore the graph\nThe symbol graph can answer questions that plain text search only approximates. It can connect a definition to the cells that call it, show what that definition itself calls, rank symbols by likely edit blast radius, and expose private helpers that have crossed notebook boundaries.\nThese prototype cells stay non-exported on purpose. They test strategies over the current nbs graph before deciding whether any report deserves a public Python or MCP surface. The results are static-analysis hints, not deletion permission: dynamic imports, entry points, examples, and short-name collisions still need human review.\n\ndef _explore_definition_id(record): return (record.get(\"module\"), record[\"symbol\"], record[\"path\"], record[\"cell_id\"])\n\n\ndef _explore_call_sites_by_name(graph):\n    sites = defaultdict(list)\n    for caller in graph[\"callers\"]:\n        for site in caller.get(\"call_sites\", ()):\n            sites[site.get(\"name\")].append(dict(path=caller[\"path\"], cell_id=caller[\"cell_id\"], cell_idx=caller[\"cell_idx\"], lineno=site.get(\"lineno\"), line=site.get(\"line\")))\n    return sites\n\n\ndef _explore_unique_sites(rows):\n    seen, unique = set(), []\n    for row in rows:\n        key = (row[\"path\"], row[\"cell_id\"], row.get(\"lineno\"), row.get(\"line\"))\n        if key in seen: continue\n        seen.add(key)\n        unique.append(row)\n    return unique\n\n\ndef _explore_callers_by_definition(graph):\n    sites = _explore_call_sites_by_name(graph)\n    callers = {}\n    for record in graph[\"definitions\"]:\n        names = dict.fromkeys([record[\"symbol\"], symbol_short_name(record[\"symbol\"])])\n        rows = [site for name in names for site in sites.get(name, ())]\n        callers[_explore_definition_id(record)] = _explore_unique_sites(rows)\n    return callers\n\n\ndef _explore_unused_candidates(graph):\n    callers = _explore_callers_by_definition(graph)\n    result = {\"public\": [], \"private\": []}\n    for record in graph[\"definitions\"]:\n        if callers[_explore_definition_id(record)]: continue\n        visibility = \"private\" if record[\"symbol\"].startswith(\"_\") else \"public\"\n        result[visibility].append(dict(symbol=record[\"symbol\"], kind=record[\"kind\"], module=record.get(\"module\"), path=record[\"path\"], cell_id=record[\"cell_id\"], caveat=\"check Python callers, docs, and dynamic calls before deleting\"))\n    for rows in result.values(): rows.sort(key=lambda row: (row[\"module\"] or \"\", row[\"symbol\"]))\n    return result\n\n\ndef _explore_hotspots(graph):\n    callers = _explore_callers_by_definition(graph)\n    rows = []\n    for record in graph[\"definitions\"]:\n        fan_in = len({(site[\"path\"], site[\"cell_id\"]) for site in callers[_explore_definition_id(record)]})\n        fan_out = len([symbol for symbol in _resolve_callees(graph, record.get(\"calls\", ())) if symbol != record[\"symbol\"]])\n        if fan_in or fan_out:\n            rows.append(dict(symbol=record[\"symbol\"], module=record.get(\"module\"), path=record[\"path\"], cell_id=record[\"cell_id\"], fan_in=fan_in, fan_out=fan_out, score=fan_in * 2 + fan_out))\n    return sorted(rows, key=lambda row: (row[\"score\"], row[\"fan_in\"], row[\"fan_out\"]), reverse=True)\n\n\ndef _explore_private_boundary_leaks(graph):\n    definitions = {(row.get(\"module\"), row[\"symbol\"]): row for row in graph[\"definitions\"]}\n    rows = []\n    for imported in graph[\"imports\"]:\n        if not symbol_short_name(imported[\"symbol\"]).startswith(\"_\"): continue\n        definition = definitions.get((imported[\"module\"], imported[\"symbol\"]))\n        if not definition or definition[\"path\"] == imported[\"path\"]: continue\n        for caller in graph[\"callers\"]:\n            if caller[\"path\"] != imported[\"path\"]: continue\n            sites = [site for site in caller.get(\"call_sites\", ()) if _call_matches_symbol(site.get(\"name\"), imported[\"local\"])]\n            if sites:\n                row = dict(symbol=imported[\"symbol\"], local=imported[\"local\"], defined_path=definition[\"path\"])\n                row.update(defined_cell=definition[\"cell_id\"], called_path=caller[\"path\"], called_cell=caller[\"cell_id\"], line=sites[0].get(\"line\"))\n                rows.append(row)\n    return rows\n\n\ndef _explore_ambiguous_short_names(graph):\n    sites = _explore_call_sites_by_name(graph)\n    by_short = defaultdict(list)\n    for record in graph[\"definitions\"]: by_short[symbol_short_name(record[\"symbol\"])].append(record)\n    rows = []\n    for name, records in by_short.items():\n        locations = {(row.get(\"module\"), row[\"symbol\"], row[\"path\"], row[\"cell_id\"]) for row in records}\n        if len(locations) &lt;= 1: continue\n        row = dict(short_name=name, definition_count=len(locations), call_site_count=len(sites.get(name, ())))\n        row.update(symbols=sorted({row[\"symbol\"] for row in records})[:6], caveat=\"short-name matching can overconnect this symbol\")\n        rows.append(row)\n    return sorted(rows, key=lambda row: (row[\"call_site_count\"], row[\"definition_count\"]), reverse=True)\n\n\ndef _explore_notebook_hotspots(graph, hotspots, private_leaks, order_problems=(), style=None):\n    def_counts = Counter(row[\"path\"] for row in graph[\"definitions\"])\n    central = Counter(row[\"path\"] for row in hotspots[:30])\n    leaks = Counter(row[\"called_path\"] for row in private_leaks)\n    order = Counter(row[\"path\"] for row in order_problems)\n    style_rows = (style or {}).get(\"notebook_problems\", ())\n    style_counts = Counter(row[\"path\"] for row in style_rows)\n    rows = []\n    for path in set(def_counts) | set(central) | set(leaks) | set(order) | set(style_counts):\n        score = central[path] * 3 + leaks[path] * 2 + order[path] + style_counts[path]\n        row = dict(path=path, definitions=def_counts[path], central_symbols=central[path])\n        row.update(private_leaks=leaks[path], order_warnings=order[path], style_warnings=style_counts[path], score=score)\n        rows.append(row)\n    return sorted(rows, key=lambda row: (row[\"score\"], row[\"definitions\"]), reverse=True)\n\nThe first useful reports are comparative rather than absolute. A symbol with no resolved callers is a cleanup candidate, not proof of dead code. A high fan-in or fan-out symbol deserves more careful tests before editing. A private boundary leak shows where notebook-local code has become a cross-module dependency. Ambiguous short names show where the current graph can lie by matching Class.method and method too generously.\n\n_explore_scope = \"nbs/*.ipynb\"\n_explore_notebook = notebook_paths(\"nbs/10_graph.ipynb\")[0]\n_explore_graph = _collect_graph(_explore_scope)\n_explore_style = style_report(_explore_notebook)\n_explore_unused = _explore_unused_candidates(_explore_graph)\n_explore_hotspot_rows = _explore_hotspots(_explore_graph)\n_explore_private_leaks = _explore_private_boundary_leaks(_explore_graph)\n_explore_ambiguous = _explore_ambiguous_short_names(_explore_graph)\n_explore_notebooks = _explore_notebook_hotspots(_explore_graph, _explore_hotspot_rows, _explore_private_leaks, notebook_order_problems(_explore_scope), _explore_style)\n_explore_connection_pairs = [\n    (\"symbol_connection\", \"_resolve_callees\"),\n    (\"symbol_graph\", \"_caller_usage_records\"),\n    (\"style_check\", \"notebook_order_problem_lines\"),\n]\n_explore_connections = [\n    _symbol_connection_public_data(_symbol_connection_data(_explore_scope, start, end, max_depth=6))\n    for start, end in _explore_connection_pairs\n]\n\nprint(f\"graph: {len(_explore_graph['definitions'])} definitions, {len(_explore_graph['callers'])} caller cells, {len(_explore_graph['imports'])} imports\")\nprint(\"hotspots:\", \", \".join(row[\"symbol\"] for row in _explore_hotspot_rows[:8]))\nprint(\"private leaks:\", \", \".join(row[\"symbol\"] for row in _explore_private_leaks[:6]))\nprint(\"unused public candidates:\", \", \".join(row[\"symbol\"] for row in _explore_unused[\"public\"][:6]))\nprint(\"ambiguous names:\", \", \".join(row[\"short_name\"] for row in _explore_ambiguous[:6]))\nprint(\"notebook hotspots:\", \", \".join(row[\"path\"] for row in _explore_notebooks[:4]))\nfor row in _explore_connections:\n    chain = \" -&gt; \".join(row[\"symbols\"]) if row[\"found\"] else \"not found\"\n    print(f\"connection {row['start']} to {row['end']}: {chain}\")\n\n\nPublic behavior\nThe order checker exposes calls that appear before their definitions or imports, which makes notebook execution problems visible before runtime.\n\n_order_examples = notebook_order_problems(\"nbs/10_graph.ipynb\")\nprint(f\"order diagnostics: {len(_order_examples)}\")\n\n\n\nnotebook_order_problem_lines\nnotebook_order_problem_lines converts the structured warnings into concise style-check lines that are easy to print or filter.\n\n_order_lines_example = notebook_order_problem_lines(\"nbs/10_graph.ipynb\")\nprint(\"\\n\".join(_order_lines_example[:3]) or \"no order warnings\")\n\n\n\nsymbol_graph_data\nsymbol_graph_data is the structured entry point for callers that need definitions, caller sites, and callee information without parsing report text.\n\n_graph_data_example = symbol_graph_data(\"nbs/10_graph.ipynb\", \"symbol_graph\")\nprint(dict(symbol=_graph_data_example[\"symbol\"], definitions=len(_graph_data_example[\"definitions\"]), callers=len(_graph_data_example[\"callers\"])))\n\n\n\nsymbol_graph_public_data\nsymbol_graph_public_data keeps the automation-facing shape small while preserving caller usage records.\n\n_public_graph_example = symbol_graph_public_data(_graph_data_example)\nprint(dict(symbol=_public_graph_example[\"symbol\"], callers=len(_public_graph_example[\"callers\"]), usages=len(_public_graph_example[\"caller_usages\"])))\n\n\n\nsymbol_usage_summary\nsymbol_usage_summary is the compact human-facing view for checking several symbols at once.\n\n_usage_summary_example = symbol_usage_summary(\"nbs/10_graph.ipynb\", [\"symbol_graph\", \"symbol_connection\"])\nprint(_usage_summary_example.splitlines()[0])\n\n\n\nPublic behavior\nThe report function prints a short orientation view for a symbol; use the data functions above when a caller needs machine-readable details.\n\nsymbol_graph(path=\"nbs/10_graph.ipynb\", symbol=\"symbol_graph\")\n\n\n\nprivate_symbol_report\nprivate_symbol_report is a boundary check for private helpers imported across notebooks.\n\nprivate_symbol_report(path=\"nbs/10_graph.ipynb\")\n\n\n\nsymbol_connection\nsymbol_connection follows the shortest approximate call chain between two symbols and prints whether a connection was found.\n\nsymbol_connection(path=\"nbs/10_graph.ipynb\", start=\"symbol_graph\", end=\"_collect_graph\")\n\n\n\nnotebook_knowledge_graph_data\nnotebook_knowledge_graph_data returns a typed graph of notebook, cell, symbol, import, and heading nodes.\n\n_knowledge_graph_example = notebook_knowledge_graph_data(\"nbs/10_graph.ipynb\")\nprint(dict(nodes=len(_knowledge_graph_example[\"nodes\"]), edges=len(_knowledge_graph_example[\"edges\"]), issues=len(_knowledge_graph_example[\"issues\"])))\n\n\n\nnotebook_knowledge_graph\nnotebook_knowledge_graph renders the typed graph as a concise summary for quick inspection.\n\nnotebook_knowledge_graph(path=\"nbs/10_graph.ipynb\")\n\n\n\nsymbol_catalog\nsymbol_catalog records where symbols live and the text evidence that helps agents decide what to reuse.\n\n_catalog_example = symbol_catalog(\"nbs/10_graph.ipynb\")\nprint(f\"catalog symbols: {len(_catalog_example['symbols'])}\")\n\n\n\nreuse_advice\nreuse_advice ranks existing symbols and notebooks before new code is written.\n\n_reuse_example = reuse_advice(\"notebook graph parser\", path=\"nbs/10_graph.ipynb\", top_k=3)\nprint(f\"reuse matches: {len(_reuse_example['matches'])}\")\n\n\n\nplacement_advice\nplacement_advice ranks likely notebook locations for a symbol or source snippet.\n\n_placement_example = placement_advice(path=\"nbs/10_graph.ipynb\", symbol=\"symbol_graph\", top_k=3)\nprint(f\"placement candidates: {len(_placement_example['candidates'])}\")\n\n\n\nnotebook_advice_problems\nnotebook_advice_problems collects conservative reuse, placement, and private-boundary diagnostics for style reports.\n\n_advice_problems_example = notebook_advice_problems(\"nbs/10_graph.ipynb\")\nprint(f\"advice diagnostics: {len(_advice_problems_example)}\")",
    "crumbs": [
      "Notebook symbol graph"
    ]
  },
  {
    "objectID": "02_edit.html",
    "href": "02_edit.html",
    "title": "nbskill",
    "section": "",
    "text": "edit_notebook is the production notebook mutation adapter. It keeps text transforms deterministic, applies notebook changes all-or-nothing, and returns structured diffs that MCP clients can inspect without parsing raw .ipynb JSON.\nThe shape is intentionally small: pure text helpers first, then one notebook/cell adapter. Read tools still provide context; this module only mutates notebooks.\nCraft warnings make risky notebook edits visible without changing edit semantics. They now flag calls whose definitions remain below the edited cell, so a notebook stays executable from top to bottom.",
    "crumbs": [
      "Notebook editing"
    ]
  },
  {
    "objectID": "02_edit.html#notebook-editing",
    "href": "02_edit.html#notebook-editing",
    "title": "nbskill",
    "section": "",
    "text": "edit_notebook is the production notebook mutation adapter. It keeps text transforms deterministic, applies notebook changes all-or-nothing, and returns structured diffs that MCP clients can inspect without parsing raw .ipynb JSON.\nThe shape is intentionally small: pure text helpers first, then one notebook/cell adapter. Read tools still provide context; this module only mutates notebooks.\nCraft warnings make risky notebook edits visible without changing edit semantics. They now flag calls whose definitions remain below the edited cell, so a notebook stays executable from top to bottom.",
    "crumbs": [
      "Notebook editing"
    ]
  },
  {
    "objectID": "02_edit.html#text-and-line-transforms",
    "href": "02_edit.html#text-and-line-transforms",
    "title": "nbskill",
    "section": "Text and line transforms",
    "text": "Text and line transforms\nThe first layer edits plain text. Keeping it independent from notebook I/O makes the same operations useful in direct Python calls and structured notebook edits.\n\nPure text helpers\nThe text helpers do not know about notebooks. They take strings, return strings, and raise clear ValueErrors for invalid line bounds or malformed replacements. That makes cell-level and notebook-level editing share the same behavior.\n\nsource\n\n\ninsert_lines\ndef insert_lines(\n    text, insert_line, new_lines\n):\nInsert new_lines before a 1-based line boundary in text; use n+1 to append.\n\nsource\n\n\nreplace_lines\ndef replace_lines(\n    text, start_line, end_line:NoneType=None, replacement_lines:NoneType=None\n):\nReplace a 1-based inclusive line range in text.\n\nsource\n\n\ndelete_lines\ndef delete_lines(\n    text, start_line:NoneType=None, end_line:NoneType=None, re_filter:NoneType=None, invert_filter:bool=False\n):\nDelete a 1-based inclusive line range, or lines matching re_filter.\n\nsource\n\n\nreplace_text\ndef replace_text(\n    text, old, new, start_line:NoneType=None, end_line:NoneType=None\n):\nReplace literal old with new, optionally inside a line range.\n\nsource\n\n\nreplace_texts\ndef replace_texts(\n    text, replacements:NoneType=None, olds:NoneType=None, news:NoneType=None, start_line:NoneType=None,\n    end_line:NoneType=None\n):\nApply several literal text replacements in order.\nThe public helper functions insert_lines, replace_lines, delete_lines, replace_text, and replace_texts use one-based line bounds for insert_line, start_line, and end_line, plus the same text semantics that edit_notebook applies to notebook cells.\n\nsample = \"alpha\\nbeta\\ngamma\"\n\nprint(insert_lines(sample, 2, [\"inserted\"]))\nprint(replace_lines(sample, 2, 2, [\"BETA\"]))\nprint(delete_lines(sample, re_filter=\"beta\"))\nprint(replace_text(sample, \"alpha\", \"ALPHA\"))\nprint(replace_texts(sample, [dict(old=\"alpha\", new=\"A\"), dict(old=\"gamma\", new=\"G\")]))",
    "crumbs": [
      "Notebook editing"
    ]
  },
  {
    "objectID": "02_edit.html#notebook-aware-edits",
    "href": "02_edit.html#notebook-aware-edits",
    "title": "nbskill",
    "section": "Notebook-aware edits",
    "text": "Notebook-aware edits\nThese operations add the notebook concerns: stable cell ids, metadata, output clearing, validation, and one atomic commit.\n\nNotebook adapter\nThe adapter selects cells, applies one deterministic operation per selected cell, validates the edited notebook before writing, clears stale outputs for changed code cells, stamps metadata, exports with nbdev, and returns structured results. The whole call succeeds or fails as one unit.\nfastcore 2.0 now provides the raw notebook insertion and movement mechanics through fastcore.nbio.Notebook. nbskill still builds cells, applies directives, validates code, records hashes, and formats MCP diffs here; only the low-level list mutation is delegated.",
    "crumbs": [
      "Notebook editing"
    ]
  },
  {
    "objectID": "02_edit.html#import-grouping-after-an-edit",
    "href": "02_edit.html#import-grouping-after-an-edit",
    "title": "nbskill",
    "section": "Import grouping after an edit",
    "text": "Import grouping after an edit\nAn edited cell may contain global imports together with exported code, tests, or examples. Before committing, the editor moves those imports into the notebook’s single matching import cell: exported code uses exported_import_cell; all runtime-only cells use internal_import_cell. Function-local imports stay in place.\n\nsource\n\nedit_notebook\ndef edit_notebook(\n    path, edits, validate_code:bool=True, default_cell_type:str='code', directive:NoneType=None,\n    auto_feedback:bool=True, feedback_timeout:int=10, feedback_safe:bool=True, detail:str='summary',\n    dry_run:bool=False\n):\nApply structured notebook edits atomically and return MCP-friendly details.",
    "crumbs": [
      "Notebook editing"
    ]
  },
  {
    "objectID": "02_edit.html#editor-facade",
    "href": "02_edit.html#editor-facade",
    "title": "nbskill",
    "section": "Editor facade",
    "text": "Editor facade\nThe facade keeps repeated edits scoped to one notebook while the lower-level functions remain useful for one-off transformations.\n\nA notebook-scoped editor\nNotebookEditor is a small facade over edit_notebook for one notebook, and Notebook.edit hands one back. Edits accept a directive (a Directive, its name, or a comma string such as export,no_test) so callers can say export and do not test instead of typing #| lines. The directive is stamped onto code cells, and context strips it back out because the semantic type already conveys it.\n\nsource\n\n\nNotebookEditor\ndef NotebookEditor(\n    path:object, validate_code:bool=True, auto_feedback:bool=False\n)-&gt;None:\nNotebook-scoped facade over edit_notebook.\n\nsource\n\n\nNotebookEditor.replace_cell\ndef replace_cell(\n    cell_id, source, directive:NoneType=None, cell_type:NoneType=None, **kw\n):\nReplace one cell source, optionally stamping directives onto code.\n\nsource\n\n\nNotebookEditor.insert\ndef insert(\n    anchor_id, source, where:str='after', directive:NoneType=None, cell_type:str='code', **kw\n):\nInsert one cell relative to anchor_id; pass None to append.\n\nsource\n\n\nNotebookEditor.explode\ndef explode(\n    cell_id:NoneType=None, **kw\n):\nSplit top-level function definitions in one cell, or matching cells, into separate cells.\n\nsource\n\n\nNotebookEditor.delete\ndef delete(\n    cell_id, **kw\n):\nDelete one cell by id.\n\nsource\n\n\nNotebookEditor.replace_text\ndef replace_text(\n    old, new:str='', cell_id:NoneType=None, **kw\n):\nReplace literal text in one cell, or across the notebook when cell_id is omitted.\n\nsource\n\n\nNotebook.edit\ndef edit():\nReturn a NotebookEditor bound to this notebook path.\n\nwith write_demo_notebook('editor_directive_demo.ipynb', cells=[mk_cell('def helper():\\n    return 1')]) as demo:\n    editor = Notebook.from_path(demo).edit()\n    print(type(editor).__name__)\n    cell_id = read_nb(demo).cells[0].id\n    editor.replace_cell(cell_id, 'def helper():\\n    return 2', directive='export,no_test')\n    print(read_nb(demo).cells[0].source)\n\nA notebook-wide rename is just a replace_text operation with target=\"all\". That path is the default for deterministic refactors such as renaming a public symbol across Markdown, examples, and tests.\n\nwith write_demo_notebook(\"edit_notebook_example.ipynb\", cells=[mk_cell(\"old_name = 1\")]) as example_path:\n    result = edit_notebook(\n        example_path,\n        [dict(op=\"replace_text\", target=\"all\", old=\"old_name\", new=\"new_name\")],\n        auto_feedback=False,\n    )\n    source = read_nb(example_path).cells[0].source\nsource\n\n\n\nHash-guarded dry runs\nAn expected_hash protects the exact cell source selected by context(..., mode=\"edit\"). Use a dry run to inspect the one-cell diff and replacement count before writing. In the MCP tool, feedback=\"off\" suppresses automatic feedback; smoke snippets remain an advanced, explicit option.\n\nwith write_demo_notebook(\"02_edit_hash_example.ipynb\", cells=[mk_cell(\"value = 1\")]) as path:\n    cell = read_nb(path).cells[0]\n    result = edit_notebook(path, [dict(op=\"replace_text\", cell_id=cell.id, old=\"1\", new=\"2\", expected_hash=source_hash(cell_source(cell)))], auto_feedback=False, dry_run=True)\n    print(result[\"affected_cell_ids\"], result[\"replacement_summary\"])",
    "crumbs": [
      "Notebook editing"
    ]
  },
  {
    "objectID": "06_skill.html",
    "href": "06_skill.html",
    "title": "nbskill",
    "section": "",
    "text": "Install the notebook workflow instructions, then register reusable local repositories.\nThe repository also ships the instructions that teach an agent how to use these tools. This notebook installs the bundled SKILL.md and references into local Codex or Claude skills directories, and can register the nbskill MCP server in Cursor’s mcp.json so the notebook-first workflow can travel with the package.\nThe package ships a compact routing skill and optional MCP reference files. The operational guidance lives in nbskill.skill, so installation copies package-owned files without rebuilding a skill from a README or changing another checkout.\ninstall_nbskill(skills_dir=\"~/.codex/skills\")\n\n\nSkill installation is Python-first. It installs only into the requested Codex or Claude skills directory, writes MCP configuration only when that target is requested, leaves MCP processes running, returns restart guidance when needed, and installs hooks only when explicitly requested. It does not alter a README or another repository.\n\nfrom nbskill.foundation import demo_path, remove_demo_path\n\n\n\n\nThe installer copies the compact routing skill and its optional MCP reference files. Native notebook methodology lives in nbskill.skill, so installation does not maintain another operational playbook or edit a separate checkout.\n\nsource\n\n\n\ndef nbskill_mcp_restart_notice(\n    timeout:float=5.0, force:bool=True, dry_run:bool=False\n):\nReturn the reconnect instruction; stdio servers are started by the MCP client.\n\n\n\nA Codex install writes a project-local configuration in the selected workspace and starts nbskill from that workspace.\n\nsource\n\n\n\ndef install_nbskill(\n    target:str='codex', # codex, claude, cursor, both, or custom when skills_dir is set\n    skills_dir:str | None=None, # Parent skills directory; skill is installed below jupyter-notebooks\n    skill_name:str='jupyter-notebooks', # Skill folder name\n    overwrite:bool=True, # Update stale managed files and server entries\n    install_hooks:bool=False, # Install nbdev-clean/nbdev-test pre-commit hooks in the current repo\n    restart_mcp:bool=True, # Report whether the MCP client needs reconnecting\n    cursor_workspace:str | None=None, # Cursor workspace for .cursor/mcp.json; omit for global ~/.cursor/mcp.json\n    codex_workspace:str | None='.', # Project workspace for .codex/config.toml\n    reference_roots:str='~/projects', # Local Git repositories to add to the reference index\n    index_references:bool=True, # Index references during installation\n):\nInstall current nbskill instructions and optional MCP configuration.",
    "crumbs": [
      "Installing the skill"
    ]
  },
  {
    "objectID": "06_skill.html#installing-the-skill",
    "href": "06_skill.html#installing-the-skill",
    "title": "nbskill",
    "section": "",
    "text": "Install the notebook workflow instructions, then register reusable local repositories.\nThe repository also ships the instructions that teach an agent how to use these tools. This notebook installs the bundled SKILL.md and references into local Codex or Claude skills directories, and can register the nbskill MCP server in Cursor’s mcp.json so the notebook-first workflow can travel with the package.\nThe package ships a compact routing skill and optional MCP reference files. The operational guidance lives in nbskill.skill, so installation copies package-owned files without rebuilding a skill from a README or changing another checkout.\ninstall_nbskill(skills_dir=\"~/.codex/skills\")\n\n\nSkill installation is Python-first. It installs only into the requested Codex or Claude skills directory, writes MCP configuration only when that target is requested, leaves MCP processes running, returns restart guidance when needed, and installs hooks only when explicitly requested. It does not alter a README or another repository.\n\nfrom nbskill.foundation import demo_path, remove_demo_path\n\n\n\n\nThe installer copies the compact routing skill and its optional MCP reference files. Native notebook methodology lives in nbskill.skill, so installation does not maintain another operational playbook or edit a separate checkout.\n\nsource\n\n\n\ndef nbskill_mcp_restart_notice(\n    timeout:float=5.0, force:bool=True, dry_run:bool=False\n):\nReturn the reconnect instruction; stdio servers are started by the MCP client.\n\n\n\nA Codex install writes a project-local configuration in the selected workspace and starts nbskill from that workspace.\n\nsource\n\n\n\ndef install_nbskill(\n    target:str='codex', # codex, claude, cursor, both, or custom when skills_dir is set\n    skills_dir:str | None=None, # Parent skills directory; skill is installed below jupyter-notebooks\n    skill_name:str='jupyter-notebooks', # Skill folder name\n    overwrite:bool=True, # Update stale managed files and server entries\n    install_hooks:bool=False, # Install nbdev-clean/nbdev-test pre-commit hooks in the current repo\n    restart_mcp:bool=True, # Report whether the MCP client needs reconnecting\n    cursor_workspace:str | None=None, # Cursor workspace for .cursor/mcp.json; omit for global ~/.cursor/mcp.json\n    codex_workspace:str | None='.', # Project workspace for .codex/config.toml\n    reference_roots:str='~/projects', # Local Git repositories to add to the reference index\n    index_references:bool=True, # Index references during installation\n):\nInstall current nbskill instructions and optional MCP configuration.",
    "crumbs": [
      "Installing the skill"
    ]
  },
  {
    "objectID": "03_execute.html",
    "href": "03_execute.html",
    "title": "nbskill",
    "section": "",
    "text": "Execute notebooks with visible outputs, local package imports, and a fast per-cell timeout. Cells that exceed the timeout are marked with execution-fingerprint metadata and skipped on later runs until their source changes.\nExecution closes the loop after reading and writing. The project needs a way to run notebooks as notebooks, with local imports available and with visible outputs copied back into the notebook for inspection.\nThis notebook wraps execnb with nbdev-friendly defaults: local import paths, optional partial execution, timeout handling, and a test helper that reports notebook errors in a concise form.\nExecution is the confidence step after an edit. The wrapper keeps imports close to how nbdev users run notebooks, records visible outputs for inspection, and marks timed-out cells so repeated runs do not keep blocking on the same long operation.\nexec_nb(\"nbs/03_execute.ipynb\", up2id=\"some-cell-id\", timeout=10, show_output=True)\n\n\nNotebook execution is production core. exec_nb must run notebooks with local project imports available, support partial execution by cell or chapter, capture visible output, keep safe mode on by default, mark timed-out cells so repeated runs do not trap users, and recover when timed-out source changes.\n\nfrom contextlib import redirect_stdout\nfrom io import StringIO\nfrom fastcore.nbio import read_nb\nfrom fastcore.test import test_eq\nfrom nbskill.execute import exec_nb as _example_exec_nb\nfrom nbskill.write import write_nb as _example_write_nb\nfrom nbskill.foundation import demo_path, remove_demo_path, write_demo_notebook, write_tool_notebook\nfrom nbskill.write import update_cell, write_nb\n\n\nwith write_demo_notebook(\"03_execute_example.ipynb\") as path:\n    _example_write_nb(str(path), \"%%code\\nvalue = 6 * 7\\nprint(value)\", replace=True)\n    _example_exec_nb(str(path), timeout=5, show_output=True)\n\nFastcore 2 removes fastcore.script.Param, and current safepyrun still imports it at module import time. Keep safepyrun lazy so MCP startup and non-execution tools can initialize, then install a tiny Param compatibility alias only when the safe runner is actually needed.\n\n\n\nSometimes verification only needs the first few cells or a single chapter. These helpers translate an index, a cell id, or a chapter into pre/post hooks that stop execution at the right point.\n\n\n\nNotebook execution should see the same local package that tests and examples see. This section finds the project root from common markers and puts the notebook folder, root, and optional src folder on the shell path.\nSafe execution also keeps a project policy file named .nbskill-exec-approval.json. Each run syncs top-level function names from the notebook into that file. Functions with no statically detected external effects default to allowed: true; functions that look like they can write files, spawn processes, remove files, or make network calls default to allowed: false until the project file is edited.\n\n\n\n_sync_execution_policy removes an approval only when the entry’s recorded cell no longer exists in its notebook. This keeps deleted cells from accumulating without removing approvals owned by other notebooks.\nSafepyrun 0.2 defaults to blocking def statements before nbskill’s project policy can approve them. We keep definitions enabled in the runner and let the notebook execution policy decide which functions are allowed to run.\n\nsource\n\n\n\ndef NBSafeShell(\n    path, extra_paths:NoneType=None, allow:NoneType=None, ok_dests:NoneType=None, cache_httpx:bool=False,\n    cache_dir:NoneType=None, cache_domains:NoneType=None, execution_policy:NoneType=None\n):\nInitialize self. See help(type(self)) for accurate signature.\n\n\n\nA timed-out cell can make repeated verification painful. Timeout metadata records the source fingerprint that timed out, skips that exact source on later runs, and automatically clears the mark when the cell changes.\n\n\n\nexec_nb is the user-facing wrapper around the execution engine. It writes outputs back to the notebook, prints visible outputs when requested, and supports partial execution through up2id or chapter.\n\nsource\n\n\n\ndef exec_nb(\n    path:str, # Notebook path\n    dest:str | None=None, # Destination path; defaults to overwriting path\n    exc_stop:bool=False, # Stop on exceptions\n    up2id:int | str | None=None, # Execute first N cells, or through this cell id\n    chapter:str | None=None, # Execute through this chapter, inclusive\n    timeout:int=30, # Per-cell timeout in seconds; &lt;=0 disables timeouts\n    show_output:bool=True, # Print saved cell outputs and errors after execution\n    verbose:bool=False, # Show stdout/stderr live while executing\n    safe:bool=True, # Use safepyrun instead of the legacy execnb shell\n    allow:str | None=None, # Comma-separated or literal list of trusted callables to allow\n    ok_dests:str | None=None, # Comma-separated or literal list of allowed write destinations\n    cache_httpx:bool=False, # Return cached httpx responses instead of making live calls\n    cache_dir:str | None=None, # Directory containing cachy.jsonl; defaults to project root\n    cache_domains:str | None=None, # Comma-separated or literal list of cacheable domains\n    allow_new:bool=False, # Execute cells without prior user/nbskill execution approval\n    check_only:bool=False, # Run in memory without writing notebook outputs or metadata\n):\nExecute a notebook with safe Python by default and local project imports available.\nWith fastcore 2, nbdev tests are treated as direct Python calls, so exec_nb(check_only=True) returns the notebook path while still leaving the file unchanged.\n\nroot = demo_path(\"03_execute_project\")\n\n\ndef _make_execute_project(name):\n    root = demo_path(name)\n    root.mkdir()\n    (root / \"pyproject.toml\").write_text(\"[project]\\nname = 'local-demo'\\n\", encoding=\"utf-8\")\n    pkg = root / \"local_demo\"\n    pkg.mkdir()\n    (pkg / \"__init__.py\").write_text(\"def meaning():\\n    return 42\\n\", encoding=\"utf-8\")\n    nbs = root / \"nbs\"\n    nbs.mkdir()\n    return root, nbs\n\n\nroot, nbs = _make_execute_project(\"03_execute_project\")\ntry:\n    path = nbs / \"sample.ipynb\"\n    write_nb(str(path), \"%%code\\nfrom local_demo import meaning\\nprint(meaning())\\nassert meaning() == 42\", replace=True)\n    exec_nb(str(path), timeout=5, allow=\"local_demo.meaning\")\n    text = \"\".join(output_text(output) for output in read_nb(path).cells[0].outputs)\n    assert \"refusing to execute unapproved cell\" in text\n    exec_nb(str(path), timeout=5, allow=\"local_demo.meaning\", allow_new=True)\n    text = \"\".join(output_text(output) for output in read_nb(path).cells[0].outputs)\n    assert \"42\" in text\n\n    site_root = root / \".venv\" / \"lib\" / f\"python{sys.version_info.major}.{sys.version_info.minor}\" / \"site-packages\"\n    site_root.mkdir(parents=True)\n    (site_root / \"project_only_dep.py\").write_text(\"def answer():\\n    return 'project env'\\n\", encoding=\"utf-8\")\n    project_env = nbs / \"project_env.ipynb\"\n    write_nb(str(project_env), \"%%code\\nfrom project_only_dep import answer\\nprint(answer())\", replace=True)\n    exec_nb(str(project_env), timeout=5, allow=\"project_only_dep.answer\", allow_new=True)\n    text = \"\".join(output_text(output) for output in read_nb(project_env).cells[0].outputs)\n    assert \"project env\" in text\nfinally:\n    remove_demo_path(root)\n\n\nroot, nbs = _make_execute_project(\"03_execute_check\")\ntry:\n    check = nbs / \"check_only.ipynb\"\n    write_nb(str(check), \"%%code\\nprint('check only')\", replace=True)\n    before = check.read_text(encoding=\"utf-8\")\n    exec_nb(str(check), timeout=5, allow_new=True, check_only=True)\n    assert check.read_text(encoding=\"utf-8\") == before\n\n    cross = nbs / \"cross.ipynb\"\n    write_nb(str(cross), \"%%code\\nx = 2\\n---\\n%%code\\nprint(x + 3)\", replace=True)\n    exec_nb(str(cross), timeout=5, allow_new=True)\n    text = \"\".join(output_text(output) for output in read_nb(cross).cells[1].outputs)\n    assert \"5\" in text\n\n    skipped = nbs / \"eval_false.ipynb\"\n    write_nb(str(skipped), \"%%code\\n#| eval: false\\nraise RuntimeError('should not run')\", replace=True)\n    exec_nb(str(skipped), timeout=5, allow_new=True)\n    assert read_nb(skipped).cells[0].outputs == []\nfinally:\n    remove_demo_path(root)\n\n\nroot, nbs = _make_execute_project(\"03_execute_safety\")\ntry:\n    slow = nbs / \"slow.ipynb\"\n    write_nb(str(slow), \"%%code\\nimport time\\ntime.sleep(2)\", replace=True)\n    exec_nb(str(slow), timeout=1, allow_new=True, safe=False)\n    cell = read_nb(slow).cells[0]\n    assert cell.metadata[\"nbskill\"][_TIMEOUT_HASH_KEY]\n    text = \"\".join(output_text(output) for output in cell.outputs)\n    assert \"ran longer than 1s\" in text\n    exec_nb(str(slow), timeout=1, allow_new=True, safe=False)\n    cell = read_nb(slow).cells[0]\n    text = \"\".join(output_text(output) for output in cell.outputs)\n    assert \"skipped cell\" in text\n    update_cell(str(slow), \"print('fast now')\", cell_id=cell.id)\n    exec_nb(str(slow), timeout=1, allow_new=True, safe=False)\n    cell = read_nb(slow).cells[0]\n    assert _TIMEOUT_HASH_KEY not in cell.metadata.get(\"nbskill\", {})\n    text = \"\".join(output_text(output) for output in cell.outputs)\n    assert \"fast now\" in text\nfinally:\n    remove_demo_path(root)\n\n\nroot, nbs = _make_execute_project(\"03_execute_blocks\")\ntry:\n    blocked_cases = {\n        \"path_write\": \"from pathlib import Path\\nPath('bad.txt').write_text('bad')\",\n        \"open_write\": \"open('bad.txt', 'w').write('bad')\",\n        \"remove\": \"import os\\nos.remove('missing.txt')\",\n        \"rmtree\": \"import shutil\\nshutil.rmtree('missing')\",\n        \"subprocess\": \"import subprocess\\nsubprocess.run(['true'])\",\n        \"magic\": \"%time 1 + 1\",\n        \"shell\": \"!echo unsafe\",\n    }\n    for name, source in blocked_cases.items():\n        nb_path = nbs / f\"{name}.ipynb\"\n        write_nb(str(nb_path), f\"%%code\\n{source}\", replace=True)\n        exec_nb(str(nb_path), timeout=5, allow_new=True)\n        text = \"\".join(output_text(output) for output in read_nb(nb_path).cells[0].outputs)\n        assert text\n        if name in {\"magic\", \"shell\"}: assert \"blocks IPython\" in text\nfinally:\n    remove_demo_path(root)\n\n\nroot, nbs = _make_execute_project(\"03_execute_write\")\ntry:\n    allowed_write = nbs / \"allowed_write.ipynb\"\n    ok_file = root / \"allowed.txt\"\n    write_nb(str(allowed_write), f\"%%code\\nfrom pathlib import Path\\nPath({str(ok_file)!r}).write_text('ok')\\nprint(Path({str(ok_file)!r}).read_text())\", replace=True)\n    exec_nb(str(allowed_write), timeout=5, ok_dests=str(root), allow_new=True)\n    assert ok_file.read_text(encoding=\"utf-8\") == \"ok\"\nfinally:\n    remove_demo_path(root)\n\n\nroot, nbs = _make_execute_project(\"03_execute_httpx\")\ntry:\n    import httpx\n    live = nbs / \"live_httpx.ipynb\"\n    write_nb(str(live), \"%%code\\nimport httpx\\nhttpx.get('https://api.openai.com/v1/test')\", replace=True)\n    exec_nb(str(live), timeout=5, allow_new=True)\n    text = \"\".join(output_text(output) for output in read_nb(live).cells[0].outputs)\n    assert \"blocked live httpx call\" in text\n\n    cached = nbs / \"cached_httpx.ipynb\"\n    url = \"https://api.openai.com/v1/test\"\n    request = httpx.Request(\"GET\", url)\n    key = _cachy_key(request)\n    (root / \"cachy.jsonl\").write_text(\n        json.dumps({\"key\": key, \"response\": \"cached body\", \"headers\": {\"content-type\": \"text/plain\"}, \"status_code\": 200}) + \"\\n\",\n        encoding=\"utf-8\",\n    )\n    write_nb(str(cached), f\"%%code\\nimport httpx\\nr = httpx.get({url!r})\\nprint(r.text)\", replace=True)\n    exec_nb(str(cached), timeout=5, cache_httpx=True, cache_dir=str(root), allow_new=True)\n    text = \"\".join(output_text(output) for output in read_nb(cached).cells[0].outputs)\n    assert \"cached body\" in text\nfinally:\n    remove_demo_path(root)\n\n\nroot, nbs = _make_execute_project(\"03_execute_httpx_miss\")\ntry:\n    miss = nbs / \"miss_httpx.ipynb\"\n    miss_url = \"https://api.openai.com/v1/miss\"\n    write_nb(str(miss), f\"%%code\\nimport httpx\\nhttpx.get({miss_url!r})\", replace=True)\n    exec_nb(str(miss), timeout=5, cache_httpx=True, cache_dir=str(root), allow_new=True)\n    text = \"\".join(output_text(output) for output in read_nb(miss).cells[0].outputs)\n    assert \"no cached httpx response\" in text\nfinally:\n    remove_demo_path(root)\n\n\nwarning_root = demo_path(\"03_execute_uncalled_warning\")\ntry:\n    warning_root.mkdir()\n    warning_nbs = warning_root / \"nbs\"\n    warning_nbs.mkdir()\n    uncalled = warning_nbs / \"uncalled.ipynb\"\n    write_nb(str(uncalled), \"%%code\\ndef unused_demo():\\n    return 1\\n---\\n%%code\\nprint('done')\", replace=True)\n    out = StringIO()\n    with redirect_stdout(out):\n        exec_nb(str(uncalled), timeout=5, allow_new=True, show_output=False)\n    warning_text = out.getvalue()\n    assert \"Execution warnings:\" in warning_text\n    assert \"unused_demo\" in warning_text\nfinally:\n    remove_demo_path(warning_root)\n\n\nwarning_root = demo_path(\"03_execute_called_warning\")\ntry:\n    warning_root.mkdir()\n    warning_nbs = warning_root / \"nbs\"\n    warning_nbs.mkdir()\n    called = warning_nbs / \"called.ipynb\"\n    write_nb(str(called), \"%%code\\ndef used_demo():\\n    return 1\\n---\\n%%code\\nused_demo()\", replace=True)\n    out = StringIO()\n    with redirect_stdout(out):\n        exec_nb(str(called), timeout=5, allow_new=True, show_output=False)\n    assert \"used_demo\" not in out.getvalue()\nfinally:\n    remove_demo_path(warning_root)\n\n\nwarning_root = demo_path(\"03_execute_decorated_warning\")\ntry:\n    warning_root.mkdir()\n    warning_nbs = warning_root / \"nbs\"\n    warning_nbs.mkdir()\n    decorated = warning_nbs / \"decorated.ipynb\"\n    write_nb(str(decorated), \"%%code\\ndef _route(func):\\n    return func\\n\\n@_route\\ndef api_handler():\\n    return 1\", replace=True)\n    out = StringIO()\n    with redirect_stdout(out):\n        exec_nb(str(decorated), timeout=5, allow_new=True, show_output=False)\n    assert \"api_handler\" not in out.getvalue()\nfinally:\n    remove_demo_path(warning_root)\n\n\n\n\nThe write tools can ask for a notebook test immediately after changing a notebook. These helpers summarize saved error outputs so a failed write/test cycle reports the useful cell-level problem.\n\nsource\n\n\n\ndef run_notebook_test(\n    path, timeout:int=30\n):\nCall self as a function.",
    "crumbs": [
      "Executing notebooks"
    ]
  },
  {
    "objectID": "03_execute.html#executing-notebooks",
    "href": "03_execute.html#executing-notebooks",
    "title": "nbskill",
    "section": "",
    "text": "Execute notebooks with visible outputs, local package imports, and a fast per-cell timeout. Cells that exceed the timeout are marked with execution-fingerprint metadata and skipped on later runs until their source changes.\nExecution closes the loop after reading and writing. The project needs a way to run notebooks as notebooks, with local imports available and with visible outputs copied back into the notebook for inspection.\nThis notebook wraps execnb with nbdev-friendly defaults: local import paths, optional partial execution, timeout handling, and a test helper that reports notebook errors in a concise form.\nExecution is the confidence step after an edit. The wrapper keeps imports close to how nbdev users run notebooks, records visible outputs for inspection, and marks timed-out cells so repeated runs do not keep blocking on the same long operation.\nexec_nb(\"nbs/03_execute.ipynb\", up2id=\"some-cell-id\", timeout=10, show_output=True)\n\n\nNotebook execution is production core. exec_nb must run notebooks with local project imports available, support partial execution by cell or chapter, capture visible output, keep safe mode on by default, mark timed-out cells so repeated runs do not trap users, and recover when timed-out source changes.\n\nfrom contextlib import redirect_stdout\nfrom io import StringIO\nfrom fastcore.nbio import read_nb\nfrom fastcore.test import test_eq\nfrom nbskill.execute import exec_nb as _example_exec_nb\nfrom nbskill.write import write_nb as _example_write_nb\nfrom nbskill.foundation import demo_path, remove_demo_path, write_demo_notebook, write_tool_notebook\nfrom nbskill.write import update_cell, write_nb\n\n\nwith write_demo_notebook(\"03_execute_example.ipynb\") as path:\n    _example_write_nb(str(path), \"%%code\\nvalue = 6 * 7\\nprint(value)\", replace=True)\n    _example_exec_nb(str(path), timeout=5, show_output=True)\n\nFastcore 2 removes fastcore.script.Param, and current safepyrun still imports it at module import time. Keep safepyrun lazy so MCP startup and non-execution tools can initialize, then install a tiny Param compatibility alias only when the safe runner is actually needed.\n\n\n\nSometimes verification only needs the first few cells or a single chapter. These helpers translate an index, a cell id, or a chapter into pre/post hooks that stop execution at the right point.\n\n\n\nNotebook execution should see the same local package that tests and examples see. This section finds the project root from common markers and puts the notebook folder, root, and optional src folder on the shell path.\nSafe execution also keeps a project policy file named .nbskill-exec-approval.json. Each run syncs top-level function names from the notebook into that file. Functions with no statically detected external effects default to allowed: true; functions that look like they can write files, spawn processes, remove files, or make network calls default to allowed: false until the project file is edited.\n\n\n\n_sync_execution_policy removes an approval only when the entry’s recorded cell no longer exists in its notebook. This keeps deleted cells from accumulating without removing approvals owned by other notebooks.\nSafepyrun 0.2 defaults to blocking def statements before nbskill’s project policy can approve them. We keep definitions enabled in the runner and let the notebook execution policy decide which functions are allowed to run.\n\nsource\n\n\n\ndef NBSafeShell(\n    path, extra_paths:NoneType=None, allow:NoneType=None, ok_dests:NoneType=None, cache_httpx:bool=False,\n    cache_dir:NoneType=None, cache_domains:NoneType=None, execution_policy:NoneType=None\n):\nInitialize self. See help(type(self)) for accurate signature.\n\n\n\nA timed-out cell can make repeated verification painful. Timeout metadata records the source fingerprint that timed out, skips that exact source on later runs, and automatically clears the mark when the cell changes.\n\n\n\nexec_nb is the user-facing wrapper around the execution engine. It writes outputs back to the notebook, prints visible outputs when requested, and supports partial execution through up2id or chapter.\n\nsource\n\n\n\ndef exec_nb(\n    path:str, # Notebook path\n    dest:str | None=None, # Destination path; defaults to overwriting path\n    exc_stop:bool=False, # Stop on exceptions\n    up2id:int | str | None=None, # Execute first N cells, or through this cell id\n    chapter:str | None=None, # Execute through this chapter, inclusive\n    timeout:int=30, # Per-cell timeout in seconds; &lt;=0 disables timeouts\n    show_output:bool=True, # Print saved cell outputs and errors after execution\n    verbose:bool=False, # Show stdout/stderr live while executing\n    safe:bool=True, # Use safepyrun instead of the legacy execnb shell\n    allow:str | None=None, # Comma-separated or literal list of trusted callables to allow\n    ok_dests:str | None=None, # Comma-separated or literal list of allowed write destinations\n    cache_httpx:bool=False, # Return cached httpx responses instead of making live calls\n    cache_dir:str | None=None, # Directory containing cachy.jsonl; defaults to project root\n    cache_domains:str | None=None, # Comma-separated or literal list of cacheable domains\n    allow_new:bool=False, # Execute cells without prior user/nbskill execution approval\n    check_only:bool=False, # Run in memory without writing notebook outputs or metadata\n):\nExecute a notebook with safe Python by default and local project imports available.\nWith fastcore 2, nbdev tests are treated as direct Python calls, so exec_nb(check_only=True) returns the notebook path while still leaving the file unchanged.\n\nroot = demo_path(\"03_execute_project\")\n\n\ndef _make_execute_project(name):\n    root = demo_path(name)\n    root.mkdir()\n    (root / \"pyproject.toml\").write_text(\"[project]\\nname = 'local-demo'\\n\", encoding=\"utf-8\")\n    pkg = root / \"local_demo\"\n    pkg.mkdir()\n    (pkg / \"__init__.py\").write_text(\"def meaning():\\n    return 42\\n\", encoding=\"utf-8\")\n    nbs = root / \"nbs\"\n    nbs.mkdir()\n    return root, nbs\n\n\nroot, nbs = _make_execute_project(\"03_execute_project\")\ntry:\n    path = nbs / \"sample.ipynb\"\n    write_nb(str(path), \"%%code\\nfrom local_demo import meaning\\nprint(meaning())\\nassert meaning() == 42\", replace=True)\n    exec_nb(str(path), timeout=5, allow=\"local_demo.meaning\")\n    text = \"\".join(output_text(output) for output in read_nb(path).cells[0].outputs)\n    assert \"refusing to execute unapproved cell\" in text\n    exec_nb(str(path), timeout=5, allow=\"local_demo.meaning\", allow_new=True)\n    text = \"\".join(output_text(output) for output in read_nb(path).cells[0].outputs)\n    assert \"42\" in text\n\n    site_root = root / \".venv\" / \"lib\" / f\"python{sys.version_info.major}.{sys.version_info.minor}\" / \"site-packages\"\n    site_root.mkdir(parents=True)\n    (site_root / \"project_only_dep.py\").write_text(\"def answer():\\n    return 'project env'\\n\", encoding=\"utf-8\")\n    project_env = nbs / \"project_env.ipynb\"\n    write_nb(str(project_env), \"%%code\\nfrom project_only_dep import answer\\nprint(answer())\", replace=True)\n    exec_nb(str(project_env), timeout=5, allow=\"project_only_dep.answer\", allow_new=True)\n    text = \"\".join(output_text(output) for output in read_nb(project_env).cells[0].outputs)\n    assert \"project env\" in text\nfinally:\n    remove_demo_path(root)\n\n\nroot, nbs = _make_execute_project(\"03_execute_check\")\ntry:\n    check = nbs / \"check_only.ipynb\"\n    write_nb(str(check), \"%%code\\nprint('check only')\", replace=True)\n    before = check.read_text(encoding=\"utf-8\")\n    exec_nb(str(check), timeout=5, allow_new=True, check_only=True)\n    assert check.read_text(encoding=\"utf-8\") == before\n\n    cross = nbs / \"cross.ipynb\"\n    write_nb(str(cross), \"%%code\\nx = 2\\n---\\n%%code\\nprint(x + 3)\", replace=True)\n    exec_nb(str(cross), timeout=5, allow_new=True)\n    text = \"\".join(output_text(output) for output in read_nb(cross).cells[1].outputs)\n    assert \"5\" in text\n\n    skipped = nbs / \"eval_false.ipynb\"\n    write_nb(str(skipped), \"%%code\\n#| eval: false\\nraise RuntimeError('should not run')\", replace=True)\n    exec_nb(str(skipped), timeout=5, allow_new=True)\n    assert read_nb(skipped).cells[0].outputs == []\nfinally:\n    remove_demo_path(root)\n\n\nroot, nbs = _make_execute_project(\"03_execute_safety\")\ntry:\n    slow = nbs / \"slow.ipynb\"\n    write_nb(str(slow), \"%%code\\nimport time\\ntime.sleep(2)\", replace=True)\n    exec_nb(str(slow), timeout=1, allow_new=True, safe=False)\n    cell = read_nb(slow).cells[0]\n    assert cell.metadata[\"nbskill\"][_TIMEOUT_HASH_KEY]\n    text = \"\".join(output_text(output) for output in cell.outputs)\n    assert \"ran longer than 1s\" in text\n    exec_nb(str(slow), timeout=1, allow_new=True, safe=False)\n    cell = read_nb(slow).cells[0]\n    text = \"\".join(output_text(output) for output in cell.outputs)\n    assert \"skipped cell\" in text\n    update_cell(str(slow), \"print('fast now')\", cell_id=cell.id)\n    exec_nb(str(slow), timeout=1, allow_new=True, safe=False)\n    cell = read_nb(slow).cells[0]\n    assert _TIMEOUT_HASH_KEY not in cell.metadata.get(\"nbskill\", {})\n    text = \"\".join(output_text(output) for output in cell.outputs)\n    assert \"fast now\" in text\nfinally:\n    remove_demo_path(root)\n\n\nroot, nbs = _make_execute_project(\"03_execute_blocks\")\ntry:\n    blocked_cases = {\n        \"path_write\": \"from pathlib import Path\\nPath('bad.txt').write_text('bad')\",\n        \"open_write\": \"open('bad.txt', 'w').write('bad')\",\n        \"remove\": \"import os\\nos.remove('missing.txt')\",\n        \"rmtree\": \"import shutil\\nshutil.rmtree('missing')\",\n        \"subprocess\": \"import subprocess\\nsubprocess.run(['true'])\",\n        \"magic\": \"%time 1 + 1\",\n        \"shell\": \"!echo unsafe\",\n    }\n    for name, source in blocked_cases.items():\n        nb_path = nbs / f\"{name}.ipynb\"\n        write_nb(str(nb_path), f\"%%code\\n{source}\", replace=True)\n        exec_nb(str(nb_path), timeout=5, allow_new=True)\n        text = \"\".join(output_text(output) for output in read_nb(nb_path).cells[0].outputs)\n        assert text\n        if name in {\"magic\", \"shell\"}: assert \"blocks IPython\" in text\nfinally:\n    remove_demo_path(root)\n\n\nroot, nbs = _make_execute_project(\"03_execute_write\")\ntry:\n    allowed_write = nbs / \"allowed_write.ipynb\"\n    ok_file = root / \"allowed.txt\"\n    write_nb(str(allowed_write), f\"%%code\\nfrom pathlib import Path\\nPath({str(ok_file)!r}).write_text('ok')\\nprint(Path({str(ok_file)!r}).read_text())\", replace=True)\n    exec_nb(str(allowed_write), timeout=5, ok_dests=str(root), allow_new=True)\n    assert ok_file.read_text(encoding=\"utf-8\") == \"ok\"\nfinally:\n    remove_demo_path(root)\n\n\nroot, nbs = _make_execute_project(\"03_execute_httpx\")\ntry:\n    import httpx\n    live = nbs / \"live_httpx.ipynb\"\n    write_nb(str(live), \"%%code\\nimport httpx\\nhttpx.get('https://api.openai.com/v1/test')\", replace=True)\n    exec_nb(str(live), timeout=5, allow_new=True)\n    text = \"\".join(output_text(output) for output in read_nb(live).cells[0].outputs)\n    assert \"blocked live httpx call\" in text\n\n    cached = nbs / \"cached_httpx.ipynb\"\n    url = \"https://api.openai.com/v1/test\"\n    request = httpx.Request(\"GET\", url)\n    key = _cachy_key(request)\n    (root / \"cachy.jsonl\").write_text(\n        json.dumps({\"key\": key, \"response\": \"cached body\", \"headers\": {\"content-type\": \"text/plain\"}, \"status_code\": 200}) + \"\\n\",\n        encoding=\"utf-8\",\n    )\n    write_nb(str(cached), f\"%%code\\nimport httpx\\nr = httpx.get({url!r})\\nprint(r.text)\", replace=True)\n    exec_nb(str(cached), timeout=5, cache_httpx=True, cache_dir=str(root), allow_new=True)\n    text = \"\".join(output_text(output) for output in read_nb(cached).cells[0].outputs)\n    assert \"cached body\" in text\nfinally:\n    remove_demo_path(root)\n\n\nroot, nbs = _make_execute_project(\"03_execute_httpx_miss\")\ntry:\n    miss = nbs / \"miss_httpx.ipynb\"\n    miss_url = \"https://api.openai.com/v1/miss\"\n    write_nb(str(miss), f\"%%code\\nimport httpx\\nhttpx.get({miss_url!r})\", replace=True)\n    exec_nb(str(miss), timeout=5, cache_httpx=True, cache_dir=str(root), allow_new=True)\n    text = \"\".join(output_text(output) for output in read_nb(miss).cells[0].outputs)\n    assert \"no cached httpx response\" in text\nfinally:\n    remove_demo_path(root)\n\n\nwarning_root = demo_path(\"03_execute_uncalled_warning\")\ntry:\n    warning_root.mkdir()\n    warning_nbs = warning_root / \"nbs\"\n    warning_nbs.mkdir()\n    uncalled = warning_nbs / \"uncalled.ipynb\"\n    write_nb(str(uncalled), \"%%code\\ndef unused_demo():\\n    return 1\\n---\\n%%code\\nprint('done')\", replace=True)\n    out = StringIO()\n    with redirect_stdout(out):\n        exec_nb(str(uncalled), timeout=5, allow_new=True, show_output=False)\n    warning_text = out.getvalue()\n    assert \"Execution warnings:\" in warning_text\n    assert \"unused_demo\" in warning_text\nfinally:\n    remove_demo_path(warning_root)\n\n\nwarning_root = demo_path(\"03_execute_called_warning\")\ntry:\n    warning_root.mkdir()\n    warning_nbs = warning_root / \"nbs\"\n    warning_nbs.mkdir()\n    called = warning_nbs / \"called.ipynb\"\n    write_nb(str(called), \"%%code\\ndef used_demo():\\n    return 1\\n---\\n%%code\\nused_demo()\", replace=True)\n    out = StringIO()\n    with redirect_stdout(out):\n        exec_nb(str(called), timeout=5, allow_new=True, show_output=False)\n    assert \"used_demo\" not in out.getvalue()\nfinally:\n    remove_demo_path(warning_root)\n\n\nwarning_root = demo_path(\"03_execute_decorated_warning\")\ntry:\n    warning_root.mkdir()\n    warning_nbs = warning_root / \"nbs\"\n    warning_nbs.mkdir()\n    decorated = warning_nbs / \"decorated.ipynb\"\n    write_nb(str(decorated), \"%%code\\ndef _route(func):\\n    return func\\n\\n@_route\\ndef api_handler():\\n    return 1\", replace=True)\n    out = StringIO()\n    with redirect_stdout(out):\n        exec_nb(str(decorated), timeout=5, allow_new=True, show_output=False)\n    assert \"api_handler\" not in out.getvalue()\nfinally:\n    remove_demo_path(warning_root)\n\n\n\n\nThe write tools can ask for a notebook test immediately after changing a notebook. These helpers summarize saved error outputs so a failed write/test cycle reports the useful cell-level problem.\n\nsource\n\n\n\ndef run_notebook_test(\n    path, timeout:int=30\n):\nCall self as a function.",
    "crumbs": [
      "Executing notebooks"
    ]
  },
  {
    "objectID": "12_knowledge.html",
    "href": "12_knowledge.html",
    "title": "Reference implementation knowledgebase",
    "section": "",
    "text": "This notebook stores favourite implementation repositories in one local LanceDB database. It keeps registered sources, indexed README/module/function/class/method records, and direct same-repo caller/callee links for examples an agent can inspect before writing similar code.\nLibrary searches inspect the target workspace before looking for a version. A relevant installed direct dependency uses its exact version, downloads that public PyPI release only when it is missing, then parses notebooks for nbdev projects and Python files for other projects. The index reuses embeddings for unchanged file hashes and keeps only the newest three indexed versions per package.",
    "crumbs": [
      "Reference implementation knowledgebase"
    ]
  },
  {
    "objectID": "12_knowledge.html#registry-and-storage",
    "href": "12_knowledge.html#registry-and-storage",
    "title": "Reference implementation knowledgebase",
    "section": "Registry and storage",
    "text": "Registry and storage\nThe registry records where reference code came from. It keeps repository metadata separate from indexed implementation rows so re-ingesting one source can replace its records without disturbing the others.\n\nsource\n\nreference_home\ndef reference_home(\n    path:NoneType=None\n):\nReturn the global reference knowledge directory.\n\nsource\n\n\nreference_add\ndef reference_add(\n    url:str, # Repository URL or local repository path\n    name:str | None=None, # Stable registry name; defaults from URL/path\n    version:str='HEAD', # Git ref to ingest\n    package:str | None=None, # Import/distribution package name\n    path:str | None=None, # Override reference home\n):\nRegister or update one reference repository.",
    "crumbs": [
      "Reference implementation knowledgebase"
    ]
  },
  {
    "objectID": "12_knowledge.html#retrieval-and-relevance",
    "href": "12_knowledge.html#retrieval-and-relevance",
    "title": "Reference implementation knowledgebase",
    "section": "Retrieval and relevance",
    "text": "Retrieval and relevance\nThe search layer combines metadata filters, lexical search, vectors, and small domain rules. The rules matter because a query for a specific helper should not be promoted by an unrelated generic name.\n\nSpecific reference relevance\nreference_query separates exact identifier intent from generic query fragments. A hit must cover the specific terms of a multi-concept request; otherwise it is reported as low confidence instead of being promoted by an incidental symbol component.",
    "crumbs": [
      "Reference implementation knowledgebase"
    ]
  },
  {
    "objectID": "12_knowledge.html#repository-extraction",
    "href": "12_knowledge.html#repository-extraction",
    "title": "Reference implementation knowledgebase",
    "section": "Repository extraction",
    "text": "Repository extraction\nIndexed rows retain enough source context to make a returned result useful without opening a whole repository. Python and notebook readers share the same normalized row format.\n\nsource\n\nreference_ingest\ndef reference_ingest(\n    name:str | None=None, # Registry name to ingest\n    all:bool=False, # Ingest all registered repositories\n    path:str | None=None, # Override reference home\n    force:bool=False, # Reingest even when resolved version appears unchanged\n):\nClone registered references and index Python/notebook implementations.\n\nsource\n\n\nreference_list\ndef reference_list(\n    path:str | None=None\n):\nReturn registered reference repositories and indexed item counts.\n\nProblem-solution memory\nEach new pair needs at least four distinct namespaced tags from topic, sub-topic, library, problem-category, and solution-category. Records also keep the repository, commit, verification command, and verification time.\nQueries keep tag filtering exact. Hits report matching tags and score components. A query with no exact match reports the miss, counts broader candidates, and suggests available tags instead of returning a loose match.",
    "crumbs": [
      "Reference implementation knowledgebase"
    ]
  },
  {
    "objectID": "12_knowledge.html#problem-solution-memory-1",
    "href": "12_knowledge.html#problem-solution-memory-1",
    "title": "Reference implementation knowledgebase",
    "section": "Problem-solution memory",
    "text": "Problem-solution memory\nProblem memory records a reusable observation beside the source-code index. It is separate from reference retrieval because an agent may need a prior decision even when no code example is relevant.\n\nsource\n\nproblem_statement_add\ndef problem_statement_add(\n    problem:str, # Reusable problem statement\n    solution:str, # Reusable solution statement\n    task:str='', # Task where this pair was observed\n    project:str='.', # Project path or label\n    evidence:str='', # Short evidence that the solution worked\n    outcome:str='applied', # applied, partial, failed, skipped, etc.\n    tags:NoneType=None, # At least four namespaced tags\n    repository:str='', # Repository containing the verified evidence\n    commit:str='', # Source commit for the evidence\n    verification:str='', # Command that verified the solution\n    verified_at:float | None=None, # Verification time as a Unix timestamp\n    path:str | None=None, # Override reference home\n):\nStore or update one reusable problem-solution statement.\n\nsource\n\n\nproblem_statement_query\ndef problem_statement_query(\n    query:str, # New problem or task to compare against stored memories\n    top_k:int=5, # Number of memories to return\n    project:str | None=None, # Optional project filter\n    tags:NoneType=None, # Optional tags; every requested tag must be present\n    path:str | None=None, # Override reference home\n):\nFind reusable problem-solution statements for a new task.\n\nsource\n\n\nproblem_statement_list\ndef problem_statement_list(\n    path:str | None=None, limit:int=20\n):\nList recent reusable problem-solution statements.\nFastcore 2 migration verification runs this test before the later reference-query setup imports. Keep the demo-path helpers local to the test that uses them.\nSmall reference API examples:\n\nWorkspace-aware queries\nThe query path reads the target workspace before looking up implementation examples. That keeps a direct dependency on its installed version and prevents a local editable package from being replaced by a public download.\n\nsource\n\n\n\nreference_propose\ndef reference_propose(\n    problem:str, # Observed problem in the library\n    repo:str, # Indexed reference repository name\n    version:str | None=None, # Optional indexed version\n    symbol:str | None=None, # Optional affected symbol\n    current_repo:str='.', # Workspace using the library\n    path:str | None=None, # Override reference home\n):\nPrepare an upstream change proposal without creating a pull request.\n\nsource\n\n\nreference_query\ndef reference_query(\n    query:str, # Natural-language implementation query\n    top_k:int=3, # Number of implementation hits to return\n    include_branch:bool=False, # Include direct same-repo callers and callees\n    current_repo:str='.', # Workspace whose installed libraries should be reused\n    repos:NoneType=None, # Optional repo name or comma-separated names to search\n    path:str | None=None, # Override reference home\n    kind:str | None=None, # Optional item kind filter\n    package:str | None=None, # Optional package filter and auto-download target\n    version:str | None=None, # Optional package version\n    module:str | None=None, # Optional module filter\n    symbol:str | None=None, # Optional symbol filter\n    include_local:bool=True, # Include current repository reuse advice\n    candidate_k:int | None=None, # Candidate pool size before final reranking\n    explain:bool=True, # Include score breakdowns and why text\n    allow_download:bool=True, # Download public source for a relevant package\n):\nSearch reference code using the target workspace’s installed version.\n\nexample_home = reference_home(demo_path(\"reference-example-home\"))\nassert example_home.name == \"reference-example-home\"\nremove_demo_path(example_home)\n\n\nexample_vector = _reference_vector(\"make page endpoint\")\nassert len(example_vector) &gt; 100\n\nReference repositories are managed through the Python registry. The executable contract uses a local git repository so ingestion never depends on network access.\n\nRetrieval Experiments\nThis small experiment compares four search surfaces on a tiny structured corpus:\n\ntags_fts: LanceDB full-text search over tags plus symbol/doc text, with no vector similarity.\ndocstring: vector similarity over docstrings only.\nsignature_doc_return: vector similarity over the signature, docstring, and return statements.\nfull_body: vector similarity over the whole indexed source block.\n\nThe expected symbols mirror common fastcore lookups: store_attr, patch, and compose.",
    "crumbs": [
      "Reference implementation knowledgebase"
    ]
  },
  {
    "objectID": "12_knowledge.html#retrieval-experiments-1",
    "href": "12_knowledge.html#retrieval-experiments-1",
    "title": "Reference implementation knowledgebase",
    "section": "Retrieval experiments",
    "text": "Retrieval experiments\nThe experiment compares searchable representations on a tiny fixed corpus. It is kept separate from the production query path so it can explain the tradeoff without changing query behavior.\n\ndef _experiment_returns(source):\n    try: tree = ast.parse(source or \"\")\n    except SyntaxError: return \"\"\n    vals = []\n    for node in ast.walk(tree):\n        if isinstance(node, ast.Return) and node.value is not None:\n            try: vals.append(ast.unparse(node.value))\n            except Exception: pass\n    return \"\\n\".join(vals)\n\n\ndef _experiment_text(row, mode):\n    if mode == \"docstring\": return row.get(\"docstring\", \"\")\n    if mode == \"signature_doc_return\":\n        return \"\\n\".join([row.get(\"signature\", \"\"), row.get(\"docstring\", \"\"), _experiment_returns(row.get(\"source\", \"\"))])\n    if mode == \"full_body\": return row.get(\"source\", \"\")\n    return \" \".join([*(row.get(\"tags\") or []), row.get(\"symbol\", \"\"), row.get(\"signature\", \"\"), row.get(\"docstring\", \"\")])\n\n\ndef _experiment_table(home, rows, mode):\n    table_rows = []\n    for row in rows:\n        text = _experiment_text(row, mode)\n        table_rows.append({**row, \"search_text\": text, \"vector\": _reference_vector(text)})\n    table = _reference_db(home).create_table(f\"reference_experiment_{mode}\", data=table_rows, mode=\"overwrite\")\n    try: table.create_fts_index(\"search_text\", replace=True)\n    except Exception: pass\n    return table\n\n\ndef _experiment_search(table, query, mode, top_k=3):\n    if mode == \"tags_fts\":\n        rows = table.search(query, query_type=\"fts\").limit(top_k).to_list()\n    else:\n        rows = table.search(_reference_vector(query)).metric(\"cosine\").limit(top_k).to_list()\n    return [row[\"symbol\"] for row in rows]\n\n\nexperiment_home = demo_path(\"reference-experiment-home\")\nexperiment_rows = [\n    _item_row(\"mini\", \"v1\", \"mini\", \"function\", \"mini.basics\", \"store_attr\", Path(\"mini.py\"),\n              \"def store_attr(names=None, self=None, **attrs):\\n    return _store_attr(self, **attrs)\",\n              signature=\"def store_attr(names=None, self=None, **attrs)\",\n              docstring=\"Store params named in comma-separated names from calling context into attrs in self.\"),\n    _item_row(\"mini\", \"v1\", \"mini\", \"function\", \"mini.basics\", \"patch\", Path(\"mini.py\"),\n              \"def patch(f=None, *, cls_method=False):\\n    return patch_to(cls_method=cls_method)(f)\",\n              signature=\"def patch(f=None, *, cls_method=False)\",\n              docstring=\"Decorator: add f to the first parameter's class based on type annotations.\"),\n    _item_row(\"mini\", \"v1\", \"mini\", \"function\", \"mini.basics\", \"compose\", Path(\"mini.py\"),\n              \"def compose(*funcs):\\n    def _inner(x):\\n        for f in funcs: x = f(x)\\n        return x\\n    return _inner\",\n              signature=\"def compose(*funcs)\",\n              docstring=\"Create a function that composes all functions in funcs.\"),\n    _item_row(\"mini\", \"v1\", \"mini\", \"function\", \"mini.foundation\", \"flatmap\", Path(\"mini.py\"),\n              \"def flatmap(f, xs):\\n    return [y for x in xs for y in f(x)]\",\n              signature=\"def flatmap(f, xs)\",\n              docstring=\"Apply f to each element and flatten the results into a single list.\"),\n]\nexperiment_queries = {\n    \"store constructor arguments as attributes\": \"store_attr\",\n    \"patch a method onto a class\": \"patch\",\n    \"compose functions into a pipeline\": \"compose\",\n}\nexperiment_results = {}\ntry:\n    for mode in (\"tags_fts\", \"docstring\", \"signature_doc_return\", \"full_body\"):\n        table = _experiment_table(str(experiment_home), experiment_rows, mode)\n        experiment_results[mode] = {\n            query: _experiment_search(table, query, mode, top_k=3)\n            for query in experiment_queries\n        }\nfinally:\n    remove_demo_path(experiment_home)\nexperiment_results\n\n\nexperiment_accuracy = {\n    mode: sum(hits and hits[0] == expected for query, expected in experiment_queries.items() for hits in [mode_results[query]])\n    for mode, mode_results in experiment_results.items()\n}\nassert experiment_accuracy[\"signature_doc_return\"] &gt;= experiment_accuracy[\"docstring\"]\nassert experiment_accuracy[\"signature_doc_return\"] &gt;= experiment_accuracy[\"full_body\"]\nexperiment_accuracy",
    "crumbs": [
      "Reference implementation knowledgebase"
    ]
  },
  {
    "objectID": "12_knowledge.html#version-aware-sources",
    "href": "12_knowledge.html#version-aware-sources",
    "title": "Reference implementation knowledgebase",
    "section": "Version-aware sources",
    "text": "Version-aware sources\nA relevant direct dependency is indexed at the version the target workspace uses. Public PyPI downloads are only a fallback when that exact source is not already available.\n\nReusing local repositories\nreference_discover stores reference roots and registers Git repositories at the root or one directory below it. Local names include a path hash, so two repositories with the same folder name remain separate.",
    "crumbs": [
      "Reference implementation knowledgebase"
    ]
  },
  {
    "objectID": "12_knowledge.html#local-repository-discovery",
    "href": "12_knowledge.html#local-repository-discovery",
    "title": "Reference implementation knowledgebase",
    "section": "Local repository discovery",
    "text": "Local repository discovery\nDiscovery only registers Git repositories at configured roots. Limiting the scan to roots and their direct children keeps a background index from unexpectedly walking an entire home directory.\n\nsource\n\nreference_discover\ndef reference_discover(\n    roots:NoneType=None, # Comma-separated or iterable reference roots\n    path:str | None=None, # Override reference home\n    ingest:bool=True, # Index each discovered repository\n    force:bool=False, # Re-ingest unchanged repositories\n):\nRegister direct-child Git repositories from configured roots.\n\nWorkspace versions and upstream proposals\nreference_query asks the target workspace’s virtual environment which distributions are installed. It only downloads public source for a relevant package, so returned code can match the version the project already uses. reference_propose writes review material only; it never creates a fork or pull request.",
    "crumbs": [
      "Reference implementation knowledgebase"
    ]
  },
  {
    "objectID": "12_knowledge.html#small-public-examples",
    "href": "12_knowledge.html#small-public-examples",
    "title": "Reference implementation knowledgebase",
    "section": "Small public examples",
    "text": "Small public examples\nreference_home is deliberately simple. It makes the storage location explicit, which is useful when an agent needs an isolated database for a test or a workspace-specific index.\n\nreference_home(\"demo-knowledge\")\n\n\nQuery planning\n_query_feature_plan makes the non-obvious parts of retrieval visible before any database work starts. It records the requested evidence, likely implementation kinds, and the search passes that will be applied.\n\n_query_feature_plan(\"find a FastHTML route test\")\n\nThe focused check keeps the public storage location and query-intent classification stable without requiring LanceDB, a model, or a repository fixture.\n\n\nLazy embedding dependency\nSentence-transformers is imported only when vector search is needed. That keeps registry and metadata commands usable without loading a model at module import time.",
    "crumbs": [
      "Reference implementation knowledgebase"
    ]
  }
]